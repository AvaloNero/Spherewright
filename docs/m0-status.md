# M0 status — First Red Matrix

Scope revised: 2026-08-31 (Asia/Singapore).

Execution decision: continue M0 development and DSP verification on the local computer. The LAN game-host topology is documented in `docs/remote-validation.md` and explicitly deferred until the local first-red-matrix run is repeatable.

| Gate | Status | Evidence / earliest gap |
|---|---|---|
| Gate A — environment, load, secure status | complete | DSP `0.10.34.28529`; BepInEx loaded once per process; protected current-user ACL and token rejection verified; MCP Inspector called the stdio server; normal exit removed the descriptor. Revalidate after protocol surface changes, but the foundation is complete. |
| Gate B — ordinary owned world and structured observation | complete | The same owned world is currently active on planet `104` after strict resume, reusable-checkpoint retries and bidirectional native flight; it remains confirmed peaceful, 1x, non-sandbox and write-healthy. The original M0 acceptance identity remains preserved below; no unrelated save was enumerated or loaded. |
| Gate C — normal-play action primitives | complete | Movement, harvesting, handcrafting, research, drone construction, belts, sorters, production configuration, transfer, refuel and explicit save all have live evidence. The co-located-sorter attribution fix is live-validated by distinct refinery outputs `164 -> 163` and `181 -> 170`, with action ownership resolving only the new entity and session health remaining `healthy`. |
| Gate D — first red matrix | complete | Lab `256` was configured to runtime recipe `18` with output item `6002` at 0, then normally fed by graphite sorter `257` and hydrogen sorter `258`; output grew `0 -> 3 -> 6`. Save action `b399facb-48cd-4838-b7ab-9c9762b6def7` confirmed the exact owned save at tick `2499658`, revision `150 -> 151`, with `ownedSaveState=saved` and `writeHealth=healthy`. |

The M0 milestone is **complete**. Its first post-M0 improvements are also complete: lab `256` now outputs through sorter `261` into storage `260`, and wind turbine `262` raised network 2 capacity from `15000` to `20000` with full service under the observed `16528` running demand. Later product milestones are also complete: power-engine assembler `285` outputs through sorter `288` into storage `287`; chemical plant `552` produces plastic; smelters `530/715` produce titanium ingots and diamonds; the gear/coil/motor chain fills storage `727`; water pump `752` supplies storage `753`; chemical plant `760` produces organic crystals; assembler `767` produces titanium crystals; lab `774` produces structure matrices; assembler `814` produces electromagnetic turbines; smelters `841/842` produce stone-derived high-purity silicon; assembler `848` produces microcrystalline components; chemical plant `861` produces sulfuric acid; assembler `853` produces processors; assembler `883` produces particle containers; assembler `891` produces logistics drones; and assembler `898` produces planetary logistics stations. These post-M0 milestones do not retroactively weaken the completed M0 evidence.

## Post-M0 continuation state and evidence boundary

- The latest exact-primary planned restart normally saved tick `8640914`, closed the old process, deployed the stable research-selection and mecha-reservation build as one matching assembly set, then resumed only the protected owned world on planet `104`. Fresh reads confirmed peaceful/non-sandbox/1x, healthy writes, and the mecha's real matrix reservations. Stable action `e54dddc4-aac3-40a8-a3bd-beacae3c80c9` queued Planetary Logistics `1604` behind the active Particle Magnetic Trap `1703` without weakening the full progression observation hash. Sorter `551` was safely reconfigured while cargo-free, and an exact 200 automatic circuit-board transfer into storage `26` restored lab `76`; `1703` then completed normally at tick `8696391` and research advanced to `1604`.
- Particle container is now a completed product milestone. Only after runtime recipe `99` became unlocked did action `d2209d94-80bd-4be8-a753-452534fe4c5a` configure the previously idle assembler `883`. Its topology is input storage `884 -> 887/888/889 -> 883(recipe 99) -> 886 -> 885`, with exact filters for electromagnetic turbine `1204`, copper ingot `1104`, and graphene `1123`. Over the acceptance window all three staged inputs declined by 10, the full-powered assembler worked with live input buffers, and dedicated output storage `885` reached 2 item `1206`. Journal sequence 38 durably recorded the first production-line particle container at tick `8698306` (`2026-09-02T12:34:53.509099+08:00`, in-save `001d 16:16:11`), with `persistencePending=false` and no persistence error. Save action `0252c2a1-4618-43cc-bd1a-8fa6d0ca105c` persisted the same healthy owned world at tick `8699182`, revision `52`.
- Planetary logistics station is now a completed product milestone. Its four-input topology is `899 -> 902/903/904/905(filter 1103/1106/1303/1206) -> 898(recipe 93) -> 901 -> 900`. Fresh reads proved the staged 40 steel, 40 titanium ingots, 40 processors, and 20 particle containers plus all four exact filters before action `8f90d632-b90b-4ff1-b9d1-fe20850153c2` enabled the already-unlocked recipe once. The source and assembler inputs then reached zero, assembler `898` completed at full power, and dedicated storage `900` gained one item `2103`. Journal sequence 42 durably recorded the first production-line station at tick `9410766` (`2026-09-02T20:20:29.8130148+08:00`, in-save `001d 19:34:06`), with no pending persistence or error. Save action `8dedea0d-2003-4e3f-80be-71db3e5a176e` persisted tick `9413535`, revision `115`, with healthy writes and an exact-primary restart ticket.
- The same owned world has now resumed from the protected planned-restart ticket. The prior process normally saved tick `8340400` (in-save `001d 14:36:46`), revision `676 -> 677`, exited without a forced kill, and left the fixed handoff ticket. The new process accepted only that ticket's exact primary with minimum tick `8340400` and planet `104`; adoption completed and automatically resaved under the same owned identity. Fresh reads proved tick `8342796+`, planet `104`, `confirmed_peaceful`, `confirmed_disabled`, 1x, healthy writes, journal `36/false/null`, and Particle Magnetic Trap progress above the prior `92929/288000` boundary. At tick `8474662` research was `221818/288000`; the three prepared assemblers `883/891/898` remained recipe zero and fully powered. One initial plus 80 bounded attempts to append tech `1604` were rejected before commit because the deployed full progression hash includes the continuously changing uploaded amount; the queue remained `[1703]`. Source now provides a dedicated stable selection hash with 83 passing tests, but it remains intentionally undeployed until a normal save/close boundary. Existing processor and titanium lines then supplied the exact remaining 10 processors and 40 titanium ingots through conserving transfers; storage `899` now holds 40 each steel/processor/titanium and only lacks 20 particle containers. Save action `dd2fc858-6720-46fb-86d9-12a9b11d525e` persisted that staging at tick `8474115`, revision `30`, with healthy writes.
- At the previous 2026-08-31 shutdown, save action `387a4629-f1b4-4c40-ad6b-10f15e840219` confirmed the then-current primary owned save at tick `4409247`; process `24828` accepted `CloseMainWindow` and exited without a forced kill. That historical continuation point was later resumed and has now been superseded by the tick `5731056` handoff below; its stale recovery tickets must not be used.
- Lab `256` remains recipe `18`. Storage build `750c7803-c967-4996-a056-63fcb0efcac8` and inserter build `7ae664fe-246f-41e8-bf85-f68270bf3262` created `256 -> 261 -> 260`; the output storage then held 22 energy matrices, while the lab output buffer was 0, both inputs were 4 and `isWorking=true`. Turbine build `135158e1-b16b-4ebb-9dbe-85d358f05217` created entity `262` on network 2, raising capacity `15000 -> 20000` and restoring running service ratio 1.0.
- Network 1 now has a renewed iron source and a continuous power-engine chain. Miner `263` covers iron nodes `2/4`; belts `264…281` and sorter `282` feed the still-valid old ore belt at `17`, restoring magnet storage `30`. Sorter `70` is intentionally filtered to item `2001` so storage `28` cannot flood the shared belt with iron ingots; after the old backlog drained, coil assembler `73` resumed and storage `26` reached 60 coils. Recipe-105 assembler `285`, input sorter `289`, output sorter `288` and storage `287` produced power engines `9 -> 30` on fully served network 1 (`31981/65000`, ratio 1.0). Storage `286` is a valid but too-distant utility storage and is not part of the product topology.
- Mecha Core level 1 technology `2101` completed normally at tick `3744134`. Storage `287` later supplied 50 power engines and verified coal node `316` supplied 150 coal; Drive Engine level 1 (`2901`) completed normally at tick `3821275`, consuming those exact item requirements. Storage `26` and lab `84` then supplied the matrix requirements for Mecha Core level 2 (`2102`, unlock tick `3932513`) and Drive Engine level 2 (`2902`, unlock tick `4013644`). The research queue is empty, sail capability is unlocked, and core capacity is now `400 MJ`. A short move ended within 17.61 m of coal node `316`; one normal harvest cleared the old deployed movement order. Storage `114 -> player -> fuel chamber` then conserved 100 energetic graphite (`3000 -> 2900`, `0 -> 100`), and native generation reached full `400/400 MJ` with 91 graphite plus an active reactor item remaining before the latest save.
- A later movement collision occurred near the dense network-1 factory. The old deployed Plugin waited for its global movement timeout even after repeated reads proved no displacement. One post-M0 Computer Use jump was then used to recover the already completed/saved world and distinguish collision from target or energy failure. This input is explicitly outside the M0 acceptance boundary and is not evidence for a structured movement capability.
- A separate post-M0 request accidentally reused factory entity ID `106` as resource node ID `106`, sending the old DLL toward an unrelated far-side iron vein. The same session was recovered without restart: baseline generation funded one bounded waypoint, verified coal node `346` supplied 60 coal through two conserving harvests, and eight roughly 37 m surface waypoints returned the player to the factory. Source now rejects harvest targets outside `WithinPlayerBuildArea` and the MCP description explicitly distinguishes node IDs from factory IDs.
- The deployed Plugin now contains the 180-tick physical-stall watchdog, 600-tick best-target-progress watchdog, power-starvation classification isolation, move/harvest single-flight, exact `OrderNode` reference ownership, and harvest-range enforcement. Live action `ed605c94-10df-409b-91db-08c6aea4e0d5` stopped its exact owned movement order after less than `0.75 m` displacement over 180 ticks with `27.36 m` remaining; the player ended at speed zero with `400/400 MJ`, so the physical-stall path is now live-verified. The other watchdog branches retain their own verification triggers.
- Same-star native flight and exact retry protection now have bidirectional live evidence. Flight checkpoint tick `4617708` protected the `104 -> 102` outbound run; return checkpoint tick `4808424` survived two Plugin restarts and several exact reloads while landing logic was corrected. The final return action `d95955e7-cd86-48dd-b79f-4cb54734863c` completed only after 600 continuous grounded Walk ticks, followed by a 10-second identical-position/speed-zero readback. The 1000 titanium cargo remained conserved, and normal save action `c6d7c88e-0c36-4c15-af29-3844a124ddc5` persisted the stable home landing at tick `4819163` with healthy writes.
- The latest old-DLL return trip invalidated the earlier 50%-energy departure heuristic. A completed move continued consuming energy after its terminal action result, and reaching a production-area waypoint produced only the roughly 80 kW baseline recovery. Reading wireless tower `180` at `(-108.25,-28.83,-165.93)` and moving within 2.47 m instead produced about 20.765 MJ over 8 seconds (roughly 2.60 MJ/s) and filled the 200 MJ core. Until the exact-order fix is safely deployed, long returns start full or with usable fuel and verify position, speed and energy immediately after every waypoint.
- Per user workflow, continue this same save while it remains healthy. Every newly completed product line must be verified in its producer, saved normally, then paired with one Git commit and push containing its implementation and updated experience evidence.
- The requested protected per-save journal is deployed. It records first manual output from DSP's persisted forge-completion feature counters, first automated output independently from factory production registers, and first technology/upgrade selections using the runtime tech page, with ISO actual time plus raw/formatted in-save time. This existing save attached at tick `4428079` with `historicalCoverageComplete=false`; its protected SHA-256-derived journal file was created, and selecting Basic Chemical Engineering (`1121`) produced a live `technology_first_selected` entry at tick `4462081`. Manual chemical-plant output and production-line plastic now provide live item-event evidence; a first upgrade-selection event remains to be live-verified. New worlds still receive complete prospective coverage.
- The active owned session is `905747a6-21cd-4782-81c0-9abeb5b5536a` on planet `104`, peaceful/non-sandbox/1x with healthy writes. Structure-matrix milestone save action `d6e2d8d5-9675-4eb9-a64b-b05403d0af9f` persisted tick `6905142`, revision `455 -> 456`, including the complete running motor, water, organic-crystal, titanium-crystal, and structure-matrix lines. Steam and Windows identity remain excluded from ownership evidence.
- Titanium ingot is now a completed product milestone. Titanium Smelting `1413` unlocked normally at tick `5692950`; only then was powered smelter `530` configured to recipe `65`. Over the acceptance window storage `259` fell from 1000 to 986 titanium ore, input sorter `532` carried item `1004`, output sorter `533` worked, and storage `531` accumulated 6 item `1106`. Journal sequence 8 independently recorded the first production-line titanium ingot at tick `5702401`, and milestone save action `996eb4ad-92fe-4c81-b42f-3da74ed52e85` persisted tick `5705293` before the later final handoff save.
- Diamond is now a completed product milestone. Crystal Smelting `1403` unlocked normally at tick `6081424`; only then was staged smelter `715` configured to recipe `60`. Input storage `716` fell `400 -> 354 -> 350`, sorter `720` carried item `1109`, the smelter worked at full power, and output storage `717` grew `0 -> 42 -> 47` through sorter `719`. Journal sequence 9 independently recorded the first production-line diamond at tick `6083748`; milestone save action `2e9ca24b-57dc-40c6-900b-a210b8fc03e7` persisted tick `6090507` with healthy writes.
- Electric motor is now a completed product milestone. Empty storage `723` first received four exact-filter outputs: `746` sends iron to belt `728`, `747/748` send magnet/copper to coil assembler `725`, and `749` sends iron directly to motor assembler `724`. Only then were 100 copper, 200 magnets, and 500 iron staged. The gear branch is `728…732 -> 745 -> 726(recipe 5) -> 743 -> 740…735 -> 744 -> 724`; the coil branch is `725(recipe 6) -> 741 -> 724`; sorter `742` drains recipe-97 motors to storage `727`. All producers and sorters read full power, source counts fell, the intermediate sorters carried items `1101/1201/1202`, and storage `727` reached 39 item `1203`. Journal sequences 11 and 12 recorded the first production-line gear and electric motor; the motor event occurred at tick `6209433` (`2026-09-01T13:04:11.9629982+08:00`, in-save `001d 04:44:50`). Save action `d0516fbf-b333-4266-aed1-dbdd5cd53e37` persisted tick `6221009` with healthy writes.
- Water is now a completed product milestone. Four automatic motors were conservatively transferred from storage `727`, and ordinary recipe `49` produced one owned water pump. DSP's native click-build validation accepted entity `752` on water; it runs on power network 1 at service ratio 1.0 and emits runtime water item `1000`. A direct pump-to-storage sorter prepare correctly failed because the pump exposes no ordinary inserter pose. The valid topology is pump output `752 -> 758…754 -> 759 -> 753`; sorter `759` was observed full-powered and carrying water, while dedicated storage `753` grew `9 -> 31`. Journal sequence 13 recorded the first manual pump and sequence 14 recorded the first production-line water at tick `6245078` (`2026-09-01T13:14:06.2593487+08:00`, in-save `001d 04:54:44`). Save action `44f2f0d0-9713-4e35-9073-45f1ce5c7787` persisted tick `6267723` with healthy writes.
- Organic crystal is now a completed product milestone. Powered chemical plant `760` runs recipe `25`; empty storage `761` was connected first through exact-filter sorters `763/764/765` for plastic `1115`, refined oil `1114`, and water `1000`, with output `760 -> 766 -> 762`. Existing automatic sources supplied 200 plastic plus 100 each oil/water through conserving normal transfers only after all filters were read back. Each input count fell, the plant worked at service ratio 1.0, and dedicated storage `762` grew organic crystal `1 -> 7`. Journal sequence 15 independently recorded first production-line item `1117` at tick `6312637` (`2026-09-01T13:32:52.5005411+08:00`, in-save `001d 05:13:30`). Save action `06cfa947-25da-490f-9e52-895989ff8e7a` persisted tick `6315704` with healthy writes.
- Titanium crystal is now a completed product milestone. Before High-strength Crystal `1123` completed at tick `6509179`, assembler `767` remained recipe `0` while storage `768` was conservatively staged with 40 organic crystals and 120 automatic titanium ingots behind exact-filter sorters `770/771`; output is `767 -> 772 -> 769`. Only after runtime recipe `26` became unlocked did action `74b451af-ed4b-47df-b0cf-8bd5b0b5a933` configure the assembler. Input fell to 29 organic crystals/88 titanium ingots, the assembler worked at full service, and storage `769` accumulated 8 item `1118`. Journal sequence 16 recorded first production-line titanium crystal at tick `6511499` (`2026-09-01T14:28:07.1667461+08:00`, in-save `001d 06:08:44`); sequence 17 then recorded normal selection of tech `1124`. Save action `39cee465-5520-4a8c-a1e3-68ac8e6208ab` persisted tick `6518917` with healthy writes.
- Structure matrix is now a completed product milestone. While tech `1124` was still locked, lab `774` remained recipe `0`; storage `775` was staged with 40 automatic titanium crystals and 40 automatic diamonds behind sorters `776/777`, filtered to items `1118/1112`, and output was preconnected as `774 -> 779 -> 778`. Tech `1124` completed normally at tick `6894549`; only then did action `c5e65c3c-11b0-4b03-9f00-36b3a95d98f3` configure runtime recipe `27`. The full-powered lab worked with six of each input buffered, source counts fell `40 -> 26 -> 23`, and output storage grew `7 -> 10` item `6003`. Journal sequence 18 recorded first production-line structure matrix at tick `6898014` (`2026-09-01T16:15:29.8733116+08:00`, in-save `001d 07:56:06`); save action `d6e2d8d5-9675-4eb9-a64b-b05403d0af9f` persisted tick `6905142`, revision `455 -> 456`, with healthy writes.
- Plastic is now a completed product line. Graphite storage `114` feeds the two-belt relay `553…554` through sorters `555/556`; storage `557` received the exact 200 refined oil previously freed from the refinery co-product backlog and feeds sorter `560`; chemical plant `552` runs recipe `23` and sorter `561` drains into storage `558`. Pole `559` brought the plant onto network 2 at service ratio 1.0 (`2400/20000` observed demand/capacity). Output grew `0 -> 2 -> 38`, and the protected journal recorded its first production-line plastic at tick `5263306` with both actual and in-save time.
- The legacy circuit-board route was not isolated: unfiltered sorter `551` pulled magnet item `1102` from mixed storage `26`; sorter `563` now drains that polluted lane into recovery storage `562`. Later endpoint reads proved old sorters `572`, `595`, and coil-input `284` no longer had device-side ownership because the old builder reused occupied machine slots. After deploying occupied-slot selection and endpoint reverse verification, sorters `714` (`36 -> 571`), `721` (`580 -> 36`), and `722` (`26 -> 73`) restored circuit and coil supply without overwriting existing ports. Blue-lab coil/circuit buffers both rose `5 -> 6`; Crystal Smelting advanced `47354 -> 48923` over 25 seconds and continued to at least `61045/90000`.
- The attempted storage-to-plastic route `163 -> 675 -> 596…674 -> 676 -> 557` is physically connected and endpoint-powered, but it is not a valid pure-oil line: storage `163` later contained oil 459 plus hydrogen 13, and unfiltered sorter `675` contaminated `557` to oil 33 plus hydrogen 271. This supersedes the earlier permanent-line claim and must be cleaned/re-filtered before reuse. Replacement sorter `678` still restores thermal-generator fuel delivery and the observed network-3 `51000` capacity/full service evidence remains valid.
- A second recipe-16 refinery `707` was connected in safe order: empty filtered hydrogen/oil outputs `708/709`, then crude input `710`. Pole `711` powers the hydrogen endpoint; turbines `712/713` raised network 2 to `30000` capacity against `29428` observed demand, restoring full service to red-matrix lab `256`. High Polymer Chemistry `1122`, Titanium Smelting `1413`, Crystal Smelting `1403`, Electromagnetic Drive `1701`, High-strength Crystal `1123`, and Structure Matrix `1124` are complete. Recipe `27` is unlocked and running in lab `774`; the research queue is empty at the structure-matrix save boundary.

## Historical evidence excluded from revised M0

The earlier sandbox basic-line actions `9170d11c-e458-4d11-b1d1-a406b818d890` and `e77fd86a-2e5f-474b-a1a4-f845252be221` proved several current-version build, recipe, connection, power, idempotency, and readback call paths. They also granted building items, called `BuildFinally`, and inserted ore directly. They are research evidence only and do not satisfy ordinary-gameplay Gates B, C, or D. `Spherewright.Plugin.csproj` explicitly excludes their coordinator from the current Plugin binary.

No pre-existing save has been enumerated, loaded, or read.

## Current ordinary-play evidence

- The same owned world is active on planet `104` after a healthy planned restart loaded only the ticket-bound exact primary saved at tick `7198197`; neither Steam nor Windows account identity was trusted. Startup compared the legacy return-flight checkpoint with that exact primary header, persisted it as retired, and SessionState exposes no reload capability. Journal sequence `20` is also reported durable through `20` with no pending persistence.
- Post-resume production repair added refined-oil sorter `290` on a distinct pose `2.60 m` from the old collision point, restoring refinery `141` and raising stored red matrices `263 -> 272`. The legacy blue-matrix cluster had several disconnected co-located sorters, so no old pose was covered. Separate coil, circuit-board, and blue-output belt routes with new sorters `296/297`, `311/312`, `321/322`, and `330/331` restored lab `76` and research lab `84`; Basic Chemical Engineering hash advanced `720 -> 1123`, providing a live consumption proof. A failed shared five-belt input attempt is retained as the counterexample that motivated independent input lanes.
- After the titanium return, storage `260` was connected to research lab `84` through source sorter `342`, seven validated belt-build actions totalling 196 ordinary belts (`332…528` with action-local ordering), and destination sorter `529`. The terminal sorter held item `6002`, lab red-matrix buffer rose `0 -> 5900`, full-power research started, and High Polymer Chemistry `1122` advanced `0 -> 428` hash. Save action `3d69b565-341d-491f-b0ee-2f5aba93abdc` persisted the same owned world at tick `5046241` with healthy writes.
- The post-flight iron source has now been renewed a second time after miner `263` exhausted its bound veins. Full-power miner `796` covers still-live nodes `1/3/6`; belts `806…805 -> 811…807` and sorter `812` side-feed belt `264`, while sorter `795` restores `20 -> 21`. Sorter `812` physically carried iron ore, smelter `21` resumed ingot production, steel smelter `791` consumed those ingots, and storage `792` grew `0 -> 2` steel as Maglev research advanced `24300 -> 24480`. A normal same-world save persisted the repair at tick `7325343`, revision `39`, with healthy writes and journal durability `20/20`.
- Post-return research repair added circuit-board input sorter `537` to assembler `36`; the first output route was later proven polluted and replaced by the pure-source route described below. Separately, the one-time 200-oil transfer that initially restored refinery `141` has been replaced by a permanent storage-to-plastic belt, while refinery `707` doubles hydrogen production. High Polymer Chemistry `1122` and Titanium Smelting `1413` have since completed normally.
- A later live read invalidated the assumption that route `551 -> 542…300` was dedicated: sorter `551` had no filter and carried magnet item `1102` from mixed storage `26`, while lab `76` again lacked circuit boards. Recovery storage `562` plus sorter `563` now drains that polluted lane. The replacement circuit route is `36 -> 572 -> 571…565 -> 573 -> 76`; its continuous iron feed is `28 -> 594 -> 593…580 -> 595 -> 36`. Observed iron left storage `28`, both iron sorters carried item `1101`, assembler `36` output item `1301`, lab `76` worked with six circuit boards, and research `1122` advanced at least `35643 -> 36180 -> 37800/72000`. No manual matrix transfer or weakened stale-state check was used.
- The completed plastic topology is `114 -> 555 -> 553…554 -> 556 -> 552(recipe 23)`, plus `557 -> 560 -> 552 -> 561 -> 558`. Direct `114 -> 552` sorter preparation correctly failed `TooFar`; the two-belt relay passed independent endpoint preparations and carried graphite normally. Storage `558` reached 38 plastic, the journal recorded the first production-line event, and save action `97c456db-b083-4604-aa75-62273b37f709` persisted tick `5265117`.
- The titanium-ingot topology is `259 -> 532 -> 530(recipe 65) -> 533 -> 531`. Automatic input consumption and output accumulation, journal sequence 8, milestone save tick `5705293`, and final handoff save tick `5731056` provide the full product boundary.
- The diamond topology is `716 -> 720 -> 715(recipe 60) -> 719 -> 717`. Automatic graphite consumption, continuous output accumulation, journal sequence 9, and milestone save tick `6090507` provide the full product boundary.
- The electric-motor topology is `723 -> 746 -> 728…732 -> 745 -> 726(recipe 5) -> 743 -> 740…735 -> 744 -> 724(recipe 97)`, plus `723 -> 747/748 -> 725(recipe 6) -> 741 -> 724`, direct iron `723 -> 749 -> 724`, and output `724 -> 742 -> 727`. Filtered source cargo, working intermediates, 39 stored motors, journal sequence 12, and milestone save tick `6221009` provide the full product boundary.
- The water topology is `752 -> 758…754 -> 759 -> 753`. Native water placement, pump/belt endpoint ownership, full-powered water cargo, storage growth, journal sequence 14, and milestone save tick `6267723` provide the full product boundary.
- The organic-crystal topology is `761 -> 763/764/765 -> 760(recipe 25) -> 766 -> 762`, with the three source sorters filtered to plastic/oil/water before loading. Conserved automatic inputs, working full-powered chemistry, storage growth `1 -> 7`, journal sequence 15, and milestone save tick `6315704` provide the full product boundary.
- The titanium-crystal topology is `768 -> 770/771 -> 767(recipe 26) -> 772 -> 769`, with organic-crystal/titanium-ingot filters configured before staging and the recipe kept at zero until unlock. Automatic input consumption, full-powered output accumulation, journal sequence 16, and milestone save tick `6518917` provide the full product boundary.
- The structure-matrix topology is `775 -> 776/777 -> 774(recipe 27) -> 779 -> 778`, with titanium-crystal/diamond filters configured before staging and the lab kept at recipe zero until tech `1124` unlocked. Automatic input consumption, full-powered output growth `7 -> 10`, journal sequence 18, and milestone save tick `6905142` provide the full product boundary. Its inputs were conservatively moved from existing automatic product storages, so this proves automatic matrix conversion but not yet interplanetary raw-input logistics.
- The logistics-drone topology is `892 -> 895/896/897 -> 891(recipe 94) -> 894 -> 893`, with the three source sorters already filtered to iron ingot/processor/thruster and the assembler held at recipe zero until Planetary Logistics `1604` completed normally at tick `8836460`. Action `532666d4-de0d-4ea9-abfc-6a44657fe555` enabled the unlocked recipe once. All staged inputs were consumed, the full-powered assembler and output sorter carried item `5001`, dedicated storage `893` reached 10 unproliferated logistics drones, and journal sequence 41 durably recorded the first automatic output at tick `9346766` (`001d 19:16:19`). Save action `e8d96a21-75d7-4f7c-a60b-598646f7f754` persisted the same healthy owned world at tick `9369181`, revision `112`.
- The planetary-logistics-station topology is `899 -> 902/903/904/905(filter 1103/1106/1303/1206) -> 898(recipe 93) -> 901 -> 900`. It consumed the exact staged `40/40/40/20` steel/titanium/processor/particle-container batch through native sorters and a full-powered assembler, and storage `900` received one item `2103`; journal sequence 42, milestone save tick `9413535`, revision `115`, and healthy writes provide the product boundary. That exact item was later conserved through storage-to-player transfer and ordinary prebuild/drone construction into entity `916`, 29.45 m from the player, with no retained prebuild or player item. The first station read exposed the native local-PLS raw `planetId=0` sentinel and therefore remains blocked by the deployed reader's too-strict planet check; the source correction and 101 offline tests pass, but station configuration/fleet operation are not yet claimed until a normal redeploy and live reread.
- The electromagnetic-turbine topology is `727 -> 815(filter 1203) -> 814(recipe 98)`, plus `725 -> 825 -> 824…818 -> 826(filter 1202) -> 814 -> 828 -> 827`. Directly binding a belt to assembler `725` correctly failed because it exposes no native belt port, so the coil branch uses an independently validated free belt with sorter endpoints. Both input sorters carried their filtered items, assembler `814` worked at full service with motor/coil inputs, and storage `827` grew from empty to 8 and then 22 item `1204`. Journal sequence 22 independently recorded first production-line output at tick `7414129` and was durable through 22 with no persistence error; milestone save tick `7419065`, revision `84`, and healthy writes provide the product boundary.
- The high-purity-silicon topology reuses the still-live stone miner `86` and directed main belt `87…121`: `121 -> 844 -> 841(recipe 34) -> 845 -> 842(recipe 59) -> 846 -> 843`, with pole `847` restoring downstream network-1 coverage. Miner buffer fell `50 -> 37`, the first smelter consumed stone ore and emitted silicon ore, the second received that intermediate at full service, and dedicated storage `843` grew from empty to 5 and then 7 item `1105`. Journal sequence 26 independently recorded first production-line high-purity silicon at tick `7514032` and was durable through 26 with no persistence error; milestone save tick `7517473`, revision `169`, and healthy writes provide the product boundary. The 10:1 stone recipe is explicitly temporary until remote silicon can be automated.
- The microcrystalline-component topology is `843 -> 850(filter 1105)/851(filter 1104) -> 848(recipe 53) -> 852 -> 849`. Both input filters and reverse slots were verified before 100 automatic copper ingots were conservatively transferred into the continuously supplied high-purity-silicon storage. Copper fell `100 -> 89`, assembler `848` worked at full service with both inputs, and dedicated storage `849` grew from empty to 8 and then 15 item `1302`. Journal sequence 27 independently recorded first production-line output at tick `7542841` and was durable through 27 with no persistence error; milestone save tick `7545277`, revision `200`, and healthy writes provide the product boundary.
- The sulfuric-acid topology is `862 -> 865(filter 1114)/866(filter 1005)/867(filter 1000) -> 861(recipe 24) -> 864 -> 863`. Before staging, belt `128 -> 859 -> 95` established an automatic stone-ore buffer from the existing pure stone line. Existing automatic sources then conservatively supplied 120 refined oil, 160 stone ore, and 80 water only after all three input filters and the output endpoint were verified. All three inputs declined, chemical plant `861` worked at full service, and dedicated storage `863` grew from empty to 7 item `1116`. Journal sequence 28 independently recorded first production-line output at tick `7661594` and was durable through 28 with no persistence error; milestone save tick `7663628`, revision `283`, and healthy writes provide the product boundary.
- The processor topology is `849 -> 855(filter 1302)/856(filter 1301) -> 853(recipe 51) -> 857 -> 854`. The assembler remained at recipe zero with all logistics and power prebuilt until tech `1302` completed normally at tick `7702430`; only then was recipe `51` enabled once. Microcrystalline components declined `58 -> 45`, circuit boards declined `100 -> 82`, assembler `853` worked at full service with both inputs, sorter `857` carried item `1303`, and dedicated storage `854` grew from empty to 6 and then 17 processors. Journal sequence 29 independently recorded first production-line output at tick `7704459` and was durable through 30 with no persistence error; milestone save tick `7707489`, revision `306`, and healthy writes provide the product boundary.
- The graphene topology is `870 -> 873(filter 1109)/874(filter 1116) -> 869(recipe 31) -> 872 -> 871`, with pole `875` bringing the plant and all three sorters onto full-served network 1. The chemical plant stayed at recipe zero until Applied Superconductor `1131` completed normally at tick `7745153`; only then was recipe `31` enabled once. Existing automatic-product storages conservatively supplied 180 high-energy graphite and 60 sulfuric acid. Over the 15-second proof window the shared input storage declined from graphite `173 -> 154` and sulfuric acid `59 -> 50`; input/output sorters were physically active, dedicated storage `871` grew from empty to 14 graphene, and one more item remained in the plant output buffer. Journal sequence 32 independently recorded first production-line item `1123` at tick `7849705` and was durable through 33 with no persistence error; milestone save tick `7854029`, revision `403`, and healthy writes provide the product boundary. These one-time conserved transfers prove automatic chemical conversion, not continuous logistics-tower supply.
- The thruster topology is `877 -> 880(filter 1103)/881(filter 1104) -> 876(recipe 20) -> 879 -> 878`. Assembler `876` and its logistics remained at recipe zero until tech `1113` completed normally at tick `8098237`; only then was recipe `20` enabled once. Existing automatic-product storages conservatively supplied 120 steel and 180 copper. During the first proof window steel declined `120 -> 106`, copper declined `180 -> 164`, and dedicated storage `878` grew `0 -> 4 -> 10`, later reaching 60 thrusters. Initial network-1 service was only about `0.825` at `80000/102622` capacity/demand, so five normally crafted and click-built wind turbines `910–914` raised capacity to `105000` and restored consumer and assembler service to `1.0`. Journal sequence 34 independently recorded first production-line item `1405` at tick `8101277` and was durable with no persistence error; milestone save tick `8123715`, post-save read tick `8123723`, revision `639`, and healthy writes provide the product boundary.
- The M0 acceptance snapshot is the exact owned session `c51a4fd0-5a50-4fb2-8520-f7acf216334d` on planet `104` at revision `151` after the structured Gate-D save. Peaceful mode, non-sandbox mode, 1x resources, ownership and healthy writes are all structured readbacks. The continuing owned world later reached revision `639` at the thruster milestone and was finally saved offline at revision `677`, tick `8340400`. The post-M0 boundary above prevents later recovery inputs from being retroactively included in Gate D.
- The existing normal factory supplies electromagnetic matrices, graphite, crude oil and continuously extracted energy matrices. Oil extractor `129` feeds belts `151…161`, sorter `162`, refinery `141` recipe `16`, refined-oil sorter `164` and hydrogen sorter `181`.
- Coverage pole `182` moved crude-input sorter `162` from `network=0/Picking` to network 3 with actual crude cargo. Refined oil then grew in storage `163`; it also normally fuels thermal generator `183` through sorter `184`, raising network 3 capacity from `15000` to `51000` and restoring service ratio 1.0.
- Hydrogen leaves tank `165` through three ordinary belt-build actions: `49013865-bb55-4aca-899a-699e8d3744e7`, `3870525c-14ce-4e41-936c-36984d560858`, and `8f733ab2-a4da-4616-ae03-ee5778299ba3`, ending at belt `255` beside the red lab. Two one-item hydrogen returns caused by live-belt extension were separately recorded in player inventory and explicitly excluded from production evidence.
- Lab `256` began with recipe 18 buffers `graphite=0`, `hydrogen=0`, `6002=0`. Sorters `257` and `258` connected the 3000-count normal graphite storage `114` and hydrogen belt `255`; the same lab then showed inputs growing and energy matrices `0 -> 3 -> 6` while production progress advanced.
- The post-M0 output chain `256 -> 261 -> 260` drained the earlier 10-item lab buffer and continued to at least 22 stored matrices while the lab resumed work. Wind turbine `262` is explicitly on network 2; at `energyRequired=16528`, the `20000`-capacity network served all demand.
- The second post-M0 product milestone is the continuous recipe-105 chain `26 -> 289 -> 285 -> 288 -> 287`. Storage `287` first held 9 power engines and later 30 while assembler `285` remained working at power ratio 1.0; this proves accumulation rather than a one-time buffered transfer. The exact world was then normally saved at tick `3746997`.
- Final save action `b399facb-48cd-4838-b7ab-9c9762b6def7` confirmed DSP's normal save API at tick `2499658`; it did not accept a client-provided save name and did not enumerate another save.
- The earlier session `73b4019b-c5cc-4f90-b1f4-bc4abc6d49c6` and its quarantine remain historical regression evidence only. Its failed attribution was repaired, deployed, and independently live-validated before the current completion evidence was accepted.

## Current offline verification

- Current takeover machine: portable SDK 8.0.424 builds the full `Spherewright.sln` with 0 warnings and 0 errors; 94 tests pass (Contracts 11, Bridge.Core 66, MCP 17).
- Current takeover machine: `dotnet restore Spherewright.sln --locked-mode --source https://api.nuget.org/v3/index.json` now succeeds after removing the SDK-dependent implicit .NET Framework umbrella package; the committed lock file remains unchanged. The explicit source is required because this user's global NuGet configuration is currently empty.
- With explicit user approval, official `xiaoye97-BepInEx-5.4.17` and the development Plugin were installed. DSP build ID `23109513` and `Assembly-CSharp.dll` SHA-256 `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85` match the verified baseline.
- Steam discovery now de-duplicates case-only variants of the same validated DSP root instead of reporting a false multiple-installation error.
- Idempotency entries now honor `IdempotencyRetentionMinutes`, reclaim expired capacity, and isolate identical UUIDs between sessions; a concurrent reservation test proves only one insertion wins within a session.
- Build completion snapshots pre-existing co-located sorter IDs before `CreatePrebuilds` and excludes them when attributing the constructed entity. The regression test reproduces old entity `211` and new entity `213`; the deployed fix was additionally live-validated by current entities `164/181` at one source pose with distinct destinations and correct new-action ownership.
- MCP assembly-registration test now expects exactly 46 public tools, adding two-phase, conservation-checked logistics-station fleet transfer to the owned-session gameplay journal, same-star flight, exact-proof quarantine reconciliation, one-time owned-world resume and lifecycle-bounded exact flight-checkpoint reload; it still explicitly rejects any `basic_production_line` registration. The running game retains the preceding 44-tool build until its next normal save/close deployment boundary.
- Healthy planned restart now loads only the exact primary owned save sealed inside the protected ticket; quarantine recovery alone may use fresh fixed LastExit to preserve unsaved progress. Both sources must pass a header-tick minimum before commit and the full identity/planet/mode checks after load. The first live deployment recovery selected the ticket-bound primary at minimum tick `7198197`, consumed the token behind two durable token-hash tombstone replicas, and immediately armed a new planned-restart generation.
- ILSpy class listing of the rebuilt Plugin contains `NormalGameActionCoordinator` and the ordinary `TestWorldCoordinator`, but no `BasicProductionLineCoordinator`.
- Current-DLL inspection proves research-result `FadeOut()` immediately makes `ready` false (`alpha 1.0 -> 0.999`) before the native close animation, preventing repeated acknowledgement. Factory DTO and canonical hash coverage bound the live lab `256` before and after input, and runtime evidence now proves item `6002` changed from 0 to 6.
- Source now captures `TankComponent.fluidId/fluidCount/fluidInc` as `tank-fluid`; this read-only enhancement builds and tests cleanly but is intentionally not hot-deployed into the healthy completed session. Current M0 hydrogen proof instead uses the refinery output sorter, complete belt topology, lab input and output deltas.
- The first v0.3 source slice now deep-copies completed logistics-station identity, energy, fleet, route, storage-slot and belt-port state into existing factory list/inspect responses. It cross-checks entity/station/planet identities and emits independent live/configuration hashes. That slice was deployed before the exact owned-world resume, but the continuing world still has no completed station, so no station DTO has yet been accepted as live evidence.
- The next v0.3 source slices adopt `PlanetTransport.SetStationStorage`'s no-inventory-mutation subset, the exact `UIStationWindow` maximum-charge field path, and a dedicated player/station fleet transfer. Storage allows empty-slot selection or same-item configuration only: no clear/replace, no outstanding orders, unlocked item, researched capacity, 100-item UI steps, separate configuration-hash binding, and immediate slot/player inventory conservation. Charge accepts only 3 MW UI steps within the exact prefab's integer 0.5x–5x range, cross-binds the power consumer, and preserves current request plus station/player inventory. Fleet transfer accepts only real player-held drones/vessels, counts working craft against prefab capacity, withdraws only idle craft, rejects proliferated deposits, and proves bilateral conservation plus unrelated-state preservation. The current source builds cleanly with 94 tests; these station writes are not yet deployed or live-validated because no completed station exists in the continuing world.
- Follow-up API verification corrected charging semantics before any station prepare could rely on them: `StationComponent.energyPerTick` is tick-live requested energy assigned by `SetPCState`, whereas `consumerPool[pcId].workEnergyPerTick` is the UI-configured maximum. The DTO now exposes requested/configured energy-per-tick and watt values separately; only the configured maximum enters the station configuration hash.
- Source now detects powered movement stalls through independent physical-displacement and best-target-progress windows, preserves the dedicated power-starvation reason, and refuses a second simultaneous move/harvest player order. Five pure Core tests cover the watchdog boundaries; the full Plugin compile covers the integration, while live validation waits for a safe same-save restart.
- Source classifies runtime build-catalog item `2306` as `water-pump` when its native `PrefabDesc.minerType` is Water. Current-DLL fields and click-build validation were inspected, and the currently deployed same-batch Plugin then completed the missing live boundary: ordinary recipe `49` produced the owned item, DSP's native water check accepted entity `752`, drones completed it, and its dedicated output chain accumulated water in storage `753`. The catalog role and ordinary construction capability are now live validated on the current DSP build.
- Fresh ordinary-world verification also confirmed 314 technology states, 161 recipes, 174 items, runtime red-matrix item `6002`, recipe `18`, and the runtime dependency recipe set `16, 17, 18, 32, 58, 74, 121`.
- The same-key new-game commit replay returned the original action with `idempotentReplay=true`; a resource cursor reused under a different filter returned `STALE_CURSOR`.
- The historical BepInEx log retains the expected earlier quarantine event for regression evidence. It is not confused with the current healthy session or used to weaken the live completion and final-save readbacks above.
