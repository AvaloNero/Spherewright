# Bridge protocol

Protocol version: `1`.

Each message is a four-byte little-endian signed length followed by UTF-8 JSON. Negative lengths and lengths over 1 MiB are rejected before allocation. A zero-length frame is accepted by the frame layer and rejected by the JSON layer.

The first message on every Pipe connection must be an authenticated `handshake` carrying the active `bridgeInstanceId` and startup token from the current-user-only runtime descriptor. Any pre-authentication request or invalid credential closes the connection.

## Current public MCP surface

Unreleased0.4 extends the existing Foundry read with an alternative explicit `blueprint` layout and free boundary ports. `construction` binds material intent, exact stage counts, allocated internal item paths, whole machine/logistics/power cost and per-object dependencies; `blueprintSite` contains the native assessment and separate power capture. The read stays executable=false. Optional `foundryIntent` plus `expectedFoundryPlanHash` on the existing finite blueprint prepare binds that graph to ordinary execution, with revalidation at commit. Protected progress preserves the original intent/hash; resume forbids replacement intent and budgets only remaining objects' power. This is blueprint-backed composition, not arbitrary auto-layout, native transport-capacity proof or continuous supply proof. Unsupported coproduct stages, non-default/banned storage and mixed transport layouts explicitly reject or block.

Normal dismantle additionally supports ordinary2011/2012 sorters, including a missing end with all PRESENT links proven reciprocal. It checks bounded incoming references, native recoverable cargo and refund capacity before the ordinary business call; afterward it proves disappearance, count/inc recovery and surviving endpoint configuration/other links. Native instant-dismantle, advanced stacking and prebuild endpoints remain unsupported. The tool does not repair connection fields or automatically rebuild a replacement.

The MCP server also exposes the direct text resource `spherewright://agent/playbooks/opening-movement-v1`. It is available without a live Bridge and gives the Agent the compact core operating rules for session ownership, idempotent terminal polling, energy and harvest approach, production-line proof, saving, flight recovery, and bounded movement-stall escape. The `spherewright_get_status` payload and the session-state, Move, and new-world tool descriptions point clients to this resource before their first gameplay action.

```text
spherewright_get_status
spherewright_get_session_state
spherewright_get_player_state
spherewright_get_progression_state
spherewright_get_gameplay_journal
spherewright_get_local_star_system
spherewright_get_recipe_catalog
spherewright_get_build_catalog
spherewright_get_foundry_plan
spherewright_get_governor_plan
spherewright_inspect_blueprint
spherewright_export_blueprint
spherewright_get_blueprint_builds
spherewright_prepare_blueprint_build
spherewright_commit_blueprint_build
spherewright_prepare_cancel_blueprint
spherewright_commit_cancel_blueprint
spherewright_get_power_summary
spherewright_get_overseer_diagnostic_bundle
spherewright_get_overseer_production
spherewright_get_overseer_summary
spherewright_get_action_result
spherewright_list_resource_nodes
spherewright_inspect_resource_node
spherewright_list_factory_entities
spherewright_inspect_factory_entity
spherewright_list_assemblers
spherewright_inspect_assembler
spherewright_prepare_new_game
spherewright_commit_new_game
spherewright_prepare_save_import
spherewright_commit_save_import
spherewright_prepare_move
spherewright_commit_move
spherewright_prepare_interplanetary_flight
spherewright_commit_interplanetary_flight
spherewright_prepare_harvest
spherewright_commit_harvest
spherewright_prepare_handcraft
spherewright_commit_handcraft
spherewright_prepare_select_research
spherewright_commit_select_research
spherewright_prepare_build
spherewright_commit_build
spherewright_prepare_dismantle
spherewright_commit_dismantle
spherewright_prepare_upgrade
spherewright_commit_upgrade
spherewright_prepare_configure_building
spherewright_commit_configure_building
spherewright_prepare_transfer
spherewright_commit_transfer
spherewright_prepare_logistics_station_fleet_transfer
spherewright_commit_logistics_station_fleet_transfer
spherewright_prepare_refuel
spherewright_commit_refuel
spherewright_prepare_save
spherewright_commit_save
spherewright_prepare_quarantine_reconciliation
spherewright_commit_quarantine_reconciliation
spherewright_prepare_resume_owned_game
spherewright_commit_resume_owned_game
spherewright_prepare_reload_flight_checkpoint
spherewright_commit_reload_flight_checkpoint
```

`prepare_new_game` and `commit_new_game` now describe only a peaceful 1x non-sandbox world. The old sandbox basic-production-line methods are not registered as MCP tools and are excluded from M0.

### Detail-only belt cargo observation (0.4 development slice)

`spherewright_inspect_factory_entity` adds nullable `beltCargo` on exact completed local belt entities. It is separate from `buffers`, not populated in paginated lists, and not part of existing `stateHash`, `configurationStateHash` or `endpointStateHash`; those action semantics and versions are unchanged. The field is a read-only single-tick observation, not a write token or preservation proof.

`state=observed` gives `capturedAtGameTick`, native path/segment identity and bounds, `pathClosed`, `cargoStackCount`, `itemCount` and item-sorted `items` (item/count/native inc and stack count). Coverage is `unique_stacks_touching_selected_belt_segment`: a stack crossing a segment edge can appear in both adjacent reads, so the results must not be summed into path inventory. Empty counts prove only this selected segment was empty at that tick, not zero flow, a stalled belt, or an empty entire path.

The adapter copies at most530 bytes (512 selected cells plus9 guards per side), checks the native ten-cell representation and zero-based cargo IDs, and confirms each distinct reference via bounded `CargoPath.GetCargoAtIndex` before copying item/stack/inc scalars. It neither scans the whole path/container nor traverses other factories. Null/missing means not observed; `state=unavailable` returns a fixed `reasonCode`, null totals and no partial items. Oversized/out-of-range segments, unresolved seam fragments, malformed/ambiguous cargo references, absent pool identity or failed native readback never become successful zero counts. Current native component and owned-world checks remain in force. Belt upgrades still require a separate full affected-path/chunk/cargo proof and are not enabled by this observation.

### Governor proposal (0.4 development slice)

Factory observations retain component and pagination boundaries: `list_assemblers` is not an all-producer/consumer query. Matrix production is in `componentKind=lab`, with research mode and matrix-point units handled separately. Complete the relevant immutable filtered pages and actual bounded directed connections before declaring absence. The current entity reader does not capture `CargoPath` cargo into belt `buffers`; an empty list means **unobserved**, not verified empty. Diagnose transport using reciprocal endpoints, observed sorter/device buffers and native production windows. Do not clear storage or construct a duplicate consumer based on a partial trace.

`sourceStateHash` uses `governor-source-v2`: stable selected identity/pose/reciprocal endpoints, recipe, native acceleration mode, sorter filter/targets, storage settings and power-network identity. It deliberately excludes live buffers, cargo/proliferation, processing progress, inserter carrying/stage and instantaneous power ratio. Those remain measured evidence/stock deltas and blocker inputs. The earlier development binding used the volatile configuration hash and could reset every normal production window; it is not valid evidence of production instability. `FactoryEntitySnapshot.forceAccelerationMode` is copied from native assemblers. Endpoint hashes now also bind this mode and sorter filter, so changing either invalidates an old explicit blueprint selection without making ordinary cargo movements stale. Old process tokens/hashes are never reused after cold deployment.

`spherewright_get_governor_plan` is read-only: `phase=governor_proposal`, `executable=false`, `balanced=false`. Select 1–32 exact current-owned local entities with fresh endpoint hashes/recipe IDs, target item/rate, tolerance (0–0.5, exclusive zero), and a 36000–216000 game-tick validation window. Optional external supply and recipe choices reuse the Foundry compiler. This proposal is not a write plan, approval token, automatic expansion loop or completed acceptance result.

Measurements reuse the same-tick Overseer local-planet item counters; they are **not per-selected-machine production counters**. The proposal checks whether the selection contains all direct target producers. Baseline metadata is bounded to eight selection series per session and three independent 600-tick windows; overlapping reads cannot manufacture independent evidence. New session, changed source/scale, regressed game time or sampling gaps invalidate the old baseline; windows predating the new binding do not qualify. Three stable nonzero windows only establish a candidate baseline, not ten-minute post-expansion success.

`supply` separates full target-chain demand, actual production/consumption, nonnegative local production surplus, additional demand/supply shortfall, and selected-buffer stock/deltas with their own observation interval. Consumption is a lower bound under starvation; local surplus excludes imports, reservations and transport. Production minus consumption is **not** an inventory delta. Power/logistics summaries, bounded findings and reciprocal-link blockers retain their evidence scope.

Alternatives compare supported native upgrades (whole target device cost and old-device refund), additional Foundry machine capacity, and an explicitly selected copyable module. Costs/power labelled as machine-only exclude unplanned logistics and generating infrastructure. Predicted nameplate gains never become measured gains; `actualTargetGainPerMinute` remains unknown. Preserve the pre-execution baseline, target, tolerance and window outside subsequent proposals; use fresh normal prepare/commit for the selected finite work and then separately prove at least ten game minutes. The current proposal tool does not yet persist or certify that acceptance window.

#### Governor explicit parallel-construction comparison

Governor optionally accepts `parallelExpansionBlueprint`, using the same explicit `FoundryBlueprintRequest` (code, native site and free boundary ports). Before a stable nonzero baseline with a positive increment, it returns no full comparison and explicitly reports that the layout is unassessed. Otherwise `parallelExpansion` contains the existing Foundry plan and its reusable construction `intent`, at **target minus measured baseline**. Its rate basis is `target_minus_measured_nonzero_baseline_v1`; no caller can supply historical rates. Same-capture session/planet/tick/revision, exact code hash/position/rotation/player-bound site assessment, original material graph, complete construction hash and native power capture are checked together. The optional envelope uses the existing 256KiB code/site limits and at most33 ports, with the stricter Foundry shape checks still applied.

`alternatives[].costScope` distinguishes `machines_only`, `whole_upgrade_devices`, `selected_module_objects` and `all_explicit_module_objects_external_infrastructure_excluded`. A full parallel comparison includes every explicit building, logistics and power object, native placement/full inventory/technology, full-base-load power and rated channel budgets. Unplanned external supply/sink infrastructure is not priced or credited. `bounded_construction_ready_at_capture` means the existing construction plan can be freshly prepared; blockers produce `bounded_construction_blocked` plus `parallel_construction_not_ready`, never an executable/balanced claim or actual gain. The proposal fingerprint is `governor-proposal-v2`, with an additional `governor-parallel-proposal-v1` binding when applicable. No new tool, executor, approval store or gameplay write is added. Use the identical explicit layout, returned intent and construction hash with the existing prepare/commit flow; post-expansion observation omits the layout and keeps the previously locked declaration. Restart still requires fresh finite-build reconciliation and discards passive Governor history.

#### Foundry native transport budget

`construction.transportBudget` describes rated full-power, single-item, no-backpressure capacity, not measured production. The native catalog exposes nullable `beltSpeedRaw`, `inserterSttRaw` and `inserterGrade`; unknown values never become free capacity. Every internal and boundary belt traverses its capacity edge in the bounded flow allocation. Basic2011/2012 sorter capacity includes the translated preview's native span;2013 stacking capacity is explicitly unavailable to this composition, without changing the ordinary blueprint allowlist. Shared allocated routes count once each against each channel. `allocatedRatePerMinute` may be below the requested demand when the graph is blocked: always check `internalFlowsRouted`, `canPrepare` and blockers, not the budget alone. `transportCapacityVerified` stays false in planning reads. Rated inputs, allocations and availability are hashed into the immutable plan; continuation rechecks current catalog rules and cannot reuse a stale material/transport contract. It still needs automatic boundary supply/sinks, full power and live sustained-throughput evidence.

#### Governor pre-execution throughput declaration

`get_governor_plan` optionally accepts `validationBaselineProposalHash`, never caller-provided baseline rates/history. A ready, healthy, attributable proposal with no unresolved `targetChainFindings` can be retained as one latest candidate per existing bounded source series (`validationBaselineAvailable=true`). These findings come from the target item diagnostic, a selected object or a path containing a selected object; all bounded planet findings remain visible, and `unattributedPlanetFindingCount` explicitly preserves the remainder without inventing a route to the selected line. Truncation still blocks; absence of attributed findings is not automatic upstream-supply proof. The first request using its exact hash must occur before any accepted write/source change, at the same revision, within3600 game ticks, with output still matching the baseline. It locks copied baseline/target/tolerance/duration/recipe-scale scalars. At most8 locked declarations exist per session; no silent eviction, persistence, old-session restoration or new write token is added. The selected source may legitimately expand after locking, but the measurement streak then waits for windows wholly after that new configuration.

`throughputValidation` records contiguous game-tick coverage from native600-tick rolling windows. Overlap is counted only once; duplicates and wall-clock time do not add duration. A one-tick uncovered gap starts a new streak. Zero/out-of-tolerance production, unavailable native windows, sampled power/diagnostic/attribution/write-health failure and tick regression clear observed progress without changing the declaration. Changed target, tolerance, duration, item/planet/session or recipe scale rejects. Thirty-six thousand covered ticks prove the requested ten-game-minute window; `doubleThroughputTargetObserved` additionally requires an exactly2× target and tolerance<=10%. This is a bounded passive observer, not a build or expansion loop. `balanced` remains false: sampled health is not continuous tick-level health proof, and resource conservation, upstream automatic supply, transport, save/resume and complete-version gates remain separate. The observer was deployed in the617-test development cohort; its first live lock rejected unattributed global depleted-mine findings. The scoped correction has separate offline evidence and requires new same-session live validation after deployment.

Foundry and blueprint site previews optionally expose `power`, a separately hashed one-capture assessment. The read-only adapter copies bounded native network-node coordinates and reserves every existing consumer's max(work,idle,current) and charger demand, current exports, planned unproliferated machine/sorter demand and existing unpowered consumers picked up by new nodes. Core uses native maximum connection radii (not their sum), conservative projected coverage and planned-node component joins; ambiguous disconnected covers or near-boundary geometry are explicit blockers. Only new native wind generation is credited. Limits:32 planned objects,512 existing nodes,256 networks,8192 active consumer scan and2048 unassigned consumers. `fullBaseLoadBudgetSatisfied` is current capacity/coverage evidence only, not fuel sustainability, an executable plan or a write capability. Its hash includes the captured tick and is deliberately separate from construction assessment hashes; power sampling cannot make an otherwise unchanged native build plan stale every tick. Unknown capture returns `state=unavailable`, never presumed zero demand. Native build conditions and post-construction power/throughput inspection remain authoritative.

### Blueprint data and native upgrades (0.4 development slices)

Ordinary `prepare_build` sorter selection also preserves the native candidate-selection guard that precedes `CheckBuildConditions`: exact straight endpoint poses must face each other (maximum angular error below14 degrees for two devices, below11 when touching a belt). This bounded adapter does not reproduce native bent/offset searches. `TooSkew` candidates are rejected before native preview initialization; current endpoint IDs are revalidated before pool access. No existing object is moved or repaired automatically. Blueprint site objects additionally expose nullable `inputEndpointFacingDot/outputEndpointFacingDot`: a negative value means that connected device slot faces away from the other sorter endpoint; belt/open ends report null. These are diagnostics, not permission to clear `ErrorInserterData` or bypass any native condition. A historically working sorter is not necessarily a valid blueprint seed.

The finite subset also accepts ordinary unstacked storage2101. Current native parameters are exactly110 integers: banned-grid count, mode0/9, eight reserved zeros, then100 grid filters. The bounded reader rejects unknown/ignored settings; the Plugin verifies native capacity, bans, unused filter tail and valid stackable item IDs before native parameter application. `FactoryEntitySnapshot.storageConfiguration` exposes actual `gridCount`, `bannedGridCount`, mode and ordered `gridFilterItemIds` (bounded100, null if unavailable). It is not build-catalog's physical connection `slotCount`. Factory, endpoint and configuration hashes bind this optional tagged configuration, leaving non-storage encoding unchanged. Export rejects stack links and proves configuration roundtrip; completed-object reconciliation proves current native capacity/bans/mode/all filters. It deliberately ignores subsequent normal cargo movement: no cargo is serialized or copied. New storage still costs one complete item and uses the ordinary prebuild/drone path; no direct configuration or inventory writes are added.

`FactoryBufferSnapshot` adds `countUnit` and `unitsPerItem` without changing existing raw `count/inc`. Ordinary buffers use `items`/1; lab `research-matrix` buffers use `research_matrix_points`/3600. They represent research-point equivalents, not raw items available for transfer or complete technology budgets.

`inspect_blueprint` accepts an explicit `blueprintCode`; `export_blueprint` accepts `entities:[{objectId,expectedEndpointStateHash,expectedRecipeId}]` in the current owned local planet. Both require `sessionId/planetId`. Export uses only positive completed IDs, at most 64, within 64 m of the first selected entity. It never scans or writes files. Native `GenerateBlueprintData` and `ToBase64String` produce the code; source connections to unselected entities are returned separately in `sourceBoundaryConnections`, not silently treated as copied links. Cargo is never copied.

Input is bounded before native import: 256 KiB UTF-8 code, 128 KiB compressed, 1 MiB decompressed, 64 objects, 8 areas, 256-grid drag/area dimensions, 128 parameters per object. Only header0/1, payload2/patch1 and building marker -102 are supported. Native MD5F signature and byte-exact native import/export roundtrip must pass. The supported data subset is smelter2302, manufacturing assemblers2303–2305 (default or boolean acceleration mode), belts2001–2003 without labels, sorters2011–2013 (native span1–3/filter), Tesla tower2201 and wind turbine2203. Other types, settings, content strings, terrain reform, malformed references, unsupported versions or excess data explicitly reject the whole inspection. Titles/descriptions are returned under `untrusted*` fields and are never instructions.

The result remains **`phase=blueprint_inspection`, `executable=false`**. It includes supported poses/configurations, native indices/slots, boundaries, unlock flags and package-only aggregate `constructionItems`. Optional `site:{position,quarterTurns,expectedPlayerStateHash,stateHashVersion}` adds a read-only native site assessment with its own hash: at most32 objects, 64m radius, closed internal sorter ends and acyclic belt dependencies. Whole budget, native blueprint capacity, item/recipe/filter unlocks, range, terrain/tropic, collisions, occupied entities/prebuilds and every native condition must pass. No covers, implicit external matching, upgrades or demolition are permitted in this path. `nativeBlueprintObjectLimit` reports actual history capacity. The assessment issues no token and never commits on its own.

`prepare_blueprint_build` binds either explicit code/site plus its assessment hash, or one exact owned `resumeBuildId` plus its fresh progress hash (never replacement code/site). Both require fresh player hash and `maximumObjectsToSubmit`1..32. It reuses normal session/revision/GameData binding, short-lived tokens and UUID idempotency. `commit_blueprint_build` returns actionId/buildId. A local bounded executor submits at most one dependency-ready native prebuild per game tick, then waits for normal drones. Aggregate action results include `blueprintBuildId/blueprintBuildPhase/blueprintObjects`; each object tracks not_submitted, submitting, pending_construction, completed, blocked or outcome_unknown, exact inventory before/after hashes and IDs/ticks. This is not atomic whole-plan success. The action pauses successfully at its batch limit; phase `completed` requires all objects proved.

Progress is stored atomically under a current-user protected, exact-owned-identity path (32 plans/4MiB), never a save picker. Write-ahead evidence is flushed before each native create; exact one-item debit and prebuild/configuration/reciprocal proof follow it. A crash in that gap is unknown, not retryable. `get_blueprint_builds` without buildId returns summaries only, no fresh stateHash. Exact buildId reads reconcile immutable configuration, known entity/prebuild identity, all connections including free ends and non-regressing game tick; newly observed completion metadata must persist before exposure. Restart restores evidence, not tokens/authority. Fresh prepare continues only unsubmitted objects; whole-module replay is forbidden. Active finite construction excludes competing normal actions. Unknown results quarantine writes.

`prepare_cancel_blueprint` uses buildId and fresh exact progress hash; commit remains bound to the same build/action while natural construction may advance. `commit_cancel_blueprint` stops future submissions only, retains every existing prebuild/entity, and never auto-demolishes/refunds/claims rollback. Poll both actions to terminal. These new execution paths are source/offline-tested development capabilities; live module-copy/cancel/restart acceptance is recorded separately, not implied by tool discovery or unit tests.

`prepare_upgrade` takes `objectId,targetItemId,expectedRecipeId,expectedFilterItemId,expectedEndpointStateHash,expectedPlayerStateHash,stateHashVersion`, with the normal session/planet binding. The filter defaults to0 for no filter. This executable subset accepts completed manufacturing assemblers2303/2304/2305 to a higher unlocked native-family grade, and ordinary sorter2011→2012 only. `get_build_catalog.buildings[].supportedUpgradeTargetItemIds` advertises the currently unlocked subset. It rejects other types/downgrades, missing materials/refund space, busy/out-of-range player, changed revision, identity, recipe/filter/acceleration mode and inconsistent reciprocal connections. Sorters must have exactly both required factory links (slot1 input/pick and slot0 output/insert), each with a valid reciprocal endpoint; positive cached target IDs do not excuse a missing link. Sorter binding also includes native span/offsets/second-end pose. Live cargo is not frozen by prepare; the executor captures and proves it immediately around the synchronous native call.

`commit_upgrade` uses the existing `CommitNormalActionRequest`, single-flight dispatch, short-lived plan store, UUID idempotency cache and `actionId` terminal polling. The exact native `PlayerAction_Build.DoUpgradeObject` consumes **one complete higher-grade device**, then refunds the old one. It does not just consume incremental recipe ingredients. Current DSP resets assembler processing/extra progress; a basic sorter retains its native cycle fraction with rounded rescaled time and normal1–3-cell span. Readback excludes pre-existing target-prototype IDs when finding a unique result at the original pose, without assuming stable ID; verifies recipe/filter/mode, cargo/proliferation, reciprocal links, second-end pose, native speed/power and exact player count deltas. `get_action_result.upgradeReadback` carries the immediate native-call tick, old/result IDs, recipe/filter, verified connection count, native timing policy/progress and before/after buffers; it is not a later moving-device sample. Always also require a terminal/succeeded result and inspect `itemDeltas`. An unproved result quarantines writes; it is never replayed or automatically rolled back. Normal save/protected resume remain separate actions. Increased nameplate speed is not Governor throughput acceptance.

### Foundry material draft and machine-site preview

The same runtime recipe catalog now reports item `fuelHeatValueJoules`, `fuelType` and nullable `acceptedAsMechaFuel`. The last field uses the existing normal-refuel predicate: positive ItemProto heat/type AND the actual bounded native `StorageComponent.itemIsFuel` flag. Missing table evidence is null, not false permission. Item names and unlock status do not substitute for that predicate; nor does heat alone prove reactor burn rate, range or sustainable production. This read adds no fuel, enables no auto-refuel loop, and does not alter native one-stack transfer, fresh state hashes or conservation requirements.

`spherewright_get_foundry_plan` is a read-only helper for the current owned local planet. It takes `sessionId`, `planetId`, `targetItemId`, `targetRatePerMinute`, optional `externalSupplyItemIds`, and optional `{itemId, recipeId, buildingItemId}` choices. A zero building ID selects the lowest compatible available grade. It uses current unlocked runtime recipes and base machine speed, aggregates shared inputs, and returns dependency-ordered stages, machine counts/cost, working-power watts, external input rates and explicit byproduct rates. Rates are items per game minute at full power without proliferation; raw items form external-supply boundaries automatically.

Without `site`, the result retains `phase=material_plan`, `executable=false` and `remainingChecks`. `machineCost` excludes belts, sorters, storage and power infrastructure. No source inventory, candidate site, automatic supply, native collision/terrain validity or finished line is proved. There are at most 64 recipe stages, 16 levels and 256 production machines; cycles, unavailable selections, unused choices and unsafe numeric budgets are rejected. The material `planHash` is deterministic across captures but is not a live `stateHash`, write token, persistent plan or ownership capability. All actual construction still requires fresh existing single-action prepare/commit and terminal readback.

Optional `site` accepts `{origin:{x,y,z}, yawDegrees, columns, columnSpacing, rowSpacing}`. The origin must be within 10 m of the current local planet surface; yaw is `[0,360)`, columns are `1..8`, spacing is `4..32 m` (defaults `0/4/12/12`). The Core helper lays out a dependency-ordered centred grid using DSP's spherical tangent frame; each machine must be within 64 tangent metres of the origin. This phase is limited to **32 machines**, not the material compiler's 256, and evaluates one grid without searching other sites. Plugin snaps each position through the native planet grid, then Core checks pairwise conservative build-collider bounding spheres plus 0.5 m clearance. Overlaps may reject tight but otherwise legal layouts; they must never silently become accepted placements.

The response has `phase=site_preview`, still `executable=false`, and a `site` assessment. Each placement reports its stable stage/machine ID, snapped position/yaw, `nativeCheckPerformed`, `nativeCheckPassed`, `nativeBuildCondition`, nullable `occupiedObjectId` (positive entity, negative prebuild), rejection and symmetric `overlappingPlacementIds`. Non-overlapping machines use the current structured build validator, including its player-UI and existing-object collider guards, then an isolated native preview copying real inventory without adding virtual items. Entity/prebuild scan cursors are bounded at 131072 each. `NotEnoughItem`, `OutOfReach` and `Collide` are first-condition observations, not a complete list of problems or authorization to bypass them. `machineInventory` separately counts the **whole plan's** package requirement, so one building item cannot fund several independently clear previews. `site.status=machine_previews_clear` requires every native check to pass, no occupied object or planned overlap, and the aggregate package budget to suffice; otherwise it is `blocked`.

`site.assessmentHash` binds the material calculation, session/revision/player action state, snapped poses, collider bounds, native conditions and inventory budget; capture tick alone is excluded. It is an informational assessment fingerprint, **not** a complete world state hash, durable construction graph or commit token. Manual intervention, restart or changed conditions require a fresh read and a new assessment/prepare; neither hash allows old tokens to be reused. A clear machine grid does not prove belt/sorter endpoints, logistics/power cost or connectivity, sufficient ongoing supply, output throughput, or resumable construction. Those remain explicit unfinished Foundry gates.

`spherewright_prepare_save_import` is the only entry point for a player-loaded unowned world. It accepts the restricted session ID and exact revision, creates only an in-memory short-lived plan bound to that process/session/revision/`GameData`, and returns a `confirmationPrompt`; it does not save, adopt, enumerate, actively load, or return the original save identity, planet, or game tick. After prepare succeeds, the Agent must ask that question and wait for a subsequent explicit user reply. `spherewright_commit_save_import` may set `userConfirmedInConversation` and the two boundary acknowledgements only after that later reply; missing declarations return `USER_CONFIRMATION_REQUIRED` without consuming the plan. The Plugin treats these fields as the MCP caller's declaration about conversation evidence—it does not claim access to chat history. Commit then revalidates the same object plus peaceful-mode/ready-local-factory state, records the actual sandbox/resource settings without using them as gates, generates an internal high-entropy name, calls DSP's normal save API, rereads the exact header tick, and adopts only on proof. The original remains unchanged, and the new per-save journal uses `attached_existing_save` with incomplete historical coverage. If the normal save reports success but header proof fails, that unowned session enters write quarantine: same-key result lookup remains available, but no new import plan/commit is accepted until the player deliberately reloads the intended original world into a new session.

`spherewright_prepare_dismantle` and `spherewright_commit_dismantle` currently expose only the completed solid-vein/oil-miner subset of DSP's normal dismantle path. Prepare binds the exact endpoint identity, complete player action state, ordinary build range and conservative package capacity without changing the world. Commit calls `PlayerAction_Build.DoDismantleObject` once, then requires the exact entity to be gone and every player-inventory delta to equal the returned building item plus the target's live buffers. Any unproved disappearance or recovery delta becomes an outcome-unknown write-health failure; the pair is not a general arbitrary-entity delete API.

`spherewright_prepare_configure_building` accepts `production`, `research`, `sorter-filter`, `logistics-station-storage`, `logistics-station-belt`, and `logistics-station-charge` modes. Sorter-filter mode additionally carries `filterItemId` and binds root `configurationStateHash`: identity, topology, current filter and held cargo are included, while ordinary progress/stage are excluded. Cargo-free sorters remain supported in empty `Returning`; ordinary2011/2012 unidirectional sorters may also retain valid cargo in native `Inserting`. Prepare and commit recheck this bounded window. Commit copies the complete native inserter/sign structs, permits only filter/sign changes, and immediately proves cargo/count/inc/stack, timing, stage, targets, offsets, energy, topology and player inventory unchanged. The endpoint comparison permits the declared filter change because blueprint settings are part of that hash. Existing cargo still goes to the same destination; changing a filter only governs future pickups and is not jam clearance or take-back. Logistics-station-storage mode separately binds the nested station `configurationStateHash`, one slot index, one unlocked item, a 100-item-step maximum, and local/remote `none|supply|demand` logic. It accepts only an empty slot or the same existing item with no outstanding orders, forbids clear/replace, and commits once through `PlanetTransport.SetStationStorage`; immediate readback must prove item/limit/logic while slot count/inc and every package/in-hand item/count/inc tuple remain unchanged. Logistics-station-belt mode binds one connected, cargo-free output port plus one configured zero-based public storage-slot index. It accepts only an unselected raw port (`storageIdx=0`), maps the requested slot to the native one-based selector, and never clears or retargets an existing nonzero selector. Commit uses the exact current-version UI field path and immediately proves the selected item, belt/component/entity identity, port direction/counter, every unrelated selector, station storage/orders/energy/fleet, and player inventory are unchanged except for the target selector. Logistics-station-charge mode binds the same station configuration hash plus `stationMaximumChargePowerWatts`; it accepts only 3 MW UI steps inside the exact prefab's integer-slider range of one-half through five times default work energy. Commit writes only the same `PowerConsumerComponent.workEnergyPerTick` field as `UIStationWindow`, then proves maximum-power readback while requested power, consumer identity, station inventory and player inventory remain unchanged. The refuel pair binds an exact current player/fuel-chamber snapshot and commits through `Mecha.AutoReplenishFuel`; readback must prove equal-and-opposite count changes and conserved proliferator points. The normal save pair binds the current owned-session revision and commits only through `GameSave.SaveCurrentGame` with the exact internally retained primary save name. An interplanetary-flight commit first saves a separately named internal checkpoint, verifies its header tick, and persists its reusable provenance ticket before native flight begins. None of these operations can address an external save or inject an item/energy value.

`spherewright_get_gameplay_journal` is a read-only, owned-session-gated view of a current-user-protected file keyed by a one-way hash of the internal owned-save identity. It records first manual and first production-line output independently per item, plus first technology and upgrade selections, with ISO-8601 wall-clock time, raw game tick, and formatted in-save time. `durableThroughSequence` identifies the highest sequence confirmed on disk; `persistencePending` and `persistenceError` distinguish newer memory-only entries from durable evidence. It never returns the raw save identity or an absolute journal path. A journal created for an already progressed save explicitly reports incomplete historical coverage instead of inventing earlier timestamps.

`spherewright_get_progression_state` returns both a complete live `stateHash` and a dedicated `selectionStateHash`. The complete hash includes each technology's naturally increasing uploaded research hash and remains suitable for exact observation comparisons. Research prepare/commit instead require `expectedSelectionStateHash`: it binds the current technology, ordered queue, unlock and level state, required hashes, lab/queue classification, and prerequisite IDs, but excludes the tick-live uploaded amount. Natural lab progress therefore cannot make a queue-only decision stale between inspect and prepare, while a completed prerequisite, queue edit, current-tech transition, or unlock still invalidates the plan. Default enqueue still checks `CanEnqueueTech` at prepare and commit.

Optional `prioritizeQueued=true` is a narrower native reorder, not enqueue or cancellation. It accepts only one selected queued non-head technology whose explicit/implicit native prerequisites (including maximum-level requirements) are already unlocked. The queue must be active, contiguous, duplicate-free and at most64 native slots. Core produces a complete index permutation preserving all other entries and empty slots; Plugin revalidates it and the selection hash before calling `GameHistoryData.SortTechQueue`, the native research-queue drag path. No queue array, research progress, level, unlock or inventory is directly written. Immediate `researchQueueReadback` proves exact before/after order, current technology, research progress and inventory preservation; any unproved result uses ordinary outcome-unknown quarantine. The action is terminal when reordered, **not** when research completes; actual materials and research time are still required. It creates no new MCP method and defaults to the original enqueue behavior.

`spherewright_get_player_state` separately exposes DSP's hidden mecha-research reservation as `autoManageResearchItems`, `mechaResearchPower`, and `mechaResearchItemBuffer`. Each buffered item reports exact internal point count plus whole-item and remainder views at the native 3600 points per item. When automatic management is enabled, ordinary game ticks may move currently required matrices from the package into this buffer before any hash is produced; package-only deltas must therefore not be classified as loss. These fields are observational and are not a matrix-injection or research-completion writer.

`spherewright_get_overseer_production` is the production-rate and bounded root-cause diagnostic v0.4 read-only slice. A request carries one to 64 unique current runtime item IDs and a planet page limit from 1 to 16. The first page deep-copies every already-created factory in the exact active owned `GameData`; later pages require the same session, item filter and page size plus the opaque `nextCursor`. It never calls `GetOrCreateFactory`, loads a remote display, scans save files or observes an unowned world. Each production row uses DSP's save-persisted level-0 `ProductStat.total[0]`/`total[7]` window: exactly the most recent 600 game ticks, or 10 game seconds after warm-up. Player/mecha production is recorded by DSP only in separate lifetime totals and therefore does not contaminate these automatic-line rates. The response declares `rateSource=native_factory_statistics_level_0`. It independently scans the current identity-bound assembler, matrix-lab, miner, fractionator, gamma-receiver and orbital-collector components to reproduce all current-version theoretical product-rate branches, returning `theoreticalRateSource=current_runtime_component_formula_v1` and `theoreticalCoverage=complete` only when the entire bounded scan succeeds. `utilization` is actual production divided by positive theoretical capacity after the native window is ready; it remains null during warm-up or when no connected capacity exists, and a short discrete window may temporarily exceed `1`. The implementation does not read or mutate DSP's unversioned `refProductSpeed` UI cache.

Each row additionally reports `directProducerCount`, `directDiagnosedProducerCount`, `directDiagnosticCoverage`, the uncapped `findingCount`, `findingsTruncated`, and at most 16 `findings`. The `direct*` fields still describe only the requested item's immediate producers: configured assemblers, matrix labs, and vein/oil/water extractors. Diagnosis uses the same captured tick, requires a ready native window covering at least one full device cycle, reproduces the current-version native output-buffer gates, and validates device/entity/consumer/network/recipe/source identities before classifying. A positive aggregate actual rate suppresses per-device blame for the requested item; a running native cycle is not called starved merely because it already consumed its next batch.

When the immediate result is `material_shortage`, each missing item is traced independently from the consumer's exact input sorter. Reverse traversal follows only directed belt/splitter/piler/spraycoater/sorter/storage/tank edges. Every sorter must be unfiltered or filtered to that item. A splitter is entered only through one exact identity-checked output belt: with no filter either output class admits the item; with a filter its priority output admits only the filtered item and every non-priority output admits only other items. A disallowed branch is not traversed, while inconsistent connection-slot/component/belt identity fails the whole read closed. Physically relevant station evidence is attached only when this traversal reaches an exact configured station output belt. For the selected demand/supply route, the same item-specific traversal starts at the exact supply station input belts; only a supported producer physically reachable behind that supply endpoint can extend the graph onto its owned planet. Station input `storageIdx` is not treated as a fixed selector: current DSP chooses from the station-wide `needs[]` and writes that field only after a successful pick. A reached producer is bound by exact planet, object and output-item identity, then classified from the same captured current buffers, power, source and working state. Its per-device historical rate is deliberately marked unknown so an aggregate rate for a different requested item cannot suppress the current-state finding. Traversal is limited to eight producer levels and 64 visited producer identities with a cycle guard. If a graph bound or inconsistent producer reference stops a still-expandable path, evidence includes `upstream_trace_stop_reason`; callers must not treat that point as a proven complete root. A missing consumer input on an exact demand route with a positive demand-side reservation, source inventory, and a nonzero fleet enters a protected per-save temporal window instead of being mislabeled as an immediate shortage. A sufficient consumer buffer resets that route's stagnant baseline. Delivery inventory, demand-order reduction, route-carrier dispatch/return, drone `t`, and vessel stage/universal-position motion count as progress and restart the baseline. Only 600 continuous game ticks without those changes produce suspected `logistics_blocked`; the finding includes window ticks, active route-carrier count, and whether it crossed a process session. Save change, tick rollback, topology change, same-tick mutation, or an adjacent gap over 3600 ticks resets or invalidates the proof. All route observations from one read are committed by one atomic file replacement before their analyses enter public DTOs; a persistence failure leaves temporal state unknown. The current-user ACL-protected file is bounded to 4096 hashed route identities and never exposes its save-derived key or path. Unsupported immediate fractionator, gamma-receiver, or orbital-collector producers make direct coverage `partial` and remain non-recursive. Multi-hop station relays are not inferred without another physical source-side path. Each planet separately returns at most 16 `infrastructureFindings` for depleted extractors whose item identity has already been cleared, with total/truncation metadata. See [game-api-overseer.md](./research/game-api-overseer.md).

`spherewright_get_overseer_summary` is the second v0.4 read-only slice. Its first page deep-copies every already-created owned factory's power-network and logistics-station aggregates plus one global current-research summary; later pages require the same session and page size through an opaque 60-second cursor. Power totals preserve DSP's native per-tick energy units: actual generation is the checked sum of each identity-bound generator component's `generateCurrentTick`, while planetary defence-field `energyExport` is exposed separately as `energyExported`; the existing local `spherewright_get_power_summary` uses the same corrected mapping. Logistics totals classify planetary stations, interstellar stations, orbital collectors and vein collectors as mutually exclusive, aggregate configured storage modes, inventory, order magnitudes, fleets and energy, and derive non-orbital station power from the identity-bound consumer network. Research reports the current technology, hash progress, ordered queue, runtime tech-state/unlocked counts and current item requirements using DSP's `hash × pointsPerHash / 3600` integer formula; every nonzero queued technology must still exist in both the runtime catalog and tech-state map. The capture is bounded to 512 factories, 4096 power-network slots, 65536 generator references, 4096 station slots, 64 storage slots per station and 12000 technology records; aggregate totals are never silently truncated. Like the production slice, it does not create/load a remote factory or expose a save identity, token or path. See [game-api-overseer.md](./research/game-api-overseer.md).

`spherewright_get_overseer_diagnostic_bundle` is the public same-tick diagnostic package. It takes the same bounded item set and planet-page limit as the production slice, captures the complete production/root-cause domain and the power/logistics/research domain synchronously in one Unity-main-thread work item, then joins planets only when factory index, planet identity/name, local/display flags and `capturedAtGameTick` all match exactly. A mismatch fails the whole request instead of combining adjacent observations. The result uses schema version `1` and `privacyProfile=public_allowlist_v1`; its DTO is an explicit allowlist of the already-public production, finding, power, logistics and research fields and has no member for an auth credential, action plan credential, save identity or filesystem path. Pages are immutable for 60 seconds and bind the session, sorted item filter and page size through a dedicated cursor store; complete one-page results do not consume retained capacity. This tool is an aggregation read, not a new game-data source or write path, and it still inherits every coverage limit stated for the two underlying slices.

`spherewright_list_factory_entities` with `componentKind=station`, and `spherewright_inspect_factory_entity` for the resulting positive entity ID, include a deep-copied `logisticsStation` object for a completed station. The object cross-binds entity, local station, galactic station and planet identities; reports stored energy, current requested charging energy/power, configured maximum charging energy/power, fleet counts and prefab capacities, raw route settings, every configured or empty storage slot, and every station belt port; and includes separate `stateHash`, `configurationStateHash`, and `fleetStateHash` values. The full hash changes with live requested charge, energy, inventory, orders and fleet activity. The configuration hash excludes those volatile values but binds the consumer's configured maximum charge, identity, station type, slot item/limit/logic, route settings and belt topology. The fleet hash binds station identity/type/position, both fleet capacities, idle/working counts, and auto-replenish settings while excluding unrelated live energy, orders, and cargo. Trip/delivery/warp values remain explicitly named `*Raw` or `*Setting`; their UI transforms are documented locally, but no route-setting writer is adopted yet.

The station-fleet transfer pair accepts only item `5001` (logistics drone) or `5002` (logistics vessel), and vessels require an interstellar station. It binds the complete player state plus the dedicated fleet hash, requires normal interaction range and an empty player hand, counts working craft against the prefab capacity, and permits withdrawal only from idle craft. Player-to-station rejects any nonzero proliferator-point total because the native station UI discards those points. Commit uses the same `StorageComponent.TakeItem`/`AddItemStacked` package operations and idle-counter mutation as the current station UI, then proves equal-and-opposite player/idle deltas, total conservation, unchanged working/opposite fleet counts, unchanged proliferator points, and preservation of station storage, energy, warpers, configuration, player hand, and unrelated package items. It never fills a station cargo slot or creates a craft.

Quarantine reconciliation is not a force-unlock: it is available only for the exact retained outcome-unknown build action and clears quarantine only after the item decrement, all new entity/component identities, and directed topology form one unchanged proof. It never repeats the build. Owned-game resume is likewise not a save picker: it accepts only a protected one-time token. A healthy planned restart loads only the exact primary owned save already sealed in the ticket; quarantine recovery alone may load DSP's fresh fixed `LastExit` to preserve unsaved progress. Before commit, the selected file header must prove the ticket's minimum game tick. Every newly issued ticket also carries a protected gameplay-journal checkpoint: one-way journal identity, tracking mode/boundary, historical-coverage flag, and minimum durable sequence. Prepare and commit reject a missing, recreated, truncated, discontinuous, or mismatched journal before starting DSP's loader; post-load attachment repeats the same check before ticket consumption, automatic save, or gameplay writes, closing the file-change race. Failure leaves the loaded world restricted and keeps the ticket available for an exact journal repair and retry. Version-1 tickets created before this optional extension remain compatible, then the next healthy normal save issues a checkpoint-bearing replacement. Adoption still requires high-entropy owned identity, minimum tick, planet, peaceful settings, and source-process shutdown proof. Sandbox state and resource multiplier are preserved and reported but do not gate adoption. Consumption first persists a token-hash tombstone, so a failed best-effort file deletion cannot resurrect the old token.

Flight-checkpoint reload is a narrower repeatable recovery path. It accepts only the protected token created immediately before that flight, never accepts or enumerates save names, revalidates the internally generated name and exact saved tick, and loads through `DSPGame.StartGame`. Adoption additionally requires the embedded primary owned-save identity, origin planet, peaceful settings, and exact checkpoint ID; sandbox state and resource multiplier are preserved and reported but do not gate adoption. Reload is allowed only for a persisted `recovery_required` flight, or for an interrupted in-flight ticket whose source DSP process terminated. A failed retry returns to the same checkpoint. Stable flight success immediately removes the reload capability; a primary save covering the success tick retires the ticket, and a newer exact primary header supersedes legacy tickets at startup. Tickets expire after 24 hours.

The public surface contains no composite red-matrix or legacy sandbox operation. Missing future methods return no simulated data and must not be substituted with historical sandbox code.

### New belt centre-occupancy containment (0.4 development slice)

Ordinary `plannedPath` and `itemBudget` describe only NEW objects. Every NEW node, including both ends, must be more than0.25m from every current built/prebuild belt and every other new node. An explicitly supported existing source is separately retained as a native non-removing cover, not clipped from native validation or charged again. The Plugin scans the complete local owned factory (at most65536 pool slots including holes), not just a nearby page; the pure guard bounds path size to256 and exact-distance comparisons to65536. A saturated256-point native path result is rejected as potentially truncated. Malformed/missing evidence, unclassified prototypes and excessive bounds fail closed. The guard runs before/after native pose adjustment, again at commit revalidation, and immediately before construction. Native checks, cost, drones, hashes, exact outcomes and single-flight remain required.

Full path validation uses a tool-owned disabled/inactive stage1 command container: native stage0 is an anchor-only UI preview and omits later bend/slope/junction checks. Only the private unregistered build tool is rebound; the real player controller/command is never replaced or temporarily edited. Source and destination object IDs are both supplied to native validation. Preview UI fields, inventory snapshot and temporary host are cleaned up in the same synchronous frame, including rejection/exception paths. Commit uses the same full check before normal creation. This stage integration has1258-cohort local prepare-only positive/negative evidence; source-cover construction remains a separate live gate.

Rejections retain the existing error DTO and report a stable occupancy reason, planned index and signed object ID (negative for prebuild;0 for self-overlap). Recovery forbids unchanged-site/endpoint-omission retries; only explicit OutOfReach calls for moving closer. Free new paths and proven non-belt native ports retain their normal rules. Nothing repairs/dismantles old coincident entities or treats observed cargo as proof of native placement legitimacy.

The1309 source-only reuse subset accepts a built same-grade2001/2002/2003 source with zero tilt, free output slot0, at most one incoming belt, an open bounded cargo path and a free destination. The source must be the unique belt at its centre; legacy coincident built/prebuild belts reject. At least two flat NEW points follow the cover. Destination cover/merging, tier replacement, tilted/raised or closed paths are not supported. Private static binding covers the whole source path (8192 cells/512 belts maximum), member identities/geometry, source's original16 slots and reciprocal neighbours; natural cargo motion alone does not stale a plan. Immediately around native prebuild creation, the complete old path/cargo must match in the same frame, while only source slot0 changes to the exact new prebuild with a reciprocal edge. Drone completion rechecks retained source/neighbours and the corresponding new entity edge, without assuming cargo stays frozen across ticks. Unknown evidence/outcome freezes writes and never triggers replay.

Every belt prepare returns optional-for-legacy-DTO `plannedBeltPath`: `nativeValidationMode=full_path_stage1`, `sourceBindingMode=none|native_device_port|non_removing_belt_cover`, nullable `reusedSourceObjectId`, and `newObjectCount`. The current MCP withholds the entire prepared result/token if the echo, source/destination, NEW path count or construction-consumption budget is missing/inconsistent. Direct Bridge clients must enforce the same compatibility boundary. This adds no MCP method or general pathfinding/cover-removal permission;1309 offline tests do not establish source reuse in game.

### Initial sorter filter during normal construction (0.4 development slice)

`spherewright_prepare_build` accepts optional `initialSorterFilterItemId` (default0). A nonzero value is supported only for native ordinary2011/2012 sorters and must be an existing unlocked bounded item ID. Non-sorters, unsupported grades, negative/oversized/unknown IDs and locked filters reject before a plan; zero retains unfiltered legacy construction. The prepared DTO returns `plannedSorterFilterItemId` (null for other build kinds,0 for explicitly unfiltered).

MCP must receive that exact nonzero filter echo before exposing a construction token. A missing or different echo (for example a new MCP with an old Plugin) returns `BRIDGE_NOT_READY` without the unconfirmed plan/capability. Direct Bridge clients must enforce the same echo check. Default0 calls retain legacy response compatibility; this guard never commits or retries a build.

The field participates in the exact build-step fingerprint and equivalence checks. Both preparation and commit recheck eligibility, native conditions and endpoints. `BuildPreview.filterId` flows through the normal `BuildTool_Inserter.CreatePrebuilds` path to the drone-built entity, not a later live-component assignment. The prebuild filter is reread immediately after native creation; completion checks the exact native filter/sign and original reciprocal endpoint proof. No new tool, stock mutation, automatic configuration action, token recovery or idempotency exception is introduced. Inspect the resulting entity after terminal; a filtered input does not clean existing mixed stock or prove sustained flow. Code/offline status and matching-build live evidence are tracked separately in IFX-040 and the save diary.

### Native single-belt inserter attachment (0.4 development slice)

Rejected searches preserve the existing error DTO/code and add bounded preparation-stage evidence to the message: capture reason or seed/projection/facing/tilt/candidate-validation stage, counts and the best measured deviation (unknown when unmeasured). Candidate validation count is not a claim that DSP's placement check ran. The last exact-slot rejection is explicitly labeled rather than presented as the cause of all fallback failures. Recovery prohibits blindly retrying the unchanged pair; only an explicit OutOfReach reason supports a range correction. A transient geometry_buffer_busy permits one bounded fresh prepare after waiting, never an old token. This diagnostic changes no accepted plan, hash, angle threshold, material cost or write semantics.

For ordinary2011/2012 builds, optional `plannedInserterAttachment` identifies `exact_slots` or `native_single_belt_segment` with both slots, endpoint positions and signed input/output offsets. The latter is a bounded native geometry choice on one caller-selected belt and one2101 storage/assembler/lab, not arbitrary offset input, nearby retargeting or automatic routing. At most64 segment intervals are considered; closed/oversized/malformed paths, clamped pivots and two-belt offset searches are unsupported. Private static geometry/offset binding is rechecked at commit and immediately before native construction; changed geometry requires a new prepare. Native angle/tilt, reach, collision, costs, drones and final reciprocal/offset/pose proof remain required. The DTO is a plan, not delivery or completion evidence; older servers may omit it. See the API research and per-save diary for separate offline/deployed/live status.

### Warehouse capacity and reservation configuration (0.4 development slice)

The existing configure-building pair additionally accepts `mode=storage-capacity` for one unstacked ordinary2101 warehouse. It uses fresh full `expectedFactoryStateHash` and one explicit `storageOperation`: `set-bans` with `storageBannedGridCount=0..gridCount` (DISABLED final automation grids;0 restores all), `lock-occupied`, `filter-empty-or-matching` with an unlocked `filterItemId`, or `clear-filters`. Non-ban operations require the default banned count−1; non-filter operations require filter0, and recipeId/techId are0. Unsupported modes/items/operations, oversized/corrupt grids, out-of-range or irrelevant arguments and no-op changes are rejected, not clamped or silently applied.

Prepare privately deep-copies at most100 native grids including empty cells and binds their ordered item/count/inc/filter/stack sizes, storage component/link identity, entity pose/topology, session and operation in the ordinary short-lived plan. Commit repeats the full projection and unlock check before using the same `SetBans`, `SetFilter` and type assignments as the storage UI. It then proves the exact expected ordered grids, unchanged item counts/inc, entity/link identity, topology and player package/in-hand state. Clear-filter may clear empty metadata only; no operation moves/deletes stock or makes a new item. The existing public `buffers` omits empty cells and is NOT an editable native grid-index list. Existing general action hashes are unchanged; the additional exact ordered fingerprint is private to this new plan.

The prepared response echoes `plannedStorageOperation` and `plannedStorageConfiguration` (mode/bans/filter array, not contents); a new MCP suppresses unconfirmed plans from an older/mismatched Plugin. This remains one normal action with existing single-flight/idempotency/terminal/unknown quarantine. A ban restricts native sorter insertion, not all player interactions, and the current storage-to-machine take path can still consume inventory. Filtering or a free grid is not throughput proof: account for already-held cargo, all admitting upstream items and real sinks, and measure subsequent production separately. Local set-bans/lock/reservation evidence is recorded in the diary;761 settings additionally survived normal save25092892 and1144 cold-deployment protected resume. The new instant DTO has a775 set-bans positive, while other operation-specific DTO checks and sustained supply are not implied.

Successful warehouse configuration additionally returns optional `storageConfigurationReadback` on the action result: exact operation/entity, `capturedAtGameTick`, `evidenceScope=synchronous_native_configuration_boundary`, before/after settings and nonempty grid buffers, and verified connection count. It is built only after the same native identity/ordered-grid/topology/player checks pass; Core rechecks the exact projection and creates independent DTO lists. It does not add a gameplay call or alter hashes, idempotency or unknown-outcome handling. Subsequent ordinary delivery is deliberately outside that captured instant. Never assert that a later live warehouse count must equal the instant count: unbanning can immediately deliver previously held cargo. Reconcile flow separately and never replay an already-successful configuration. Empty reserved grids are absent from buffer lists, not new items. Old Plugin responses may omit the field, which means unavailable evidence rather than zero stock. The new evidence field's offline and installed/live status must be reported separately.

## Request rules

Configuration hash domains are mode-specific: `sorter-filter` requires root `configurationStateHash` in `expectedFactoryStateHash`, whereas `storage-capacity` requires full `stateHash`. A sorter request that can be positively identified as the current full hash returns non-retryable `INVALID_REQUEST`; an unmatched older/wrong-domain value remains `STALE_STATE` because its origin cannot be proved. Missing native configuration evidence fails closed. Inspect the actual request field before retrying; no canonical hash, prepare/commit binding or native mutation semantics are relaxed by this diagnosis.

`inspect_factory_entity` requires `objectId`, positive for a built entity and negative for a prebuild. Zero/missing and `Int32.MinValue` return `INVALID_REQUEST` after the owned-world guard; this is not evidence of a missing building. Do not reuse the `entityId` key from configure/transfer request schemas. Direct Bridge deserialization can otherwise default an unrecognized/missing ID to zero; older Plugins reported that case as `INVALID_ENTITY`. A nonzero representable ID still passes through the unchanged native existence check. `get_player_state` uses `planetId` from the fresh session's `localPlanetId`. An unfinished paginated list cannot establish that an object disappeared.

`inspect_factory_entity` additionally returns optional detail-only `sorterEndpoints`. An `observed/native_slots` result contains at most16 world-space positions and outward unit directions, slot index and current occupancy/neighbor; an `observed/belt_virtual` result contains four orientations with slot=-1 and null occupancy/neighbor. The latter does not identify a free physical slot. Null/unavailable means unknown, not no endpoints; malformed or over-limit input yields no partial poses. Lists and prebuild details omit this extension. Existing action/configuration/endpoint hashes are unchanged, and the read never creates a plan: use fresh prepare/commit and final reciprocal readback, and verify power/delivery separately.

Every save/player/factory request carries the active `sessionId`; planet-bound requests also carry `planetId`. Save import is the narrow exception that accepts only an unowned session's restricted ID/revision and exposes no world content before confirmation. A mutable prepare binds the current complete target/resource state and returns an opaque short-lived plan. Commit carries the plan token and a UUID idempotency key. Factory observations expose both `stateHash` for complete mutable device state and `endpointStateHash` for build connections. Build source/destination requests use `endpointStateHash`, which binds identity, pose, and existing connections without becoming stale solely because a miner, belt, or assembler advances normal production.

Ordinary sorter construction records any pre-existing same-item entities at the prepared source pose before calling DSP's native prebuild path. Completion excludes those IDs before topology verification, so two legal sorters that share the same building-side position cannot cause the newer action to be attributed to the older sorter.

New-game creation and exact owned-game resume are the ordinary main-menu mutations and have no active session/planet envelope yet. Flight-checkpoint reload may replace only the exact matching active owned game or run from an idle main menu; it can interrupt no active normal action except its own bound flight. New-game creation generates the save name and fixes peaceful mode, sandbox disabled, and resource multiplier 1x. Neither recovery path can enumerate or accept a save name. The primary-save resume fallback is internal-only, requires that exact protected ticket identity and a fresh file timestamp, and intentionally restores no post-ticket unsaved action.

A stale session returns `STALE_SESSION`, an unowned session returns `SESSION_NOT_OWNED`, and a stale target/resource snapshot returns `STALE_STATE`. Missing descriptor or confirmed combat mode blocks gameplay commits; sandbox state and resource multiplier remain visible evidence but do not block them.

Responses never contain the Pipe name, bridge authentication token, descriptor path, stack trace, absolute game path, raw save identity/content, or raw game component. Opaque plan tokens plus one-time resume/reusable checkpoint tokens appear only in their intended authenticated structured fields and must not be copied into logs or evidence documents. List methods are bounded and use session/planet/snapshot/filter-bound cursors.

## Action semantics

All normal-game mutations use separate `prepare_*` and `commit_*` methods. Prepare is read-only even when writes are disabled. Commit is single-flight by `(sessionId, idempotencyKey)`, rereads all preconditions on the Unity main thread, and records a queryable action state.

If a commit may have been accepted but its result is unavailable, the MCP mapping returns `ACTION_OUTCOME_UNKNOWN`; clients must retry with the same request/key or query `get_action_result`. They must never use a new key to guess-replay the action.

`prepare_move` validates the current owned session, planet, fresh player hash, finite surface target and arrival tolerance. It deliberately does not predict the collision-free path. A terminal movement stall now returns `failureKind` (`position_stalled` after the 180-tick displacement window or `route_stalled` after the 600-tick target-progress window), `stalledGameTicks`, `remainingDistance`, `doNotRetrySameTarget`, `recommendedRecovery`, `recommendedShortMoveDistanceMeters`, `orthogonalProbeDistanceMeters`, and `maximumOrthogonalProbeAttempts`. The coordinator still aborts only the exact `OrderNode` reference that it owns. It does not teleport, write position, or run an internal pathfinder.
