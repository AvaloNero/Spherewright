# Environment research

Audit date: 2026-09-08 (Asia/Singapore)

Latest installed development cohort: **25c42cf / 1508 tests** (45 Contracts/1376 Core/87 MCP), locked restore, complete current-DSP Release zero warnings/errors and all Release tests pass; source CI34149716402 success. Save29002293 and the persisted nine-action audit preceded normal shutdown. Raw-34c6a004 binds the same source/version to each corresponding build flavor; raw-67dfeae7 verifies all4 Plugin and224 self-contained win-x64 MCP installed files, retains recoverable old-install backups and dispatches Steam exactly once. Non-RID Plugin and RID MCP common assemblies are compared to their own build outputs, not incorrectly required to be byte-identical across RID builds. Installed Plugin SHA-256 `1AA4E880467AB4B7533BE099E332FEA7463FC38598C2674906FFE5538CE1ADEF`; MCP DLL `D13FDDAD0B02E0591EC16CE2486DFE469A8B4B2161033D223296A244E9C5A93B`. Actual installed MCP raw-def44beb starts with64 tools/1 resource and the exact50349-character playbook, exits0 without extra stdout. Resume2eebbe2e uses only the protected current primary and resaves29002324. Full2435-entity/396-nonbelt restoration and ten-action audit raw-3970fea9/6f8fe357 preserve configuration, inventory, Journal55/55 and three served networks, prebuild0/healthy. The explicitly recorded13.93-micrometre player-position offset is independently stable, not reported as bit-identical. Magnetic delivery continues in four independent600-tick windows; the new shortage error is live-verified, with stocked full revalidation still pending. This is a development cold deployment, not a final ZIP, clean-machine or cross-computer acceptance (EXP-249/250).

Previous installed development cohort: **fa29180 / 1488 tests** (45 Contracts/1359 Core/84 MCP), Debug and Release pass, complete current-DSP Release zero warnings/errors, Windows CI34117645812 success. Save27804949 and the persisted six-action audit preceded normal11764 exit; four same-batch DLLs were installed and Steam launched31168 once. Source/installed hashes: Plugin `EF8F6F66FB22BD9DBED44506C98E685434A69E62203A1933BBC4381FD0FF51C2`, Contracts `001BA3E5FC6672609339259D56B7BDA39C715FB38FCE783FD552D1EADE08B562`, Core `8DE7FE0FF70F734D2FA8879A797A276A0B3758D71B9DDB5DC580C5E5F2234360`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Protected resume7/a8e101c8 resaved27804980, new-session revision1/healthy. Root raw-a7bf4900 compares all2310 structures/387 nonbelt configurations and unchanged player inventory/fuel, Journal55/55, three served networks and prebuild0 against the saved snapshot; accepted7 stayed7. Position deltas are explicitly retained in the diary, not described as bit-identical.

Subsequent same-cohort live milestone: iron branch39 belts/two filtered sorters delivered to723 in four independent600-tick windows with positive planet iron production. Normal save28056055/revision12 and raw-e61b034c full2351-entity/389-nonbelt three-action audit pass; accepted3 retained, J55/55/healthy/Walk0/400MJ/36graphite. This new branch has not yet crossed a restart and motors remain coil-starved. Portable SDK Release `--no-build` regression rerun passed all1488 tests; system PATH's runtime-only dotnet did not start tests, consistent with EXP-030. No new final release package or cross-computer validation.

Actual source MCP against that installed development Plugin passed64 tools/1 resource/exact48248-character guide, no extra stdout/exit0 and unchanged player hash/revision. Raw-4ffacceb reads wind1 and thermal183 as joules_per_tick/unitsPerItem0 while warehouse28 remains items/1. This is a local live unit correction, not final ZIP, cross-computer validation or sustained fuel production. The following cohort paragraphs are historical snapshots and do not override this current installation.

Current installed development cohort: a157a0b,1472 Debug/Release tests (45 Contracts/1344 Core/83 MCP), locked restore, complete current-DSP Release zero warnings/errors, source MCP64 tools/1 resource/46813-character guide with clean stdout, and Windows CI34112598180 success. After normal save27578976 and its audit, DSP43176 exited normally, four same-batch DLLs were installed and Steam launched11764 once. Source/installed SHA-256 pairs match: Plugin `91B3B23E76FBAF3CC881AC89CE66DFEAADF71BDEA9C5525F4FF8A777154C4AE8`; Contracts `CADDDE2892C50F8E5E6696DF7D2A902F00E36857ADCF8CFF23F5D898916DEE21`; Core `E2127F5E8C5EE287E88AF3335837291C1ED2E8D89732D7732D1BBFCFF47957A2`; Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. The subsequent guide-only changes passed locked restore, zero-warning/error Core builds and all1472 Debug/Release tests again; real source MCP still has64 tools/1 resource, now47750 matching characters, exit0 and no extra stdout. This guide refresh is not a new Plugin installation.

Startup retired failed checkpoint27507953 because the exact primary was newer. The earlier plan incorrectly considered only the save callback; it was withdrawn without editing tickets or replaying a commit. Current-primary resume381fc98d resaved27579007, preserving the full179-object source factory and materials. A new checkpoint27593800 supported flight532d165b, completed27599682 with600 stable grounded ticks. Full ten-action audit then verified mother2310 structures,387 complete nonbelt details (only37 nonbelt configurations had earlier comparison coverage), no prebuilds, Walk0/400MJ/52 graphite and Journal55/55. After that persisted reset, new-window Save1/312d2127 saved27644409; independent raw-1a811574 proves checkpoint27593800 durable retired at27644409 and protected primary resume available. Accepted count remains1. There is no retained continuous trace reproducing the transient-shore defect branch, so this is a normal-flight local positive, not that targeted live test, a new ZIP or cross-computer validation. Subsequent guide-only source changes do not hot-replace the installed Plugin. Older paragraphs below retain historical boundaries only.

Latest source MCP guidance-only slice:1457 Debug/Release tests (45 Contracts/1330 Core/82 MCP), locked restore, complete current-DSP Release zero warnings/errors, actual source MCP64 tools/1 resource/exact46326-character playbook with clean stdout pass. It distinguishes native non-idle drone counts from proven build completion, permits bounded read-only readiness checks and forbids replaying a completed route. Plugin control/hash semantics are unchanged; runtime remains the1455 development installation below, without hot replacement or newZIP. On that runtime, independent102 silicon inlet construction (four belts plus179 filtered sorter) passed per-object/material/full179-object audit;21 prospective separated600-tick windows showed titanium refill despite full silicon. Normal source save27507910 was independently verified. This is not a continuous Governor run, final package or cross-computer validation.

Current installed cohort is now1455 (source08557ac; CI34102424940 success). Save27241962/full8-action audit preceded normal21992 exit and one Steam launch; new43176 is responsive, BepInEx error/fatal/exception scan0. Source/installed SHA-256 matched: Plugin `7F6FC6EE9E1CE48DF45DB60366D8BEB045C318A32DB2B6E630C038B7CE13B3AF`; Contracts `A8BEF0566F78CEBABA7CEFBF61AD4BE329AC4797B210CA49F43B8E1B55B8C278`; Core `8F0E348934F6C1C987EDBA0D0DDD15E9D228BA6E1BB6EC50407FAD901DD38D1A`; Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. A startup-only read timeout preceded a successful menu read and did not consume a ticket. Resume8cdaf6df completed with null action ticks and resaved27241993; full175 structures/28 configurations/inventory/Journal55 persisted, y=-1floatULP explicitly recorded, healthy/Walk0/400MJ/rev1. Actual MCP rearPickup positive/non-tail negative reads passed with no player/revision change; source MCP is not an installed finalZIP. Accepted9 retained. Older source-only/1420 paragraphs below preserve earlier boundaries, not the current deployment.

Latest source-only slice:1455 tests (45 Contracts/1330 Core/80 MCP), additive detail-only rearPickup observation. Locked restore, Debug/Release tests, complete current-DSP Release zero warnings/errors and real source MCP64 tools/1 resource/exact45564-character playbook/clean stdout pass. Not installed yet; runtime remains1420. Normal102 save27241962 and all8 accepted actions (6 successful Moves,1 exact position-stall,1 save) were independently audited at27244818/27244887:175 unchanged structures/28 unchanged configurations/prebuild0, inventory preserved, Journal55/55 durable, healthy/Walk0/400MJ/rev26. Counter8 retained before orderly cold deployment; no ZIP or cross-computer claim.

Current installed development cohort:1420 tests (40 Contracts/1302 Core/78 MCP), including the9419da8 diagnostic boundary and source content recorded inb6757c3's Agent guidance/audit slice. Debug/Release, complete current-DSP Release and source MCP64 tools/1 resource/exact43737-character playbook/clean stdout pass; b6757c3 Windows CI34092710946 succeeded. After normal save26879583 and the persisted two-action2310-object/82-detail audit, the exact old DSP process exited normally. All4 source/installed hashes matched before a single Steam launch: Plugin `37A1496B324A2B2295F20DD95A09EBB45CC816FE8A7B94B806047A5D16E3B624`, Contracts `71348A2038706E6EC245DCDB5376DA5AA2353FEF52823EC3793A88BC3075AA74`, Core `777E9143E87B1AAEC621C11C5685D232FA08BBA16B9249A89339744FCA28B8E3`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. No force kill, hot replacement, direct game-EXE launch or new ZIP.

Local live follow-up: protected resume b89b6fbe completed and normally resaved26879615; its action start/end gameTick fields were null. Root raw-65bc888b verified the full2310-object/82-detail saved factory at26890740 and independent prebuild0 at26891422; all structures, configurations and inventory persisted, with Journal55/55 durable, healthy revision1, Walk0/400MJ. Position differs by negative1 float ULP on each axis (x0.000000476837158203125m/y0.00000762939453125m/z0.0000152587890625m); the explicit private4ULP audit envelope neither changes product hashing nor proves a native cause. Accepted count3 remains, not reset by restart. The unconnected oil candidate persists but is not a working supply line.

The new diagnostic boundary passed through Bridge at26890053 and actual source MCP at26949464 (window26948865–26949464,3 factories): the titanium-dependent1106/1118/6003 findings retain confirmed shortage, aggregate source inventory8, stocked_logistics_boundary and dispatch unproven on104:530→1657→102:44. Source MCP raw-4cf7d8f8 had unchanged player hash/revision, zero game writes, exit0 and no extra stdout. This is installed development-DLL local live validation, not a ZIP/cross-computer test or evidence of repaired supply. Current BepInEx error/exception scan returned0 matches after restart.

The following deployment/validation paragraphs are historical snapshots, not current readiness instructions.

Current installed cohort: `0956f6b`,1405 Debug/Release tests (40 Contracts/1291 Core/74 MCP), complete current-DSP Release zero warnings/errors, source MCP64 tools/1 resource/exact42052-character playbook, clean stdout, and Windows Core CI34085762619 pass. Following the audited normal save26508578, the exact prior DSP process exited normally; same-batch DLLs were installed and Steam launched once. All4 source/installed SHA-256 pairs match: Plugin `0E373301A9BC5B432E5E733D62F4389DE2145DB35A898CACFFE7BC5D02B6D7AA`, Contracts `9868ACF303C3F41ECB0806671326F8D925694B400B2999B2A2F42C31E546D843`, Core `1B75423F19FCA0E894E78CF6A8212B2146F5FCE99036A222ACA93DE1228E8408`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. The PhysicsModule compile reference remains private/Private=false, SHA-256 `9B8C76635404945AD690B219415151E244C3D6D0A76851FBD40045272F160D38`; no game binary is distributed.

Local validation: protected resumef2c87cfd completed and resaved26508610. Root's full2299-object/71-detail snapshot preserves saved factory structures/configurations and all player inventory. Player position was not bit-identical: y+0.000030517578125m and z+0.0000152587890625m (4/1 float ULP), x unchanged. This explicitly reported private audit envelope does not change product state hashing or claim a proven native cause. Actual source MCP→installed Plugin returned risky short routes and a40m unavailable/unknown preview without gameplay writes; the root-selected24m route then completed via normal Move55c910d9, followed by Walk0 and an in-range iron transfer. After transfer300 iron and native recursive crafting60 belts, root audited all10 unique terminals,26615174 complete23-page/2299 structures and71 unchanged configurations,26615737 independent prebuild0, Journal55/55 durable/healthy/settled398.267MJ/full power. Current BepInEx error/exception scan has0 matches. Ledger persistence resets accepted10→0; the new materials are not yet separately saved. This is a development-cohort local live result, not a new ZIP, cross-computer or complete0.4 validation. Older paragraphs below are historical snapshots.

Post-deployment recheck: protected same-world resume11bac49a completed and resaved26074553. Root independently verified all4 current-window terminals and the complete2283-object/47-detail snapshot, settled400MJ, durable Journal55/55,0 prebuilds and3 fully served power networks. Native geodesic prepare returned15 NEW points; a bound-source negative returned INVALID_REQUEST without a plan or changes. Construction and continuous supply remain pending. Older deployment paragraphs below retain their chronological evidence, not current readiness instructions.

Latest cold cohort: code commit `3b2d9ea`,1370 Release tests and full current-DSP Release zero warnings/errors after commit. The verified1324 process exited normally after the persisted Save3 audit;4/4 source/installed DLL hashes match: Plugin `48DE383F4AD64F7188261D4DDCE332FC64EC8A30B6045B44278EBBF52A662BAD`, Contracts `9FCAB814D4852A7F7C4BBFE8C367D50B5C4CCDBB53DECC63762892B64DEB45AB`, Core `3D73B7C1B3DDC1B6DE95228ED03DD0D59411A3443ABCFAA44B4BF85FBEE814E6`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Steam launch dispatched once; accepted3 retained. No hot replacement, force kill, direct EXE launch, final package or geodesic live-success claim. Protected same-world resume is next.

Current pre-geodesic deployment boundary:1370 Debug/Release tests (36 Contracts/1264 Core/70 MCP), full current-DSP Release zero warnings/errors, and source MCP64 tools/1 resource/exact40458-character guide/clean stdout pass. The runtime is still1324, not hot-replaced. Luna normal savecd78d82a completed at26074522; root independently reverified all3 current-window terminals,26079023/rev4 healthy owned104,26079037 complete23-page/2283 unchanged structures,47 unchanged detailed configurations/player inventory, independent prebuild0 at26079619, Journal55/55 durable and settled400MJ/full power. Accepted3 is retained for the next protected resume, not reset. Native routing live and final packages remain pending; older paragraphs below retain their dated evidence only.

Current verified boundary: protected resumeeabe71fa completed with null action start/end and resaved25827909 after normal save25827878. Root independently audited all10 unique successful terminal actions (5 resumes,4 saves,1 build),25828447/rev1 owned104/healthy/peaceful/non-sandbox/1×,25828460 complete23-page snapshot/2283 unchanged saved structures and47 details/44 old configurations, independent prebuild0 at25829855, durable Journal55/55 and settled400MJ/3idle/full power. The exact2269→2283→2281→2282 source-cover build, source's native rotation and43 remaining belts survived normal save/close/resume. Ledger persistence resets accepted10→0. This is one same-grade2001 source-cover case, not all belt grades, continuous supply or full0.4/package validation.

Latest save boundary: action6ecdce80 saved25827878 (start=end), and root independently reverified all9 terminals. At25828461/rev4 the same owned104 world is healthy;25828474 single-snapshot23 pages/2283 structures exactly match the post-build poses/rotations/connections,47 details/44 prior configurations persist, independent prebuild0 at25830233, Journal55/55 durable and settled400MJ/full power. Accepted9 retained before the planned orderly same-cohort restart. Client-only fixbdf688f is pushed,9 standalone PowerShell assertions plus the unchanged1324 .NET tests pass, and CI34073601443 succeeded. Current BepInEx error/fatal/TypeLoad/exception scan found0 lines. No final package or publication.

Latest1324 construction evidence: actione1574d6f completed25768360→25768977 via three ordinary drone-built prebuilds; belt2001 inventory46→43, new path2269→2283→2281→2282. Root verified25805050/rev3 healthy,25805063 complete23-page/2283-object snapshot,2280 old identities/positions retained, only source2269's native-proven rotation changed, old edges retained except the planned new output,47 details/44 prior configurations, independent prebuild0 at25805659, durable Journal55/55 and settled full-power400MJ player. Source-cover save/resume remains pending. An expired audit was discarded; the corrected local PowerShell byte-array reader passed9 offline assertions and root's single-log full factory fetch took9784ms with no new game write. The1324 Plugin DLL cohort is unchanged; this is not ZIP or version completion. Accepted8 retained.

Latest1324 live: protected resumedf974eb2 completed with null action start/end, resaved25723180. Root independently verified all7 terminals,25743549/rev1 healthy owned104,23 complete same-snapshot pages/2280 unchanged structures at25743564,43 unchanged key configurations, independent prebuild0 at25747307, Journal55/55 durable, settled400MJ player and three fully served networks. A prior expired-cursor audit was discarded, not joined to the replacement snapshot. Fresh root2269 source-cover prepare at25737359–25737373 confirms all three native modes including `whole_path_native_rotation_v1`,3 NEW/3-item cost, unchanged player/endpoint and0 prebuild/commits. Accepted7 remains7. This proves protected resume and the new read-only confirmation, not source-cover construction or final packaging.

Latest cold install: after root persisted the sixth-action25723149 save audit, the verified1309 DSP process exited normally. All4 tested1324 Release sources exactly match the installed files: Plugin `729899F33A232A97C46E952D23044BD2FF15D04B01FD3B930FC2330FF02FC511`, Contracts `17923D9405F430B8DCE73E7DAB8D28E2747FAD599F2252FC2CFBFFEF0F457A8D`, Core `54547E66523A94B032D98606652D9D14CB73CBCFFBF8AE6DBE651979EFBA5389`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. One verified Steam launch was dispatched. Accepted6 is retained; protected resume and source-cover construction are pending. This development deployment is not a final ZIP or clean-release-build claim.

Latest pre-deployment boundary: save007b61c8-5c7a-4985-81fc-64402ca42c72 completed at25723149 (start=end). Root independently verified all6 accepted terminals,25746211/rev2 owned104/healthy/peaceful/non-sandbox/1×, durable Journal55/55, settled400MJ player, three fully served networks, independent prebuild0 at25746255 and23 complete same-snapshot pages/2280 unchanged structures at25746258. All43 detailed configurations match the fifth-action audit. Accepted6 stays6; the single-log audit layout was checked explicitly. Sourcea6cff2b CI34071202182 succeeded. Installed1309 is still running;1324 cold install, protected resume and first source-cover construction remain pending. Earlier counter/deployment paragraphs are historical snapshots.

Final1324 source verification: locked restore, Debug/Release tests (33/1230/61), complete current-DSP Release with zero warnings/errors, and actual source MCP0.4.0.0/64 tools/1 resource/exact37804-character guide, exit0 and zero extra stdout all pass. The native completion rotation follow-up is not deployed/live yet; these are source checks, not ZIP validation.

Current source follow-up:1324 Release tests (33 Contracts/1230 Core/61 MCP) and complete current-DSP Release zero warnings/errors pass. Native completion rotation now needs exact cargo-path/collider-derived proof plus `sourcePreservationMode`; this is source-only, not installed. The first cover commit remains frozen until cold deployment and a fresh confirmed plan. CI34069716871 forddafe38 succeeded. No new final ZIP is claimed.

Current1309 local live: protected resumef054d5b1-e44e-4941-92dc-31bc70803e9a completed, action start/end null, automatic resave25636692. Root independently verified all5 terminals,25640704/rev1 owned104/healthy/J55/55/settled400MJ/full power,0 independent prebuild at25640750,23 complete same-snapshot pages/2280 unchanged structures at25640757 and43 unchanged key details. Accepted5 remains5. Fresh source-only checks have zero commits:752 has no free belt port;754 overlaps1199 and rejects with source-centre ambiguity;2269 forward4m returns full native non-removing cover echo,3 NEW points and3-item cost. Each retains player/endpoint hashes, healthy/rev1 and0 prebuild. These are narrow prepare positives/negatives, not native construction, continuous supply or source-cover save/resume evidence.

Latest cold install: the1309 tested output (code committed/pushed asddafe380bb20b021c435c2a0908694e64a5e23ed) replaced1258 only after the audited25636661 normal save and verified orderly DSP exit. Four source/installed SHA-256 pairs match: Plugin `4471D411A53FC41AE1E7030713DCFC2F8BE7C1C93718ABA0FB212A52999F2062`, Contracts `17020C2EA948FE9BD2E53A56602BD8CFF9282B5E537AB2AD6921330DC19B20A9`, Core `ACF531A2F0356DEAF527DCFBEF1B32D641DCCD581432965B6ABA781D652C8CD7`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. One Steam launch dispatched, not a direct EXE launch. Final source smoke is64 tools/1 resource/exact37119-character guide, clean stdout/exit0; same-world protected resume and cover prepare/construction remain pending. Accepted4 is retained. This is a development deployment, not a final ZIP or clean-release-artifact claim; earlier source-only paragraphs are historical snapshots.

Current source candidate:1309 Debug/Release tests (33 Contracts/1217 Core/59 MCP) and complete current-DSP Release pass with zero warnings/errors. Source-only non-removing belt cover reuse now has bounded whole-path/member/cargo/old-neighbour proof, NEW-only budgeting and a compatibility echo; the current MCP rejects an older Plugin's unconfirmed belt plan. Source metadata smoke still reports64 tools/1 resource, an exact same-batch playbook and clean stdout/exit0. No1309 deployment, source-cover live success or final ZIP is claimed here; installed1258 remains the earlier stage-check cohort below.

Latest same-world save boundary: Luna's normal save43281160-bf8b-4dd6-bbed-d2df44aa8340 completed at25636661 (start=end), accepted window#4. A preceding envelope/payload session mismatch was a proven STALE_SESSION rejection with no action/accepted; only after fresh reinspection was the corrected request submitted. Root independently verified all four successful terminals,25641130/rev2 healthy/owned104/peaceful/non-sandbox/1×, J55/55 durable/no pending/error, Walk0/400MJ/3idle/empty forge, three fully served networks, independent prebuild0 at25641169,23 same-snapshot pages/2280 unique built at25641174 and43 key details. All2280 identities/poses/connections and41 prior detailed configurations remain unchanged. Accepted4 is retained across the pending cold install/resume; no construction or new save identity occurred. Earlier counter/source/deployment paragraphs are historical snapshots.

Local1258 follow-up: exact-primary resume234f01df-c659-4e77-9cee-e773490d1c3e completed successfully (action start/end null), automatically resaved25521346. Root independently audited25525911/rev1 healthy/owned104, J55/55, Walk0/400MJ, three fully served networks, independent prebuild0 at25525953,23 same-snapshot pages/2280 unique built at25525961 and41 unchanged key identity/pose/connection/configuration details. Accepted3 is retained. A previously specified20-node free path now passes the full stage1 native prepare; a bound754 source is rejected at planned point0 with belt_path_existing_overlap. Neither was committed. Player hashes from25538451/25541440/25541451 are identical; final25541454 is healthy/rev1, source endpoint unchanged and prebuild0. Current BepInEx error/fatal/TypeLoad/NullReference scan found0 lines. Windows CI34067373558 for8883e31 succeeded. This proves bounded prepare behavior and same-world resume, not construction, cargo throughput, faithful cover reuse or final packaging.

Latest cold install: the tested1258 cohort (code committed as8883e311d836f58c45c1c5a0dabd0351015c80ec) replaced the old1217 files only after the audited25521315 normal save and verified orderly DSP exit. Four Release-source/deployment hashes match: Plugin `455F59A876B4A4D0D7653DDE932E61059BABD77B24081D69581839D417E49AD2`, Contracts `126AF2CC75AE2B3155E020C785501C611C3ECEC4606A65E5B780ABBE0AE2C76E`, Core `A29DDEB53FA4994492096076F0123AD1EF695A531A7B77E95EA88B9ABC1ACE6F`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Steam launch dispatched once; protected resume and read-only native checks are still pending. Accepted2 remains2. This is a development deployment, not a new ZIP or completed live gate; earlier candidate/deployment paragraphs are historical.

New source candidate (not installed):1258 Debug/Release tests (32 Contracts/1170 Core/56 MCP), complete current-DSP Release zero warnings/errors, actual source MCP0.4.0.0/64 tools/1 resource with exact35797-character guide, exit0 and clean stdout. It adds full native path checks using a tool-owned inactive stage1 command container without changing the player's command, on top of a8396b6's bounded NEW-belt occupancy containment (Windows CI34066120124 succeeded). Existing-belt cover reuse remains unimplemented. The installed cohort is still1217/5834edc; no new ZIP or live-positive claim is made by this source check.

Current pre-deployment boundary: normal save cd900130-3d48-4551-82da-eaae043cdc7d completed at25521315 (start/end equal), the second accepted action of the current window. Root independently verified both resume/save terminals and the fresh25535243/rev2 owned104/healthy/peaceful/non-sandbox/1× session, J55/55 durable/no pending/error, settled400MJ player, three fully served networks, independent prebuild0 at25535282,23 same-snapshot pages with2280 unique built at25535287 and41 key details. Every built identity/pose/connection equals the complete source-trace snapshot;33 previously audited detail identities/poses/connections/storage settings also match. Accepted2 is retained across the pending normal close/cold install/resume; no construction occurred. Older deployment/counter paragraphs below are historical snapshots.

Latest pre-deployment boundary: the1201 world was normally saved at25328373 (action8ffa3c29); root independently audited all10 unique successful terminal actions, healthy owned104/rev2 at25336801, Journal55/55 durable, settled400MJ player, three fully served networks,23 same-snapshot pages/2280 unique built with every identity/pose/connection unchanged,0 independent prebuilds and33 key details. Diary/ledger persistence resets the accepted counter10→0; the next protected resume is new-window#1. Source5834edc has1217 tests, a zero-warning/error full Release build,64 tools/1 resource with34268-character guide, and successful CI34063130914. The new failure-stage diagnostics have not yet been installed/live-tested. Supply remains blocked and no final ZIP is claimed. This boundary supersedes the older accepted counts below, not their historical installation evidence.

Current development deployment: source `5834edc3a0b3e3741a5527feb66897e07bdfa7e4`,1217 tests (32 Contracts/1131 Core/54 MCP), full Release zero warnings/errors, source MCP64 tools/1 resource with34268-character guide and successful CI34063130914. After audited save25328373, orderly exit and one Steam launch,4/4 installed files equal their Release sources: Plugin `BE9643C4025A2E260A5A5A0DB297BB19C34A5F33AFCCBC7942293169874AF904`, Contracts `FDC543B00B6E52E090A44A58AA6F9F92850807BBA041573FA90FD8F6F9EF9A3E`, Core `4FF34E9EF74D20EF12BB6D824A3E0B6440E336FF3E88720752C55E7094DFDADE`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Current DSP DLL SHA remains the table value below. Protected resume2fe2922b succeeded with null action start/end ticks and resaved25328405; root audited25331137/rev1 healthy/owned104/peaceful/non-sandbox/1×, J55/55, Walk0/400MJ,0 prebuilds,3 fully served networks and33 unchanged identity/pose/connection/settings details. New-window accepted1 remains1. Four exact prior candidates now report real rejection stages (two no-finite-projection, two facing minima59.768°/19.745°);0 build commits. Diagnosis is locally verified, successful offset-assisted construction and sustained supply are not. No final ZIP, cross-computer test or complete0.4 acceptance is claimed. Older deployment/counter paragraphs are historical, not current authority.

Latest pre-deployment boundary: the1066 world was normally saved at25092892 after three successful configuration actions. Root independently verified all four terminals, healthy owned104/rev29, Journal55/55, settled full-energy player,23 same-snapshot pages/2280 unique built with prior identity/position/connections unchanged,0 independent prebuilds and full service on three networks. Accepted count4 is retained across the pending restart. Source4faf169 has1144 Debug/Release tests and a zero-warning/error full Release build; actual source MCP64 tools/1 resource matches its32678-character guide. The new instant configuration DTO and hash-domain diagnostic are not installed/live yet. Warehouse settings are saved, but restoration and sustained chemical/yellow supply still need separate evidence; no new final ZIP exists.

Subsequent local live on the same1066 deployment supersedes the pending-feature sentence in the installation snapshot below: held-cargo filters, set-bans, lock-occupied and one empty water reservation have now passed individual native terminal/readback checks. The latest ten-write audit at24946625/rev22 retained owned/healthy, Journal55/55, Walk0/400MJ/3idle,2280 unique built across23 same-snapshot pages,0 independent prebuilds and three fully served networks. Storage761 is still banned30 with317 oil/1055 plastic/1 hydrogen and zero water; reservation and stock preservation are proven, sustained supply and settings save/resume are not. Source00b2243 has1115 Debug/Release tests, full Release zero warnings/errors,64 tools/1 resource with31150-character guide and successful Windows CI34056095397, but is not installed. No new ZIP or final0.4 acceptance is claimed.

Previous1066 installation snapshot: `ca7b64edd6958ed27b356bda5e5c49fcd71db2a4`, 1066 Debug/Release tests (30 Contracts / 987 Core / 49 MCP), full Release build with zero warnings/errors, and successful Windows Core CI `34053887675`. Source stdio metadata verified 64 tools / 1 resource and an exact30340-character playbook. After audited normal save24827987 and orderly process exit, all four installed files matched their Release sources: Plugin `0C4920FD53254EB8B17DED7D1E9FB38CDC93C1D0125E42DD0EC01D360C815101`, Contracts `54556E8788223C36C12CFE0D053BB3779C9DC4D10D166F60671369F4B659B39D`, Core `CA53009CBE8B6C1586B361EDBAB1BA3D22D3E5FCCF34C7E5F55BB28DF0C17B07`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Steam was launched once through its normal app route. Exact-primary action `f331b862-a4eb-476e-a26f-3c42dc604584` completed successfully and automatically resaved24828018; its action start/end game ticks were null. Fresh24830745+ retained owned planet104/healthy/peaceful/non-sandbox/1×, Journal55/55 durable, Walk0/400MJ/3idle, no prebuilds, and full service on three networks. Held-cargo filters and warehouse settings were installed but not yet live-accepted at that point. This is not a new ZIP or final0.4 validation; older deployment records below retain their historical scope.

| Item | Evidence |
|---|---|
| OS | Windows 10 build 26200, win-x64 |
| .NET SDK | portable 8.0.424 on the current takeover machine; previous verified baseline 9.0.315 |
| .NET 8 runtime | 8.0.28 available |
| DSP location source | Steam auto-detected |
| DSP root | `<DSP_ROOT>` = Steam library `steamapps/common/Dyson Sphere Program` |
| DSP version | 0.10.34.28529 from `Updates/Versions.txt`; Steam build ID 23109513 |
| Assembly-CSharp SHA-256 | `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85` |
| Unity executable version | 2022.3.62.1451004 |
| BepInEx | 5.4.17.0, installed from `xiaoye97-BepInEx-5.4.17` |
| BepInEx.dll SHA-256 | `DC1CB6B58B962BDA5AAA1D6B5F9AE14EC174F61836A1A1F96C1A040C7E8381F7` |
| MCP SDK | `ModelContextProtocol` 2.2.0, stable NuGet package, repository commit `6fa3825973949a9c4f0cd8af344e15a8db09dc35` |
| Plugin target | net472 / BepInEx 5 |
| Core targets | netstandard2.0 |
| MCP/tests target | net8.0 |

Package versions are pinned in `Directory.Packages.props` and resolved transitively in repository `packages.lock.json` files. Game assemblies remain under `.local/game-refs` and are not committed.

## Local paths and commands

No absolute game path is embedded in a project file. Scripts resolve the installation from `SPHEREWRIGHT_DSP_DIR`, Steam registry/library metadata, or an explicit `-DspDir` argument. Documentation uses these variables:

```text
<DSP_ROOT>
<DSP_ROOT>\DSPGAME_Data\Managed\Assembly-CSharp.dll
<DSP_ROOT>\BepInEx\plugins\Spherewright
```

The local development flow used:

```powershell
./scripts/locate-dsp.ps1 -AsJson
./scripts/sync-game-refs.ps1
dotnet restore Spherewright.Core.slnf --locked-mode
dotnet build Spherewright.sln --no-restore
dotnet test Spherewright.sln --no-build --no-restore
./scripts/install-dev-plugin.ps1 -NoBuild
./scripts/smoke-test.ps1 -LiveBridge
```

`sync-game-refs.ps1` copied only the required compile references to `.local/game-refs` and recorded hashes; it did not change a game assembly. `install-dev-plugin.ps1` copied only Spherewright outputs plus its JSON dependency to `<DSP_ROOT>\BepInEx\plugins\Spherewright` and refuses to run while DSP is active. On 2026-09-02, after normal save tick `9413535` and normal process exit, the 46-tool Release assembly set was deployed together and matched its build outputs byte-for-byte: Plugin `36E164835BDC8C3F208FF9C98DD0138148B260923FC3602A8ECD83A735D9E003`, Contracts `76F8AF903BEA5D15067D46B766D33F5F79BA034F922C47AE07D5FE90551CD70E`, and Bridge.Core `4D99FC51E549152DBB209BA6D5CAC90F8BB1AED33F4F3CAAB1A5F00695C4397F`. The protected exact-primary resume then succeeded and automatically resaved tick `9413567`; no game assembly or save file was edited.

## Verification history (newest first)

- On 2026-09-07, source commit `83a191aa284db703186ea897bbe90f4ca08d1556` passed 1018 tests (28 Contracts, 945 Core, 45 MCP) in Debug/Release and a zero-warning/error full Release build; its exact staged snapshot passed independently and Windows Core CI `34049565268` succeeded. After normal save24568249 and orderly shutdown, four installed DLLs matched their Release sources: Plugin `2933763D0CBCC074B7D655A88D58C3AE9CC31BAE2758F4B066B6D141BF19968B`, Contracts `77FC7A00B9D4DEFFB1A6C676363836E9EF9665EF0C07CC01EC46A79F60477D46`, Core `BA6231FE3C469021CA5EF0B08BAF3B77C4C54623BECA8B3634595E2F593EFBF5`, Newtonsoft.Json `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Direct EXE launch mistakenly ignored EXP-002 and exited with `SteamAPI_Init()` failure before any resume; one subsequent Steam launch remained active. Protected exact-primary action `e2eb85a4-a760-4a5b-8f3c-f1d2019de98c` completed successfully and automatically resaved24568281. Fresh audit24581014+ retained the same healthy peaceful/non-sandbox/1× owned world, Journal55/55 durable, 2280 built/0 prebuild and full service on three networks. Twenty-two selected details live-verified the new endpoint/unavailable semantics and fourteen bounded belt-cargo observations. This is local development deployment, not a fresh release ZIP, cross-computer validation, completed initial-filter construction, water-line recovery or final0.4 acceptance.

- On 2026-09-05, the journal-continuity refresh produced the owner-review candidate from clean commit `8c49bcba1226e152a653fb5cfded7face00db86b`. The full Release build had zero warnings/errors; all 262 tests passed (Contracts 19, Core 217, MCP 26), as did Windows Core CI run `33957178227`. The manual package has 233 manifest entries plus its manifest, SHA-256 `001323b108d76b0de4fdc9102312a8db2c8ba85d555392e7b8b572ad32542ee8`; its package smoke negotiated protocol `2025-06-18`, reported MCP `0.4.0.0`, listed 53 tools and one readable Agent-playbook resource. The 12-entry Thunderstore package has SHA-256 `c8e53d60b37940cca9fe229dbaecaf4f72f4c71c599e5a63d685ae2a7a5c33ed`, the same clean source commit, and passed static manifest/icon/payload verification; runtime Mod-Manager testing remains separately unclaimed. The actual manual ZIP installed with all four Plugin files and all 224 MCP files present and byte-equal, with no MCP extras; three old PDBs remain outside the manifest and are not runtime assemblies. Protected resume from save tick `18318499` restored only planet `104`, automatically saved `18318530`, and was fresh-read at tick `18319586` as owned/saved/healthy with Journal `49/49`, no blocker, and no checkpoint. Installed MCP/playbook reads succeeded; a `public_allowlist_v1` bundle at tick `18334303` returned all three factories/planets on one captured tick without an observed private field. No tag or release was created.
- On 2026-09-04, the first clean v0.4.0 package preflight was deliberately rejected as the final candidate after a human-document readback found stale version prose. Commit `f43c8ce` produced a `sourceDirty=false` ZIP with 232 manifest entries and SHA-256 `b586a79452c50b94282e08f4ea09adc6766abbe08a7b0386ef6a0a2a493392a3`; Release build was clean and all 223 tests passed (Contracts 19, Core 181, MCP 23). Independent package smoke reported protocol `2025-06-18`, MCP `0.4.0.0`, and 53 tools. After normal save tick `17635167` and an orderly DSP exit, all four packaged Plugin runtime files and all 224 self-contained MCP files installed with zero hash mismatch. Three old development PDB files could not be removed because the host denied that deletion; they are absent from the package manifest and are not loaded runtime assemblies. Exact-primary action `adaabff9-3b8f-4d91-97da-c02d7407af57` restored only the same planet `104` world and automatically resaved tick `17635198`. The installed Plugin rejected a wrong token and stayed healthy; installed MCP returned a ready 600-tick three-factory bundle at tick `17640449`, then three cursor pages at the identical tick `17647128`, with both wrong-item and wrong-limit continuations rejected as `STALE_CURSOR`. Its public JSON contained no audited protected property or absolute Windows path. Final inspection then found that packaged `INSTALL.md` still called itself v0.3.0 even though every machine-readable version was 0.4.0. IFX-023 changed that source document to version-neutral manifest-authoritative wording; this ZIP remains preflight evidence only and must be regenerated from the next clean commit.
- On 2026-09-04, the staged one-ended route closed the controlled power-fault/recovery gate and corrected an earlier sampling inference. Normal harvest at node `102:379`, exact native refuels, preflight save tick `17409786`, return action `efabaa9f-7e11-49f6-8f4a-27d018239b67`, 600 stable landing ticks, checkpoint retirement, and landing save tick `17420046` returned the source station's sole vessel in player inventory while keeping that station at zero fleet. The home ILS ceiling was set to 150 MW and thermal-generator sorter `678` was filtered only while cargo-free; generator `183` naturally reached zero output. Low-frequency audit tick `17442891+` still saw the full station and ratio 1, but a later high-frequency native silicon dispatch at tick `17472191` caught about 59.7 MJ removed from the home station and an 8.30 MW request, proving the prior read cadence had missed the short departure window. Silicon deliveries advanced `533 -> 733 -> 933`; the sole home vessel then immediately entered a 200-titanium order. At ticks `17505966–17505969`, home ILS energy was `11,873,487,813/12,000,000,000`, requested charge was 9.13776 MW, network 1 required/served/capacity was `197786/115000/115000` with ratio about 0.5815, and the same-tick bundle returned six `insufficient_power / confirmed` findings for real producers on that network. Normal actions `0f7c84b7-847b-4502-81b2-5384db1e4bfa` and `427099af-62d2-41fb-a1ad-75d6314d55cd` restored the sorter filter to zero and the station ceiling to 30 MW. By tick `17512817`, the station was full, all power was served, underpowered stations were zero, and no power finding remained. No inventory, device buffer, station energy, consumer request, or grid field was directly written. Two later active-route save/normal-exit/exact-primary-resume cycles at `17572610 -> 17572642` and `17579665 -> 17579696` reset the protected baseline to each new session's current game tick, excluded offline wall time, completed delivery, and retained titanium-ingot output at `12 min⁻¹`; final healthy save was tick `17584412`. A true 600-tick frozen-carrier/recovery trial remains an explicit live-coverage limitation because this DSP build has no safe normal-game control that freezes an already-dispatched carrier while preserving its order and route.
- On 2026-09-04, ten normal short-arc moves carried the player from the source ILS area to within 7.444 m of coal vein `102:379`, without changing the saved one-ended logistics precondition. The mandatory audit at tick `17371613+`, revision `90` reconciled ten unique actions as terminal/completed/succeeded with no stall, recovery, or outcome-unknown reconciliation. The same owned peaceful, non-sandbox, 1x world remained healthy with no blocker/checkpoint, 175 built/0 prebuild, network 1 at `4050/4050` and ratio 1, Journal 49 durable with no pending/error, and no BepInEx error. The player was Walk/0 with about 213.6/400 MJ core energy, empty fuel and handcraft queues, 3/3 idle drones, and exactly one vessel. Station `102:44` remained full-energy and fleetless with titanium 200/200, silicon 109/300, Remote Supply, and zero orders. This audit reset the write counter; coal may now be acquired through the normal harvest/refuel path for the return flight.
- On 2026-09-04, the remote supply endpoint was staged for single-ended shipment-energy attribution. Eight normal MoveTo actions followed a precomputed 217.8 m great-circle path in roughly 27 m segments and stopped about 45 m from ILS `102:44`; all completed without stall or recovery. One next-step prepare saw natural residual speed and returned `STALE_STATE`, so the caller waited for a fresh Walk/0 snapshot and did not replay. Normal fleet transfer action `d0ac4683-8c05-4d07-83a7-f29b3a0e3e02` conserved the only source vessel from station idle `1 -> 0` to player inventory `0 -> 1`, with station working fleet 0, energy 12 GJ, cargo and configuration unchanged. Save action `9a77399b-84d4-4821-95b8-ef3dfd9073ac` persisted tick `17334563`. The resulting ten-write audit at tick `17338179+` found every action completed once, healthy writes, no blocker/checkpoint, Journal 49 durable with no pending/error, 175 built/0 prebuild, the sole remote network fully served, and no BepInEx error. The source station remains Remote Supply with titanium 200/200 and silicon 109/300, zero orders, and zero fleet while the player holds exactly one vessel. This is a controlled precondition, not yet an underpower result; return energy must be acquired normally before the home-only dispatch trial.
- On 2026-09-04, a checkpointed staging flight moved the same owned world from planet `104` to source planet `102`. Flight prepare first rejected 18 coal because the conservative reserve was only about 449 MJ versus the 600 MJ gate, without creating a checkpoint. Native harvest reduced coal node `402` by 80 total and native refuel conserved the package/fuel transfer; a local reporting failure after the last 20+20 pair was reconciled from the unique fresh node/package/fuel state and never replayed. The safe preflight state was normally saved at tick `17296462`. Action `2209a388-9f77-41f7-bd31-d32f7d9e6066` then created its own flight checkpoint, maintained decreasing distance and increasing speed up to 1000 m/s even while low-power coal let core energy reach zero, and completed only after 600 grounded Walk ticks on planet `102`; checkpoint capability disappeared. The landing was normally saved at tick `17305571`. The mandatory ten-write audit at tick `17309480+` found healthy writes, no blocker/checkpoint, Journal 49 durable with no pending/error, 175 built/0 prebuild, remote network 1 fully served, and no BepInEx error. Source ILS `44` was full-energy at 30 MW with titanium `200/200`, silicon `109/300`, both Remote Supply, no order, and one idle vessel. This provides known source stock; the next controlled step is to withdraw that idle source vessel normally, return home, and open demand so only the home station can initiate and pay for the trial shipment.
- On 2026-09-04, a second reversible live fault trial validated controlled material shortage without touching inventory or buffers. Crude-oil input sorter `162` was cargo-free and unfiltered on the only currently continuous refinery `141`; the normal sorter UI field path temporarily set its filter to unlocked iron ore `1001`, which the source belt did not carry. After the refinery consumed its existing four crude oil, bundle tick `17177994` reported `material_shortage / confirmed` with input item `1007`, available `0`, required `2`, and 600-tick refined-oil output `0 min⁻¹`. Clearing the filter through the same normal path let the sorter carry crude again, refilled the input to four, removed the finding, and restored a later complete window to `12 min⁻¹` produced and consumed. Subsequent controlled power attempts established stricter preconditions rather than fabricating a success. Raising full ILS `1657` from 30 MW to 150 MW kept requested charge at 60 kW. One delayed native `+200` silicon order completed delivery `333 -> 533`, yet low-frequency full-trip sampling saw only 12 GJ, 60 kW, and network ratio 1; a local working-vessel count therefore did not prove which endpoint paid energy. A second combination let thermal generator `183` naturally fall to zero after filtering its fuel sorter, then reopened a `533/700` demand under the 150 MW ceiling, but the remote endpoint had no stock and dispatched nothing. The first mandatory audit at tick `17196640+` and the next at `17272033+` each found 2254 built/0 prebuild, all networks at ratio 1, full stationary player energy with empty handcraft/construction queues, Journal 49 durable with no pending/error, healthy writes, no blocker/checkpoint, and no BepInEx error. The second audit also proved fuel sorter `163 -> 678 -> 183` restored unfiltered and the generator back at `18,335/tick`. These were valid negative controls; later high-frequency dispatch evidence and the completed power-fault/recovery gate are recorded in the newest entry above.
- On 2026-09-04, a reversible live logistics-fault trial stayed on the same owned planet `104` world. Raising the home ILS silicon demand limit from 100 to 300 through `PlanetTransport.SetStationStorage` produced a native `+200` order; delivery increased home silicon `133 -> 333`, cleared the remote shared-input head, and allowed a separate titanium shipment to deliver 189 ore. The titanium smelter then refilled local PLS supply `918`. With all orders settled, the same normal station API changed only that slot's local logic from Supply to None; supply inventory accumulated while demand station `916` and its ten drones remained intact. At bundle tick `17130476`, the full 600-tick production window reported titanium-crystal assembler `767` as `logistics_blocked / confirmed`, with `logistics_configured=false` and the physical path ending at demand station `916`. Restoring Supply dispatched native drones, cleared the logistics finding, and titanium-crystal actual output reached `12 min⁻¹`. The final normal save was proved by fresh `LastOwnedSaveGameTick=17136808`, revision 8, healthy writes, no blockers/checkpoint, and a new protected restart ticket. No item, station buffer, energy, research, position, or save file was directly written. A true stalled-carrier temporal trial plus controlled material-shortage and insufficient-power trials remain open.
- On 2026-09-04, the project owner explicitly approved the narrow v0.3.1 compatibility release for cross-computer testing. Annotated tag `v0.3.1` points to commit `33a733f4b4d07d3d916946426b4e4905c60e4954`, whose direct parent is the immutable v0.3.0 commit `a52ff440b47830f2f3a06a5ae97c7ff11bd15833`; no unfinished v0.4 code entered the backport. The GitHub prerelease asset is `sourceDirty=false`, contains 232 manifest entries, reports MCP `0.3.1.0` with 50 tools, and has ZIP SHA-256 `b05eabb20928e98850f6792ea001149fd2e30c92082994e6d9c43254e611cdcf`. The backport full build passed with 0 warnings/errors and 127 tests. It remains a prerelease because the user will validate original-save preservation, new-copy save/resume, and restricted re-entry on another computer; no such live result is claimed yet.
- On 2026-09-04, the ninth v0.4 Overseer slice completed a source-equal live deployment of the dedicated same-game-tick diagnostic bundle. The old process first saved normally at tick `17048233` and exited through its window; the Release build then produced 0 warnings/errors and deployed Plugin/Contracts/Core SHA-256 `90408AD2BC9ED88335853F09695BB75A0900522D9CA022AF86E644DC393B1B16`, `583AFCFCC3995C80278679CA891191DA43CFC7B370B6EB2DD4E5CD197524BAF7`, and `98DFF4CDA2F192691070892F94AD6FDDB47901E594D410C37AC5222E33131A3F`, each byte-equal to the source output. Exact-primary protected resume stayed on planet `104` and automatically resaved tick `17048265`. At live tick `17051000`, one `public_allowlist_v1` complete page returned all three factories and exact per-planet captured ticks; three `limit=1` continuation pages retained the same snapshot/tick and returned planets `104/102/103`. Reusing that cursor with either another item filter or page size failed closed with `STALE_CURSOR`. The 12,156-byte serialized public response contained none of the audited protected-save/auth/plan field names, Windows/UNC absolute paths, or internal save markers. The source MCP negotiated protocol `2025-06-18`, reported `0.4.0.0` with 53 tools, and successfully called the live bundle. Final audit tick `17059827+` remained peaceful/non-sandbox/1×, healthy, with 0 blockers/checkpoint/prebuild, 2254 built entities, Walk/0, full core, 3/3 idle construction drones, Journal `49/49` durable, full service on three power networks, and no BepInEx error. All 223 automated tests passed (Contracts 19, Core 181, MCP 23). Controlled stalled-shipment/recovery and the three fault/recovery gates remain open.
- On 2026-09-04, the explicit player-loaded save handoff was implemented as a conversation-confirmed boundary, replacing an unshipped hotkey/code draft. The restricted prepare binds only the current process/session/revision/`GameData` and returns a generic disclosure; commit requires a later conversation-confirmation declaration plus both original-save/journal acknowledgements, then revalidates peaceful/non-sandbox/1×/local-factory state before the normal-save/header-proof adoption path. The current `Assembly-CSharp.dll` remains SHA-256 `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85`. The full solution built with 0 warnings/errors and all 214 tests passed (Contracts 18, Core 174, MCP 22); the public MCP registration now contains 52 tools. No Plugin deployment, game mutation, save write, tag, or Release occurred in this offline batch; live original/new-copy/restricted-reentry validation remains open.
- On 2026-09-03, live logistics-window validation and snapshot-capacity hardening stayed on `owned-world-001`. Remote ILS `102:44` was normally reconfigured from titanium/silicon limits `100/100` to `200/300`, which exposed the current single-input-belt head-of-line behavior and allowed a full 200-item titanium order. Source/demand reservations became `-200/+200`; more than 2100 moving ticks remained finding-free, source titanium fell `200 -> 79` on pickup, the vessel delivered and returned, and home titanium-ingot production recovered to `12 min⁻¹` while the remote miner reported `30 min⁻¹`. Remote save `14535735`, successful native return action `3515d9f4-8a65-404b-b7bd-79f75ed7a7bc`, and home save `14575384` cover the result. High-frequency observation then found that complete first pages with no cursor were retained in `SnapshotPageStore` and could exhaust unreachable capacity. The final store retains only genuinely paginated snapshots. After an orderly close, the Release Plugin/Contracts/Core files were source-equal at `66D6E4631D0AF8DD3B6C7D6AE11DFF02AE37B13EE8A7A8D77DD2BDCF598B38C9`, `D0F580634540C486BFE865A8C6E50402647AD6A435F7D66205D6DDCAB9C2353E`, and `DE36BF4028D3C3FED0A5E7CA871F7CA040E66EF0682521A2BF5A25E6BE62AA32`. Exact-primary resume automatically resaved tick `14575416`; the persistent process was launched through Steam. Live ticks `14577940–14577989` accepted 16 complete three-factory pages without cursors, retained eight real `limit=1` snapshots, rejected the ninth with `SERVER_BUSY`, and still served both a complete page and an existing continuation while full. Final audit tick `14585723+` remained peaceful/non-sandbox/1×, healthy, with 0 blocker/checkpoint/prebuild, 2254 local built entities, Walk/0, 3/3 idle drones, Journal `49/49` durable, full powered-network service, and no BepInEx error. The full Release solution built with 0 warnings/errors and all 206 tests passed (Contracts 17, Core 168, MCP 21). A controlled stalled-shipment/recovery trial and the three final controlled fault trials remain open; no tag or Release was created.
- On 2026-09-03, the seventh v0.4 Overseer slice extended exact producer recursion across one physically proven logistics route. Current-assembly decompilation showed that station input ports draw from the station-wide `needs[]`; `UpdateInputSlots` writes `storageIdx = needIdx + 1` only after `TryPickItemAtRear` succeeds, so input `storageIdx` is not a fixed selector. The Plugin now reverse-traces each supply endpoint item from every exact Input belt through the existing sorter/splitter-aware graph, then resolves producers by planet/object/item only after every owned factory is deep-copied. Final review kept aggregate inventory/carrier evidence separate from the single public supply path and forbade a logistics path from jumping into a different local direct candidate. The final normal save/orderly-close/source-equal-deploy/exact-primary-resume boundary was tick `14413801 -> 14413832`; deployed Plugin/Contracts/Core hashes were `344614FE3B827BE8397D5D6DC77C3CCB90C8991C01D088E3108B6F473AB11869`, `8E2FB3205B54972180540D6A6C9B08F62B028453DA5E44BBC36CA96E04F56991`, and `9507C3AAEE729ACF13693573C2AB53466B8043F53B762B054F2DEEBD59AAF412`. Live tick `14414535` traced home titanium-ingot assembler `104:530` through demand `104:1657` and supply `102:44` into blocked titanium miner `102:1` on an unloaded remote factory; item `1004` independently reproduced that miner's theoretical `60 min⁻¹`, actual `0`, and 50/50 output block at tick `14417684`. The yellow-matrix same-planet path remained unchanged. Three production pages shared tick `14415270` and rejected a mismatched filter cursor with `STALE_CURSOR`. Source MCP hash `E86BE095EA8FDF10D7487C65876EEFD534CE3E4684F9EC31C378F3A737A4E70E` negotiated protocol `2025-06-18`, reported `0.4.0.0`, listed 50 tools with the supply-input description, and returned the same cross-planet path at tick `14416829`. Final audit at tick `14418919+` remained peaceful/non-sandbox/1×, healthy, with no blocker/checkpoint/prebuild, full core, idle player/drones, Journal `49/49` durable, and no BepInEx error. The full solution built with 0 warnings/errors and all 205 tests passed (Contracts 17, Core 167, MCP 21). Active/stalled shipment and controlled fault trials remain open; no tag or Release was created.
- On 2026-09-03, the sixth v0.4 Overseer slice added a current-user-protected per-save logistics progress window for physically traced demand routes. Current-assembly review bound local drones to `endId/direction/maxt/t` and remote vessels to `otherGId/stage/uPos/uSpeed/warpState`, with native order records cross-checking item and peer station identity. A route qualifies only while the consumer lacks one cycle of input, the demand endpoint has a positive reservation, source inventory is positive, and the deduplicated endpoint fleet is nonempty; a refilled consumer buffer, carrier motion, delivery, order reduction, or active-route-carrier count change resets the baseline. Only 600 continuous game ticks without progress yield a suspected stall. Save/route change, tick rollback, same-tick mutation, or a gap above 3600 ticks invalidates continuity. Final review replaced per-route synchronous persistence with one atomic batch replacement per read; temporal analysis enters public DTOs only after that batch is durable. The normal save/orderly-close/source-equal-deploy/exact-primary-resume cycle stayed on the same owned world at tick `14290235 -> 14290266`. Deployed hashes were Plugin `A66033BFC60DBCAC8B2E798F815E7A22E635AAFCBFDD7F604E5256F191E3CDC5`, Contracts `CFC334048B20C6AE775B81DF9C963B58533C19F47285C7FC7184ED17508B208C`, and Bridge.Core `EE9F5519C23A1EC9BC21987D78D29D90A79E561EBEE80F72702A74678B8E492E`, all byte-equal to final build output. The protected version-1 document held three hashed routes and 2942 bytes, a protected DACL with no foreign allow rule, no raw save identity, new-session samples after resume, and both consumer-input-missing states. Source MCP `0.4.0.0` negotiated protocol `2025-06-18`, listed 50 tools with the positive-demand/refill-reset description, and returned factories `104/102/103` for item `6003` at tick `14302792`. Final production/summary pagination shared ticks `14304692/14304700`, rejected a filter-mismatched cursor with `STALE_CURSOR`, and the final audit at tick `14308785+` remained confirmed peaceful/non-sandbox/1×, healthy, 0 blockers/checkpoint/prebuilds, player Walk/0 with full core, 3/3 construction drones idle, and Journal `49/49` durable. The full solution built with 0 warnings/errors and all 204 tests passed (Contracts 17, Core 166, MCP 21). All three current routes lacked orders and active carriers, so active/stalled shipment trials, cross-planet producer recursion, and controlled fault trials remain open.
- On 2026-09-03, the fifth v0.4 Overseer slice added bounded same-planet upstream-producer tracing. Reverse cargo traversal is item-specific and rejects sorter filters that do not admit the item. A final metadata review of the unchanged current assembly additionally proved the splitter rule: `SetPriority` moves the chosen belt to `output0`, while `UpdateSplitter` sends only `outFilter` to that priority output and excludes it from `output1..3`. Traversal now retains the exact splitter connection slot, cross-checks component/entity/downstream-belt identity, and applies both halves of that rule before entering the splitter; invalid identity fails closed. The latest normal save/orderly-close/source-equal-deploy/exact-primary-resume cycle stayed on the same owned world at tick `14109460 -> 14109491`. Deployed hashes were Plugin `46E62CC930CAD0756BBFB06625C9585F04A074B25FEDC15C4D7DCE2A322F4B70`, Contracts `507C57A10C49435C8D0AEF71F49DA5C6255711C9093D651CBD1A57F91088055F`, and Bridge.Core `CA8B33DD66330211ECD78E535CBD05932933278AF6E640BE67AF7AFD6301E7C5`, all byte-equal to build output. Bridge tick `14111293` and final-build source MCP tick `14138110` both traced `matrix lab 774 / item 6003 -> material 1112 -> assembler 715 / item 1112 -> material 1109`, ending naturally with no trace-stop marker. The MCP `0.4.0.0` negotiated protocol `2025-06-18`, listed 50 tools with the sorter/splitter-filter-aware recursive description, and returned the same path. Final production and summary pagination shared ticks `14119083/14119093`, returned planets `104/102/103`, and rejected a filter-mismatched production cursor with `STALE_CURSOR`. Audit remained confirmed peaceful/non-sandbox/1×, healthy, 0 blockers/checkpoint/prebuilds, player Walk/0 with full core, 3/3 construction drones idle, and Journal `49/49` durable. The full solution built with 0 warnings/errors and all 188 tests passed (Contracts 17, Core 150, MCP 21). Cross-planet producer recursion, temporal transport progress, and controlled fault trials remain open.
- On 2026-09-03, the fourth v0.4 Overseer slice connected direct assembler, matrix-lab, and miner diagnostics to current runtime buffers, power, veins, and physically traced logistics-station routes. The first live trace stopped at intermediate storage `259`; after storage/tank/inserter transit was admitted, the exact directed path from assembler `530` through sorter `532`, storage `259`, sorter `1784`, and the belt chain reached station `1657` output belt `1783`, then attached remote supply station `44` on planet `102`. Three normal save/orderly-close/source-equal-deploy/exact-primary-resume cycles remained on the same owned world; the final save/resume pair was tick `13943810 -> 13943842`. Final deployed hashes were Plugin `D40D6BEA4E76697EB14C5F1DE3B0CC61532E4BF634125E1A9488D5024FDF59E1`, Contracts `74D9F98325CA5620991D4595C3F216C261B9523629990C31777F5CC8422BD9B5`, and Bridge.Core `3F4C7B197B03F843E8963F3B3C2573CF559E63EC8E04105CB39FBB0FEA0D0D67`; a post-documentation rebuild still matched all three deployed files. Natural live states returned `output_blocked`, `material_shortage`, transient `insufficient_power`, and identity-lost `vein_exhausted` infrastructure findings without game writes; an active item with positive aggregate production remained finding-free. A final `limit=1` three-page snapshot shared tick `13986388`, returned planets `104/102/103`, and rejected a filter-mismatched cursor with `STALE_CURSOR`. The source MCP `0.4.0.0` negotiated protocol `2025-06-18`, listed 50 tools with the updated direct-diagnostic description, and called the live endpoint successfully. Audit tick `13990990+` remained confirmed peaceful/non-sandbox/1×, healthy, 0 blockers/checkpoint/prebuilds, player Walk/0 with full core, 3/3 construction drones idle, and Journal `49/49` durable. The full solution built with 0 warnings/errors and all 174 tests passed (Contracts 17, Core 136, MCP 21). Recursive upstream traversal, temporal transport progress, and controlled fault trials remain open.
- On 2026-09-03, the third v0.4 Overseer slice completed a source-equal live deployment. The first candidate correctly failed closed on a legal depleted-miner shape, which was then fixed and covered by a zero-source regression test. A normal save at tick `13831872` was followed by an orderly window close; all seven Debug deployment files matched their build outputs, including final Plugin SHA-256 `5AC257D5AB8013E7D088A8609D08A9FA7FD83A633D4D2DA2F0F549BA53815DC1`. Protected exact-primary resume automatically resaved tick `13831903`. The active three-factory world returned complete current-component theoretical rates for matrices, mining, water, oil, smelting, chemical production, and assembly; a three-page snapshot shared tick `13837732`. The source MCP `0.4.0.0` completed `initialize`, listed 50 tools, and called the live production endpoint. The full solution built with 0 warnings/errors and all 160 tests passed (Contracts 17, Core 122, MCP 21).
- On 2026-09-03, the second v0.4 Overseer slice passed a final source-equal deployment after the research queue identity gate was tightened. A normal save at tick `13767062` was followed by an orderly window close; seven Debug deployment files matched their build outputs, including Plugin SHA-256 `3766E3A770FFB7BAA24FA870CA569BD90F5BE776802A04F213EB2634B79E9C6E`. The protected exact-primary resume automatically resaved tick `13767093`. A three-page `limit=1` snapshot shared tick `13773036`, safely returned factories `104/102/103`, accepted the valid `[3401]` queue, preserved a ready native production window, and reported corrected generated/exported energy. The full solution built with 0 warnings/errors and all 150 tests passed (Contracts 17, Core 112, MCP 21).
- On 2026-09-03, the station output-selector batch built with 0 warnings / 0 errors and 114 tests passed (Contracts 13, Bridge.Core 82, MCP 19). After normal save tick `10449537` and an orderly game exit, Release Plugin `941951FA0F5B8ADDEE16EF1B66B0ABA98664BF79474F49AEFACD6668071B0C76`, Contracts `1DD0C244463FBB78EA4C769990341E8699E3372E4F4155693869961B77097274`, and Bridge.Core `F032394C60CC9840FEE854A2ADC3702D7EA942378DBA75D939EE24894999DDF6` matched the deployed files byte-for-byte. Exact-primary resume and live PLS output selection then restored titanium-crystal production; normal save tick `10456408` covers the result.
- Full solution build: succeeded with 0 warnings and 0 errors.
- Automated tests after checkpoint lifecycle, resume-source, movement recovery, belt free-end, and journal durability changes: 76 passed (Contracts 4, Bridge.Core 59, MCP 13).
- BepInEx load: verified in DSP at the main menu and in a current-process-created world.
- MCP Inspector: the 46-tool source surface compiles and is covered by registration tests, including the new two-phase logistics-station fleet transfer. The matching Plugin/Core/Contracts build is now live after a normal save/close/deploy/resume boundary. The current owned world runtime-verified explicit save, research-result acknowledgement, normal refuel, exact-proof quarantine reconciliation, planned exact-primary resume, both refinery-output sorter filters, journal durable-through readback, and legacy checkpoint retirement. Station fleet transfer remains pending first-tower validation, not deployment.
- The first normally built PLS exposed the native local-station `planetId == 0` sentinel. The identity-policy correction builds with 0 warnings / 0 errors and 101 tests pass (Contracts 11, Bridge.Core 73, MCP 17). After normal save tick `9462208`, the corrected Release set was deployed together and matched its outputs: Plugin `0086A97036B19C43178DBC8129EF53CE31E8F68E613178B068B1945B90BAA600`, Contracts `0A244DCA1C72D46079A2B05D5AE67C7A04EF3C52FF059971857080BF7414D579`, and Bridge.Core `058E3EFD37E5EA4E7D63487B19A788E207712D4D37C1205B8B935EF0E3B98BF3`. Exact-primary resume and live entity `916` inspection returned the complete normalized local-station DTO; configuration and fleet commits remain separate pending validations.
- Historical sandbox basic line: six entities built and reread; the installed DLL converted 6 iron ore into 6 iron ingots under wind power, following an earlier 20-to-20 run. This is not evidence for revised M0 Gates B-D.
- Revised ordinary mode: live verification created a peaceful 1x non-sandbox owned world and exercised structured research plus normal drone-built power, copper/iron/coal/oil extraction, logistics, smelting, electromagnetic-matrix research, graphite, steel, oil refining, co-product separation, and energy-matrix production. The earlier co-located-sorter attribution failure was repaired and live-validated after strict same-world recovery; lab `256` then produced energy matrices `0 -> 3 -> 6`, and the exact owned world was saved normally. The same live lab later accumulated 10 before its output buffer filled.

## Takeover-machine audit

The current checkout was re-audited on 2026-08-30 and the live milestone evidence was refreshed on 2026-08-31:

- The system-wide `dotnet` installation contained only runtime 8.0.7 and no SDK. SDK 8.0.424 was therefore installed under ignored `.local/dotnet` with Microsoft's official `dotnet-install.ps1`; no system directory or global `PATH` was changed.
- The user NuGet configuration contained no package sources. Locked restore succeeded by supplying the official `https://api.nuget.org/v3/index.json` source on the command line; the user configuration and repository lock files were not changed.
- The .NET SDK's environment-dependent automatic `Microsoft.NETFramework.ReferenceAssemblies` umbrella reference conflicted with the Plugin's locked, explicit `.net472` package. The Plugin project now disables that implicit injection, so full-solution locked restore is reproducible without changing `packages.lock.json`.
- Steam auto-detection initially returned the same DSP root twice with different path casing. `locate-dsp.ps1` now de-duplicates validated roots with Windows case-insensitive path semantics.
- Steam build ID remains `23109513`; `Assembly-CSharp.dll` remains SHA-256 `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85`, matching the previously researched current-version API baseline.
- After explicit user approval, official `xiaoye97-BepInEx-5.4.17` was installed and its `BepInEx.dll` hash matched the recorded baseline. The development Plugin loaded successfully and created exactly one user-authorized validation world.
- The full solution builds with 0 warnings and 0 errors. The current suite passes 206 tests: Contracts 17, Bridge.Core 168, MCP 21.
- The installed Plugin's earlier positional build readback selected an older sorter when two refinery outputs shared an exact source pose. Source now snapshots and excludes pre-existing co-located sorter IDs; the `211`/`213` regression is covered offline, and the corrected DLL was later deployed through the strict recovery path and live-validated with distinct co-located outputs `164` and `181` in the current healthy session.
- Source includes a pure movement-progress watchdog and player-order single-flight. Five Core tests cover the original watchdog baseline; the implementation was subsequently deployed through protected save/restart cycles and remains part of the current live Plugin.
