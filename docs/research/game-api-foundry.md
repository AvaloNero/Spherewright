# v0.4 Foundry DSP API evidence

## Baseline and boundary

- DSP `0.10.34.28529`, BepInEx `5.4.17.0`; inspected 2026-09-05.
- `Assembly-CSharp.dll` SHA-256: `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85`.
- Read-only ILSpy inspection of the local assembly; no decompiled code or game binaries are redistributed.
- Plugin reads occur only on the Unity main thread after the existing exact-owned-session/local-planet check. Core receives copied DTOs, not game objects.

## Bounded blueprint data and native upgrades (2026-09-06)

### Detail-only native belt cargo observation (2026-09-07; source/offline, not installed)

The current Assembly-CSharp SHA-256 was reread and remains `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85`. Current ILSpy evidence confirms `BeltComponent.id/entityId/segPathId/segIndex/segLength`, `CargoTraffic.beltPool/beltCursor/pathPool/pathCursor`, `CargoPath.id/buffer/pathLength/closed/cargoContainer` and `CargoContainer.cursor/cargoPool`. The native `GetCargoPath(int)` bounds condition uses **and**, so the adapter explicitly validates the positive path ID against both cursor and array length before calling it. The exact entity/component/path/container identity must match the selected owned local factory.

`CargoPath.GetCargoAtIndex(int,out Cargo,out int,out int)` is a read-only native lookup under `lock(buffer)`; it finds the ten-cell frame's250 sign, decodes four base100 digits stored plus one, then indexes the container. Its native guard bytes are246/247/248/249/250, four digits,255. Native `CargoContainer.AddCargo` allocates `cursor++` starting at **zero**, unlike entity/component pools; CargoId0 is valid. The returned `Cargo.item` is a short and `stack/inc` are bytes. These observations, not assumptions about ordinary one-item belts, determine the new decoder and stack-count/Inc semantics. No insertion/pick/remove/clear function is called.

Only exact entity detail requests capture this optional field. The adapter copies at most512 selected cells plus9 guards on each side (530 bytes) under the native buffer lock on the Unity main thread. Core validates every touching frame and all digits, deduplicates references within the segment, and rejects conflicting IDs or missing/malformed frames. Guard framing also prevents a corrupt zero at the only selected cell from masquerading as an empty segment. Each distinct copied reference is bounds-checked and confirmed with the native GetCargoAtIndex before item/stack/inc scalars are summarized; no DSP pool/reference escapes into DTOs. Incomplete seam fragments remain unavailable, not silently omitted. Totals stay null and items empty on any failure. Neighbour segments can observe the same crossing stack, so totals are not additive; a sample does not establish flow, an empty whole path or cargo conservation across upgrades.

Pagination, device buffers and all existing action/endpoint/configuration hash encodings are unchanged. This adds no write, autonomous tracing or public MCP method; detail/read/list descriptions and the embedded playbook disclose the distinction. The main-thread work is bounded, but live latency, moving/empty/stacked cargo and ordinary save/resume still require same-cohort installation and local validation. The running767 installation does not have this field. Whole-path/chunk/geometry/cargo proof for native belt upgrades is separate and remains pending.

Offline validation:70 new Core cases (including1000 deterministic valid local-window comparisons),2 contract cases and updated MCP/playbook coverage checks. Debug/Release879 tests passed (23Contracts/819Core/37MCP); complete current-DSP Release build0 warnings/errors. Real source stdio MCP metadata returned0.4.0.0,64tools/1resource,26107-character exact embedded guide, exit0 and no extra stdout. This is not installed Plugin/local-live or cross-computer validation, and no ZIP was generated for it.

Committed source `a7eb939` independently passed locked restore, the full Release build and879 tests from the exact staged snapshot, excluding local packaging/CI changes. Its pushed Windows Core CI run `34044633226` also succeeded. This does not change the live767 installation or the pending native cargo observation checks.

### Governor explicit parallel-construction comparison (2026-09-06; source/offline, not installed)

Observation coverage audit (IFX-039): `TryCaptureFactoryEntity` invokes the separate assembler/lab/miner/storage/station/tank/inserter readers, but no belt cargo reader. Empty belt buffers therefore do not establish an empty `CargoPath`. `CaptureLab` uses actual lab recipe/data and keeps research matrix points separate; absence in `list_assemblers` cannot prove no matrix consumer. MCP descriptions and the embedded guide now disclose these existing native-read boundaries, without introducing a new gameplay call.

Read-only preliminary belt-upgrade research, **not exposed as support**: the same DLL's `PlayerAction_Build.DoUpgradeObject` uses `ObjectAssetValue` (one for completed positive entities), consumes the new device and refunds the old device via `UpgradeFinally`/`UpgradeEntityWithComponents`. The latter calls `CargoTraffic.UpgradeBeltComponent`, updating the belt speed and native path chunks, including a tail-segment/closed-path branch. `CargoPath.InsertChunk` rewrites/arranges chunk intervals; current factory `buffers` cannot prove cargo preservation around this operation. Before enabling belt upgrades, separately bound and verify the full affected cargo/path/chunk evidence, target identity and reciprocal connections. No belt upgrade was prepared, committed or claimed live here; ordinary sorter/assembler support remains unchanged.

The optional `parallelExpansionBlueprint` adapter reuses `GetFoundryPlanOnMainThread`, not a new DSP API. That existing main-thread path supplies bounded native blueprint parsing/site inspection and current power evidence. Core compiles only the positive increment from a measured ready three-window baseline, verifies same session/planet/tick/revision plus exact code/site/player assessment and the unchanged full Foundry graph, and exposes all explicit module-object costs with external-infrastructure exclusions. The returned intent and construction hash use the existing finite build protocol; there is no parallel approval store, executable read or actual-throughput credit. Cold deployment of this new comparison and its local-live positive example remain pending. The separately installed767 cohort below does not contain it.

Thirty-nine new Core regressions cover measured delta versus instantaneous starvation/full target, warm/zero/unstable baseline, all seven synthetic module objects, native/power/transport/material blockers, changed capture/intent/code/position/orientation/graph, bounded malformed requests and fingerprint scope. They reuse the existing finite `BlueprintBuildState` binding; these synthetic DTO tests do not simulate native DSP execution. Release806 tests (21Contracts/749Core/36MCP) and complete Release0 warnings/errors passed; the MCP contract and packaged playbook explain discovery, cost scope and fresh prepare/observation boundaries.

### Native transport budget (2026-09-06; local readback, successful construction pending)

The767 installation resumed the same owned world via action5381b28d, succeeded and normally resaved23992240. Fresh reciprocal links on repaired115/719/720 and the source recipe/storage settings survived; the one-action audit at24112905–24113594 was healthy, Journal55/55 durable,2267 built/0 prebuild and no checkpoint. Four explicit five-object diamond-module previews actually reported two30/min transport allocations, rated45/min (span2,80ticks) and30/min (span3,120ticks). All four still failed readiness: occupancy, missing power coverage, or native BlueprintAreaCrossTropic. Network2's30000/tick capacity versus45600 existing reserved plus6600 added demand leaves22200/tick deficit despite instantaneous consumerRatio1. Top-level nativeCheckPerformed/nativeCheckPassed remained false; individual translation Ok does not certify whole native placement. There was no blueprint commit, module throughput proof or in-progress construction restart in this window.

767 cold deployment from source30f125d: after ordinary save23992209, complete one-write audit and normal exit of process22120, process/descriptor counts were0. All four newly installed files matched the tested Release output: Plugin `B3C556ECBBE4FB745D8CFC5027C68BA7131ED15DF0BBF2511C43D9B4EFC33536`, Contracts `D67C74A4DFC36904F014943ACD73D411F6575E37697912F48C5A5B6395C04EAE`, Core `5BEFB9C0BB682A88C65A7423A20B1DD13173A4DF954238EEC06B1F76175348E8`, Newtonsoft unchanged. Actual source MCP reports64 tools/1resource and23382 matching playbook characters, with normal stdin-close exit0 and no extra stdout. This supersedes the installed736 statement below only for deployment, not native module/throughput acceptance. No ZIP or release was produced; protected resume is the next live step.

The same current DLL proves `PrefabDesc.beltSpeed`, `inserterSTT`, `inserterGrade`. `PlanetFactory` passes belt speed to `CargoTraffic.NewBeltComponent`; `CargoPath.kCargoLength=10`, and `CargoPath.Update()` advances each chunk by its speed cells per game tick. Rated single-item flow is therefore speed×60/10 cargo per second, with no stacked-item bonus. Capacity is bounded by the native120 cargo/s maximum. Startup/end effects, merges and downstream backpressure can reduce actual throughput; it is not a guaranteed rate.

For a native sorter prebuild, `PlanetFactory` clamps its distance parameter and constructs `stt=max(10000,prefab.inserterSTT×span)`; the accepted blueprint parameter subset remains one integer1–3. `FactorySystem.NewInserterComponent` gives grade1/2 stackInput=stackOutput=1, no bidirectionality and no stack delay. At full power, `InserterComponent.InternalUpdate` advances10000 time units per tick in Sending/Returning and also on successful Picking/Inserting. Sending keeps its remainder; Returning resets it. Each of the four stages runs at least once, yielding a single-item cycle `max(4,ceil(2×stt/10000))` game ticks. Item needs, source starvation, failed insertion or a power shortage can add time; grade3 stacks are not credited. Pure arithmetic tests independently simulate the four-phase quantization over bounded synthetic values; this is not live DSP execution.

Core now uses those capacities on node-split route edges, including input/output boundary belts, and hashes the native profile/span/allocated flow into the existing Foundry construction record. Missing/invalid metadata or advanced stacking is an explicit composition blocker; ordinary blueprint data/finite-build support is unchanged. Resume rechecks both original material and transport rules before remaining construction. `transportCapacityVerified` remainsfalse, and budget success never claims actual fair flow or continuous supply. Release767 tests (21Contracts/710Core/36MCP) and full Release0 warnings/errors passed for the source slice; the running Plugin remains the separately recorded736 cohort. Final merged ZIP and live rate/module proof remain outstanding.

### Blueprint-backed Foundry composition and ordinary sorter recovery (2026-09-06; source/offline)

Later736 local live audit: normal dismantle/rebuild actions cd0bd811/9a1ae940/5711428f/642a2238 fixed719/720 through the same native paths;719 returned its one held diamond as well as its building item. Both original IDs happened to be reused and were re-read, never assumed. Reciprocal715:0–719:1,719:0–717:7,716:3–720:1 and720:0–715:9, neighboring links, recipe60 and both storage settings were verified. At23819363 the selected blueprint preview reported sorter facing dots0.997947454/0.9982711 and0.9793151/0.984285235 with both nativeCondition=Ok, so the earlier ErrorInserterData no longer occurred. Five occupied preview objects still prevented the complete native check (nativeCheckPerformed=false); the independent power budget also failed. This is not a clear-site or blueprint-copy pass. Three actual600-tick windows23823818–23824417,23824491–23825090,23825160–23825759 had diamond0/min and output_blocked at715 (100/100); their73/69-tick gaps cannot count as continuous validation. Save304e46eb completed23827812; the five-write audit retained owned/healthy, Journal55/55,2267built/0prebuild and full power. Empty and single-item2011 recovery are now locally proved;2012 removal, nonzero-inc cargo and post-repair protected resume remain pending. Raw observation run7b8f026b ordinals3/5/7, not similarly named files from the earlier115 repair, supplies the three windows.

736 cold installation: normal-close process21000 after saved23725814/complete three-write audit; process and descriptor counts0. Installed4/4 matched Plugin `B3FED3B0E87AC5BEA11647F327E9743E1F157013B181A7F1D409B755FCDF1768`, Contracts `FB3FA5F4A4C9EA9E135EA888A12558B84A5C7F7C5CCE67D4927B3F965551B355`, Core `A83205BD6B059DD0D7325E538164149842C12254084CE5119FC67B57D634641D`, Newtonsoft unchanged. Debug/Release736 tests, full Release0 warnings/errors, actual source MCP64tools/1resource/22881-character matching guide and35 package-surface checks passed. Protected resume and sorter repair results are recorded only after actual observation; no hot replacement, final ZIP or publication. A subsequent source-only continuation guard also recompiles the original material intent against current catalogs before resume, refusing changed recipe/machine rules; it is not in this installed736 cohort yet.

Foundry now composes its existing runtime material graph with an explicitly supplied/native-exported finite blueprint and the existing native site assessment. No guessed DSP placement API or alternate executor:32 objects, exact single-output2302–2305 stage counts/recipes, default unbanned2101 storage, explicit free boundary ports, bounded per-item node-split max flow (machines cannot relay arbitrary goods), mixed-channel rejection, whole construction costs and per-object dependencies. Allocated flows are not measured throughput or verified belt/sorter bandwidth. Layout selection remains explicit; the earlier machine grid does not become routed automatically. Prepare/commit recompute intent/graph and power and bind them to the existing protected finite build. Metadata survives partial/cancel/save/restart while tokens do not. Resume budgets only the remainder against the current native network.42 Core composition/binding tests and one MCP discovery/mapping regression cover this source slice; live construction remains pending.

736 local live: protected resume `e85fa1d9-a16d-4c46-96ae-beaabecaa344`, empty2011 dismantle `53e71a7f-b27c-4a2a-9cee-05e97eb4402c` at23729227, ordinary drone rebuild `1b4b8246-6e52-4f7b-ab89-2b1e6e77f764` at23738747–23738879 and save `d4dc63eb-915c-48c9-9eae-4690b4262e71` at23753687 all completed/succeeded. Building inventory4→5→4, held cargo/inc0; fresh ID really115, both108:4↔115:1 and115:0↔113:10 present. Neighbor107/109/116 connections and recipe17 unchanged. Independent600-tick windows reported graphite18/12/12 per minute, so topology repair is not sustained balance. Four-write audit healthy/Journal55/55/no pending/error/2267built/0prebuild/three fully served networks, game remains running. First resume prepare timed out without commit. Empty2011 is the only dismantle live subset proved here;2012, nonempty cargo and post-repair protected restore remain unproved. The source-only material-rule continuation guard subsequently passed the same736 Release tests/full Release0 warnings/errors but was not hot-deployed.

The local blocker was narrower: after successful Move `74b3566b-e04c-4d78-952d-797c9291dba6` to coal308, `prepare_dismantle(115)` explicitly rejected because installed672 accepted resource miners only. No dismantle commit occurred. Current-DLL inspection confirms `PlayerAction_Build.DoDismantleObject(int)` is the normal one-object business entry, returns the building through `TryAddItemToPackage(...throwTrash:true)`, then invokes native teardown. `PlanetFactory.DismantleFinally` calls `TakeBackItemsInEntity`, whose sorter branch calls `FactorySystem.TakeBackItems_Inserter(Player,int)`. It returns exact itemCount/itemInc only when itemId>0 AND stackCount>0; a nonempty zero-stack component cannot be treated as refundable. Native grade1/2 construction uses stackInput/output1, canStack=false and bidirectional=false. The new subset accepts2011/2012 only, with live cargo sampled immediately before synchronous removal.

`RemoveEntityWithComponents` invokes neighbor disconnection, removes the component and clears object connections. `ClearObjectConn(int,int)` clears its referenced neighbor slot unconditionally. Missing ends may therefore be repaired by ordinary removal, but every present edge and inbound reference must first be reciprocal. Read-only scans are capped at8192 entity/prebuild pool entries and4 distinct present/cached neighbors; pending-prebuild references reject. After the ordinary call, exact disappearance, whole inventory delta, cargo inc and surviving endpoint pose/configuration/other slots must match. No direct DismantleFinally, RemoveEntityWithComponents, ClearObjectConn, field assignment, refund injection or automatic reconstruction is exposed.21 pure recovery/reciprocity/inc/survivor tests passed; the subsequent local live cases above supersede this implementation checkpoint's initially pending deployment.

Native teardown has a separate preferences.instantDismantle AND sandboxToolsEnabled branch that clears instead of returns cargo. Active instant dismantle is now rejected before removal, never toggled by Spherewright. This is an operation-specific native-path safeguard, not a blanket sandbox-world gate. Same DLL hash/version as above. Full Release build0 warnings/errors;736 Release tests (21 Contracts/679 Core/36 MCP) passed before the later guide rebuild. These are offline results, not a package or live completion.

### Full base-load power assessment (2026-09-06; offline and bounded local live readback)

Same current-DSP assembly baseline. `PowerSystem.OnNodeAdded(int)` builds each `PowerNetwork.Node` from the entity position divided by `entity.alt / (planet.realRadius + 0.2f)`; its `x/y/z`, `connDistance2` and `coverRadius2` are the authoritative copied geometry. It joins nodes when squared distance is at most the **maximum**, not sum, of the two connection radii squared. Existing native network membership already proves their internal connectivity; the helper does not arbitrarily merge old networks by rescanning proximity. A new node may join components and may also attach existing net0 consumers.

`OnConsumerAdded(int)` projects `PowerConsumerComponent.plugPos` by `plugAlt / (realRadius + 0.2f)`, searches native networks and connects covering nodes. `NewConsumerComponent(entityId,work,idle)` initially uses the entity position; stacked/add-on paths can change the plug position, so the finite ordinary unstacked subset does not extrapolate to those types. Visual `PowerNodeComponent.powerPoint` is used for rendered lines/charging, not this network-connection geometry. Core uses a planet-scaled margin of at least2mm and marks boundary evidence uncertain instead of accidentally crediting a missed network merge or newly energized load.

Native power tick sums consumers' `requiredEnergy` and charger-node demand; charging interpolates idle/work costs. The bounded read-only adapter therefore reserves max(work,idle,current) for existing consumers and chargers and separately includes network exports. It verifies native pools, membership and entity component identities, including net0 consumers, and reuses the existing generator/counter audit. Bounds:32 planned objects,512 active nodes,256 networks,8192 consumer scan,2048 unassigned consumers and4096 generator-list entries; unknown/over-limit evidence yields unavailable, not zero cost. Planned consumers/chargers use native prefab work/idle maxima, including sorters. This is an unproliferated base-load budget, not a guarantee about all future accelerating recipes.

`PowerGeneratorComponent.EnergyCap_Wind(float)` casts `(windStrength * (float)genEnergyPerTick)` to long. The catalog copies only that bounded prediction from the current local planet; the mutating component method is never called by the reader. Other new generation receives no speculative credit, and existing capacity is only the current native snapshot, not sustained fuel proof. `site.power` is advisory and independently hashed by capture; it does not change native conditions or add a per-tick volatile value to the construction token's assessment. Thirty-nine power-policy tests passed in the662-test Release suite; two later tests explicitly guard this hash separation. Full current-DLL Release compilation passed with0 warnings/errors; deployment/live results follow separately.

### Fuel-readiness catalog (2026-09-06; offline and local live readback)

Fuel-readiness observation extension: current `ItemProto` declares `long HeatValue`, `int FuelType`; the existing normal-refuel prepare already requires both positive plus `StorageComponent.itemIsFuel[itemId]`. The recipe catalog now exposes these values and nullable acceptance from that exact shared predicate. A missing/unindexed native fuel table is unknown; the write adapter keeps the same three checks with an added null guard. No guessed item list, fuel grants, field writes or burn-rate assumptions are introduced. This addresses an external validation agent looking only for named fuel rods while a nearby storage contains hydrogen/refined oil. Eight pure eligibility regressions cover every original negative predicate and positive non-name-based cases; live catalog values must be checked after cold deployment.

### 672-test cold deployment and readback

Normal save23604289 and the complete nine-write audit preceded normal window close; process39344 exited and both process/descriptor counts reached0 before installation. All4 installed DLLs matched the672-test Release cohort: Plugin `6AD5C8B1F0BBBBBFF27897B620C93FC6B0428C9F4FCB06A23B4506D427FED72E`, Contracts `8CAA2875B4E126A2D1B269000C22788DABA87DAAC0F70B4F053228148E7CA7E0`, Core `1542EBEC71988B7D2DCD609D7CF4DC9026889CBA1213928554E515A2921829DB`, Newtonsoft `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Protected resume action `accbb81f-3027-47bb-8506-7802dcd87c4e` completed and normally re-saved23604320; same owned104/peaceful/non-sandbox/1×, revision1, healthy, Journal55/55 durable without pending/error. This is the first accepted write of a new audit window, not another nine-write reset.

Fresh catalog readback confirms refined oil1114:4500000J/FuelType1 and hydrogen1120:9000000J/FuelType1, both acceptedAsMechaFuel=true. These are eligibility/heat values, not energy-per-second or flight-range guarantees. Native build profiles distinguish2201 ordinary node from2202 charger (work80000/idle1500 per tick),2203 wind5000 per tick,2011 consumer300/150 and2302 consumer6000/200.

The explicit one-smelter diamond1112@30/min site at the player's current position passed native machine clearance/whole-machine inventory, but the independent power assessment correctly returned `power_plan_incomplete`, `planned_consumers_without_power_coverage` and360000W new base load. The player remained Walk/0 at tick23612476 with274.193MJ and no fuel. This validates catalog publication and the uncovered-load branch only, not full network merges, a powered copied module, continuous fuel supply or executable Foundry completion. Debug and Release each passed672 tests (21 Contracts/616 Core/35 MCP); complete Release0 warnings/errors, real MCP64tools/1resource with20838-character matching guide, and29 package-surface policy checks passed. No final merged ZIP, new commit or publication is implied.

### 617-test observer deployment and finding-scope correction

Normal save23398622 and the complete two-write audit preceded a normal close of DSP; process/descriptor counts reached0 before replacement. Installed files matched4/4: Plugin `307150613641D7A4A50246BE9900B39D6679579D292A12DDF702C40FDC8AF582`, Contracts `B1FAFF5DEC4A75423E0752CE093633817C91F1E613944C5190F10AAA627DEA87`, Core `19232604B91F259D4F72501A1671563041C32ABE209C9E2A86E633D0BBD427CC`, Newtonsoft unchanged. Protected resume `08eeb797-fe98-4add-b9b0-673a0901fa2a` completed and normally re-saved23398654; same owned104/peaceful/non-sandbox/healthy/Journal55/55.

Three independent windows23406387–23406986,23407062–23407661,23407732–23408331 all measured diamond30/min and retained static source identity. The first observer lock returned `governor_validation_baseline_unavailable`: three depleted-miner findings14/263/796 had no item attribution or selected-source path, but the installed code treated every global finding as a target-line failure. Source now retains all findings while separately scoping target-diagnostic/selected-root/selected-path findings;6 new regressions and623 Release tests passed. This does not declare those mines irrelevant or repaired. Actual graphite production was deficient: source115 retained a cached target113 and coal but lacked its factory output link. Repair is a separate normal-action task. Buffer observation23407661→23408331 spans670 ticks between captures; it is not the native600-tick production window23407732–23408331. No baseline was locked and no blueprint was committed in that attempt.

Research repeated against DSP `0.10.34.28529` / BepInEx `5.4.17.0`; compile reference SHA-256 remains `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85`. Local decompilation is not redistributed.

- `BlueprintData.HeaderFromBase64String(string)` parses metadata; `CheckSignature(string)` uses **MD5F**, not an assumed standard MD5. `ContentFromBase64String` performs an unbounded GZip copy, and `Import(BinaryReader)` permits up to 1048576 buildings before allocations/references. Never pass external code through this unrestricted entry.
- `BoundedBlueprintReader` first enforces code/compressed/decompressed budgets, version2/patch1, 64 objects/8 areas, finite bounded coordinates, acyclic area parents, valid object indices/ports and supported settings. It rejects content-bearing records **before** native `ReadString`, and rejects reform/unknown versions/types instead of skipping bytes. Current building record marker is -102. Native import is invoked only on these bounded bytes; exact native Export roundtrip and native signature must then match. Synthetic unit fixtures prove structural rejection, not a valid DSP signature or live construction.
- `BlueprintUtils.GenerateBlueprintData(BlueprintData, PlanetData, PlanetAuxData, int[], int, float, uint[], bool)` is the proven native copy path. The adapter passes only fresh explicitly selected completed positive entities, a cluster at most64m from the first, no reform IDs and `hasReform=false`. The prebuild branch's `PostRefreshPrebuildDisplay` is deliberately excluded. `BuildingParameters.CopyFromFactoryObject`/`ToParamsArray` preserve assembler recipe/acceleration, sorter filter/span and supported native connection fields. Native `ToBase64String()` exports only newly allocated blueprint data. No UI input or file operation is called.
- Native blueprint code deliberately lacks connections to unselected entities. The API separately returns each observed source boundary; it cannot silently claim that external links, power or supplies were copied. Export contains no live cargo. The initial subset excludes stations, storage configurations, miners, labs, labels, foundation/reform and advanced types; later support requires separate parameter/placement proof.
- `BuildTool_BlueprintPaste.CheckBuildConditions` and `CreatePrebuilds` are distinct native placement/construction stages. They are researched entry points, **not yet called or exposed** by this data slice. Native site/yaw translation, whole-budget validation, bounded execution receipts, cancel and durable per-object restart reconciliation remain implementation gates.
- `BuildTool_Upgrade.UpgradeAction()` calls `PlayerAction_Build.DoUpgradeObject(int objId, int grade, int upgrade, out int error)`, then `SetPlanetPhysicsColliderDirty()`. For a completed entity `ObjectAssetValue` is1: the method consumes a complete target item (hand or package), calls the native internal upgrade, then returns the previous item through `TryAddItemToPackage(...throwTrash:true)`. The adapter therefore independently prechecks unlock, exact native family/grade, one target item and one empty refund slot; it never calls internal `UpgradeFinally` or writes `protoId`.
- `ItemProto.GetGradeItem` can select a grade without a complete technology check; `GetUpgradeItem(history,...)` only has a few special locked-tier fallbacks. Neither is sufficient authorization alone. The first executable subset is manufacturing assemblers2303–2305, higher grades only, with normal local build range, settled player and no active accepted action. Plan binding reuses the existing owned-session/plan/idempotency machinery and adds revision plus stable recipe/acceleration/endpoint evidence.
- `PlanetFactory.UpgradeEntityWithComponents` changes assembler speed/power and resets `time/extraTime` to0 while retaining recipe, arrays, modes and connections. The adapter snapshots live arrays/scalars immediately around the synchronous native call, verifies target speed/progress, exact inventory debit/refund, unique pose-based result identity and reciprocal links. It does not rely on entity ID stability even though this DLL currently retains it. An exception or mismatched proof becomes the existing outcome-unknown quarantine, never an automatic replay/rollback.
- Native sorter upgrade computes span as old `stt / oldPrefab.inserterSTT`, rescales `time` by its previous cycle fraction with +0.5 integer rounding, and changes grade/stack mode. The second slice supports **2011→2012 only**: span1–3, grade1→2, stackInput/output1, bidirectionalfalse, delay0. Before/after proof also covers raw held item/count/inc/stack, stage, careNeeds, both target/typed IDs, filter, offsets, requireEnergy and `InserterPose` second-end pose/t1/t2. Power consumer idle/work costs must match the destination prefab. Old higher-grade IDs are excluded from result attribution because overlapping sorter centres cannot alone establish ownership. Advanced grade3/4 stacking and belts remain unsupported; `CargoTraffic.UpgradeBeltComponent` edits path speed chunks/renderers and requires separate cargo proof.
- A validation-side interpretation error exposed an older DTO ambiguity: `LabComponent.matrixServed` is in **3600 points per matrix**, not item count (`PlanetFactory` native insert adds `3600*itemCount`; lab auto-serve threshold36000 means ten matrix equivalents). `FactoryBufferSnapshot` now preserves raw `count/inc` but labels research buffers `countUnit=research_matrix_points`, `unitsPerItem=3600`; ordinary buffers default to items/1. Do not infer sufficient research inventory from raw counts or round partial points up to whole items.

Initial offline checkpoint: locked restore, Debug and Release Core/Contracts/MCP tests **366 passed** (20/316/30), complete Release build0 warnings/errors. After normal save19983210 and process exit, all four installed DLLs matched this build: Plugin `7B7C00A9987A6FCA70424C85BB3A71D1DA3872CBEE75CA53CD2C8B7CB8C0F09F`, Contracts `9D463FD60A96BB53C4AB7ACC8A55B3A6BF80074B58ED21BD2464D9CB378724EE`, Core `A34354295802BD625028E3F3C2BE6BCE654E0F2C4E94E0ECBF4AE4F23B42B281`, Newtonsoft `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Source MCP discovery tests expect58 tools/1 resource. Local live results are recorded separately below when actually observed; no cross-computer, module-construction, upgrade-success or Governor2× acceptance is implied by build/deployment.

Local live data checkpoint: protected resume action `5c66b788-9fb2-4cfe-9a22-fb7cdf5d0202` completed, same owned planet104 and normal re-save19983241. Fresh native export of724+741/744/749/742 returned5 objects/1 area, nativeSignatureVerifiedtrue/executablefalse; inspecting the exported code retained its hash, recipe97 and filter1101, with4 source boundary edges disclosed separately. Costs: assembler1 missing1, basic sorters4/package8/missing0. Altered signature, duplicate selection and stale endpoint were rejected without writes. Source MCP really advertised58 tools/1 readable playbook and exited0. Live lab84/679 research buffers expose `research_matrix_points`/3600, while production labs76/256 expose items/1. Manufacturing upgrade724→2304 prepare returned TECHNOLOGY_LOCKED without commit; no successful assembler upgrade is implied. The basic-sorter extension subsequently passes379 Debug/Release tests (20/328/31) and full Release0 warnings/errors; its newer deployment and successful upgrade are tracked separately.

Native-paste continuation caution: `BuildTool_BlueprintPaste.CreatePrebuilds()` is not an atomic checked transaction. It admits both Ok and NotEnoughItem previews, can create a prebuild with `itemRequired=1`, and includes cover-object upgrade/configuration/reconstruction paths. Its GPU-preview eligibility guard also cannot be casually replaced with guessed flags. A future bounded executor must independently reject cover mutation and missing budgets, bind every external endpoint and record object receipts; this method's existence alone does not justify exposing unrestricted paste. This was read-only DLL research, not an executed paste or observed gameplay failure.

Further native placement research: `BlueprintUtils.InitBuildPreviewByBPData(BlueprintData, ref BuildPreview[], int)` creates/reset bounded preview objects and copies parameters/links; its positive `bpgpuiModelId` values are assigned by that native initializer, not proof that a site passed checks. `RecalculateCursorPos(Vector3,float,BlueprintData,int,int)` and `RefreshBuildPreview(PlanetData,BlueprintData,BuildPreview[],ref byte[],bool,Vector3[],int,IntVector4[],float,bool,int)` implement the native quarter-turn/tropic translation. Refresh can adjust sorter ends to connected slot poses, so an internal link must have a valid native target slot before that call, and translated geometry must be read back rather than approximated as a Cartesian copy. Its reform branch invokes platform-data preparation; a no-reform subset must not enter that branch. `CheckBuildConditionsPrestage` also independently requires `history.blueprintLimit >= buildings.Length`, reform allowance, a non-gas planet and valid tropic conditions; unlocked individual building recipes do not alone prove blueprint placement technology. The paste tool's `_OnInit` and preview-prestage paths create UI/GPUI resources and subscriptions, so isolated preview ownership/cleanup must be proved before reusing them. These are researched future adapter constraints only; none of these placement calls is exposed or called by the current data tools.

Basic-sorter deployment checkpoint: after strict ten-write audit, normal save action `14debecf-81f4-461e-b09c-81057307cbb1` succeeded at22139771 with Journal54/54; CloseMainWindow exited the bound process and live descriptor count became0. Added public `upgradeReadback` retains immediate before/after buffers and timing so a later poll cannot disguise whether cargo was actually present. Full Release0 warnings/errors and393 tests (21/341/31) pass. That deployment's hashes matched4/4: Plugin `93CE2A218E1C8B281D75137A00BB3DD43EEAA7B60DE7B37DB9B3404A3FFE22EF`, Contracts `CBCEAEC057D59AD25689FC04E522E71C3C16B5366EBC03428EF655CC2923FF0B`, Core `B98036B23463EA327F1A3372222FA0DFFA5B1B5C4D7F4B22E1DB19FC514BA3D8`, Newtonsoft `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. An earlier install in the same closed-process window was superseded before any startup. Actual results and the subsequent connection-guard correction follow; this cohort is not the final build.

Native connection correction (IFX-028): `PlanetFactory.ReadObjectConn` reads the factory connection pool, independently of `InserterComponent.pickTarget/insertTarget`. `WriteObjectConn` clears the occupied source/destination slots and writes both reciprocal records; `ClearObjectConn` clears those records without itself rewriting the cached inserter targets. Native blueprint export reads sorter slot1 as input and slot0 as output. Therefore positive target IDs and a held item cannot establish both factory edges. The upgrade gate now requires exactly those two links matching pick/insert, then verifies both reciprocal endpoints, at prepare, commit revalidation and immediate readback. Eleven new Core regressions cover missing/duplicate/extra/wrong-direction/invalid endpoints; total404 Debug/Release tests and full Release0 warnings/errors pass.

The393-test cohort did run: resume `d8eaab78-3a8a-4839-be0a-646d051320d9` re-saved22139802, Journal54/54; wrong-filter prepare rejected STALE_STATE with no action. Upgrade `ef9e6118-8f33-496a-93af-493b684ef4de` succeeded synchronously at22170196 (27→27,2011→2012,2012 2→1/2011 6→7, held copper1/inc0 preserved,stt600000→300000). Same-key replay returned the same action and no second debit. However its verifiedConnectionCount was1:27:1↔10:1 existed, while cached insert26 had no reciprocal factory edge. This is evidence of original-state preservation, not complete topology acceptance. It triggered IFX-028 instead of being labelled fully passed. Fresh same-cohort blueprint5-object roundtrip and source MCP58 tools/1 resource/expectedFilterItemId discovery also passed. Normal save `2966ac62-286a-4860-84a5-cf8288ec9a15` at22192567 retained healthy writes and Journal54/54; normal close removed process/descriptor. No line was repaired or save rolled back.

Final corrected cohort: locked restore,404 Debug/Release tests (21 Contracts/352 Core/31 MCP), complete Release0 warnings/errors. Normal closed-game install verified4/4 hashes: Plugin `3FB233FBD3F9B26F0E483E75B2FAF305ADBF3D695A1B5DF1B39007E268F9B01D`, Contracts `CBCEAEC057D59AD25689FC04E522E71C3C16B5366EBC03428EF655CC2923FF0B`, Core `7199C2C00A2FB17F8ECEA3271D16B51DEAF7841C3E10F19CCCC6A461627EFDF5`, Newtonsoft `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Resume `62bec45b-556e-4bc2-b6fb-6b99c8aaaa22` re-saved22192599; a fresh one-ended sorter23 preparation rejected BUILD_CONNECTION_INVALID with no action. Upgrade749 `8f7af8d5-e3f3-44ea-bac1-2924b5485676` succeeded at22207214:2011 7→8/2012 1→0,filter1101,two reciprocal factory links,stt600000→300000. Its immediate before/after cargo was empty; do not combine it with the earlier27 copper sample into one fictional action. Normal save22213308→CloseMainWindow→resume `61e28558-e8e5-45e7-b766-278bb6eaa23f` / re-save22213339 retained both749 links,filter and full power. The upgraded5-object export/inspect roundtrip then passed with1×2303/3×2011/1×2012, correct model/filter, identical new-code hash,4 source boundaries and explicit missing2012/2303 materials. Actual source MCP58 tools/1 resource exposed expectedFilterItemId and the new two-required-links rule. Final save `de8963be-58f4-47a7-aba2-12b24701ace1` at22221235, fresh22224301–22224335, remained owned/healthy,Journal54/54,all three networks fully served. No release package, cross-computer test, blueprint construction or Governor2× result is claimed.

## Native site and finite execution follow-up (2026-09-06; not yet live validated)

The earlier data-only paragraphs above describe that historical cohort. The follow-up now uses the researched native geometry methods for site preview, but never exposes unrestricted `BuildTool_BlueprintPaste.CreatePrebuilds`.

- Further DLL inspection found that even `BuildTool_BlueprintPaste.CheckBuildConditions` can temporarily write/clear connection slots on covered belts. Therefore preview safety requires an independent **no cover** guard before the call, not merely checking its return value afterward. The adapter rejects occupied entities/prebuilds, overlapping planned colliders, external/open sorter matching and any reconstruction/cover results. Native `NeedConn` is not treated as full success. See IFX-029.
- An isolated paste-tool subclass owns only the bounded arrays/private reform-id set required for no-reform geometry; it does not run native UI-anchor/GPUI subscription initialization or enter the reform branch. Native quarter-turn/tropic translation and slot-adjusted poses are captured, then hashed with recipe/filter/parameters/offsets, dependency graph, whole package budget and identity. Native `history.blueprintLimit` and item/recipe/filter unlocks are separately required.
- `BuildTool_Click`, `BuildTool_Path`, `BuildTool_Inserter` already provide the proven normal per-object `CheckBuildConditions`/`CreatePrebuilds` path. An isolated real-inventory snapshot is checked, all cover flags and pose/config hashes are verified unchanged, then the ordinary create owns debit, prebuild, links and drone assignment. No direct `AddPrebuildData`, `BuildFinally`, recipe injection or `protoId` mutation is introduced. Native path creation normalizes belt rotations using `Maths.SphericalRotation`; inserter parameters determine `stt` from prefab timing and span, and are verified after construction.
- Ordinary native checks can update build cursor text/state, `UICursor.SetCursor` and inserter tip `gridLen`. The adapter reads/restores that small UI preview state around the isolated check; it sends no input and never writes player position. Tool/snapshot cleanup runs even on exceptions. Constructor/init cleanup is also bounded.
- Per-plan32/per-world32/4MiB store limits and one submission per game tick bound work. One pending prebuild at a time allows dependency completion proof; after7200 pending ticks the action pauses instead of resubmitting. Whole-budget preflight plus immediate exact full-inventory delta surrounds each native create. Atomic current-user-protected write-ahead `submitting` evidence precedes native calls; unknown/crash-gap records fail closed. Completed IDs/configuration/all reciprocal slots and expected-free ends are rechecked before every continuation; native reused prebuild IDs are not assumed globally unique.
- Exact build reads persist newly observed completion metadata before returning a new progress hash; list reads are compact summaries, avoiding a32-plan full-world scan/oversized frame. Restart requires current same-owned world at least as new as its durable watermark and a fresh prepare. Cancel stops unsubmitted work only. Manual changes are never silently repaired or made authoritative by a saved plan.

Current offline checkpoint: complete Release build0 warnings/errors and458 tests (21 Contracts/404 Core/33 MCP). This proves bounds/transitions/discovery forwarding, not real native placement, material consumption, cancellation or resumed construction. The installed game is still on the previously documented404-test cohort until a normal closed-game deployment is recorded.

Cold deployment follow-up: after normal save22943478 and verified process/descriptor exit, the458-test cohort installed4/4 matching DLLs: Plugin `23B0CC8C4ABEB4D12BED5A9A09A73BF3501ACDD473FA8624768FFFBBC412C772`, Contracts `70FA38B609638619AE8D180AA0F15FEBA197E75C65C6926220AD7D3AA5B33C1B`, Core `A94C69AA2C5A6EBF9C8C036305FBDD02CF11C859E4DF8E1FB33265B64D8E6990`, Newtonsoft unchanged `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. This is a development install, not a release package.

Local live first read: accepted resume `80d4ccea-35a4-4876-82c2-1a53f45a7f4a` terminal/completed/succeeded, automatic primary save22943509; its action tick fields were omitted. Fresh22954916–22954938 proved owned/healthy,revision1,planet104,Journal54/54 durable,Walk/0,400MJ and three idle construction drones. Exact-primary resumed the last normal save, not the later unsaved read-only observation tick. Source Release MCP actually advertised63 tools/1 resource, read the playbook and completed normal status/session/resource reads. An early not-ready prepare was rejected without an action.

The explicit source selection20/21/795/840/832 exported five objects and eight source boundary connections, none working at that observation. A site assessment at the selected21 position returned native blueprint capacity0, technology=false, inventory=false (missing one2012 and one2302), occupied/range/internal-overlap blockers and object2 `ErrorInserterData`. `nativeCheckPerformed=false`/`nativeCheckPassed=false`: this was a negative preview, **not** a terrain pass, a running module or construction acceptance. No blueprint build was committed.

### Inserter collider geometry correction and Governor proposal (source-only follow-up)

Current DLL remains DSP0.10.34.28529 / SHA-256 `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85`. Targeted `BuildTool_BlueprintPaste.CheckBuildConditions` and `BlueprintUtils.RefreshBuildPreview` inspection distinguishes two conditions: the adapter's internal collider overlap and native `ErrorInserterData` caused by invalid adjusted slot geometry/facing. Clearing one must never override the other.

The native inserter half-length is `max(0.1, span/2 + prefab.ext.z - 0.5 + 0.35*inputIsBelt + 0.35*outputIsBelt)`; the local center shifts `-0.35` for an input belt and `+0.35` for an output belt. The frame uses the average of the two endpoint rotation-up vectors, with the prefab collider quaternion applied. The initial adapter had overextended this volume with an arbitrary `+1` and discarded the prefab rotation. It now follows that exact native formula via `NativeInserterColliderGeometry`; existing-object exclusion and native errors remain fail-closed. Nine formula/bounds regressions pass. The first live overlap is a trigger for this review, not proof this change cures native object2's separate error; deployment and a positive site remain pending (IFX-030).

Governor adds no new DSP write path or guessed field: it reuses existing same-tick recipe/build/entity capture and `TryCaptureOverseerDiagnosticBundle`, passing copied public DTOs to `GovernorPlanCompiler`/`GovernorMeasurementSeries`. Source-bound independent windows exclude pre-binding history; reset on restart/source/scale change or gaps. The adapter checks actual reciprocal factory links and re-fingerprints resulting blockers. Its three cost/nameplate alternatives, separately scoped stock deltas and local surplus remain proposals, not a persistent ten-minute validator or write permission. Current source complete Release build has0 warnings/errors and487 tests (21 Contracts/432 Core/34 MCP). Registration tests expect64 tools, while the running458-test Plugin is still the earlier63-tool cohort. No Governor local-live or new release-package result is implied.

## Machine capacity and power

Current public fields:

```text
PrefabDesc.assemblerSpeed       int
PrefabDesc.labAssembleSpeed     int
PrefabDesc.workEnergyPerTick    long
RecipeProto.TimeSpend          int
RecipeProto.Type               ERecipeType
RecipeProto.Items/ItemCounts/Results/ResultCounts   int[]
```

`PrefabDesc` converts assembler `speedf` and lab `assembleSpeed` to integer units by multiplying by `10000`. `PlanetFactory` passes these exact values to `FactorySystem.NewAssemblerComponent(entityId, desc.assemblerSpeed)` and `NewLabComponent(entityId, desc.labAssembleSpeed)`. Recipe execute data uses `TimeSpend * 10000` for its ordinary cycle duration. At 60 ticks/second the unproliferated full-power capacity is therefore:

```text
items/game minute = 3600 * prefabSpeedRaw * outputCount / (TimeSpend * 10000)
working watts     = workEnergyPerTick * 60
```

The build catalog now exposes nullable `productionSpeedRaw` and `workEnergyPerTick`. Unknown or nonpositive speed is not replaced with an invented 1× value. Assembler/smelter/refinery roles must match the runtime recipe type; matrix labs accept `Research` recipes for matrix production, not arbitrary research unlocks. Other production mechanisms, including fractionators and mining, are not simulated by this compiler.

This is a **base-capacity budget**, not measured throughput. It does not credit proliferator bonuses, sorter stack upgrades, spare electricity, input buffers, or existing miners. Actual component rates and transport must still be verified with [Overseer](./game-api-overseer.md).

### Native research priority for construction prerequisites

Same inspected DSP DLL/hash as above. `GameHistoryData.SortTechQueue(int[] sortOrder)` replaces only the queue array using an index permutation; if research was active, it invokes private `AlterCurrentTech(queue[0])`. `UIResearchQueue.UpdateDragging` calls this method when a valid queue drag ends. Its UI dependency ordering must not be bypassed: the narrow adapter permits only a selected already-queued non-head technology with all `PreTechs` and `PreTechsImplicit` proven through `history.TechUnlocked(id, tech.PreTechsMax)`, exactly as `AlterCurrentTech` checks. Moving that independent/prerequisite-complete target earlier preserves every other order; paused, duplicate-level, sparse or oversized queues reject. No arbitrary sort order is accepted from MCP.

Optional `prioritizeQueued=true` on existing select-research prepare/commit uses Core `ResearchQueuePolicy`, then rechecks native eligibility and the fresh selection hash. The synchronous native call is followed by exact queue/current-tech, every uploaded/required hash, level/unlock/tick and package comparison. The normal action boundary quarantines an unproved result; no manual rollback, progress mutation or synthetic materials occur. This does not call `RemoveTechInQueue`, pause/resume, or a keyboard/mouse handler. Default enqueue remains unchanged. Local need: 2701 was appended after3402/1202 and requires ordinary circuits; a successful enqueue alone does not unlock blueprint capacity.

Offline follow-up:16 Core queue/progress regressions and both MCP option mappings pass; six more finite-store tests reject null nested edges/budgets, invalid endpoints and oversized parameter arrays as `InvalidDataException` before fingerprinting, rather than leaking a null-reference failure from attachment. Locked restore, Debug and Release tests **510 passed** (21 Contracts/454 Core/35 MCP), complete Release0 warnings/errors. New priority/Governor/collider cohort remains source-only until the next normal shutdown/install/resume; live queue priority and material completion are not yet claimed.

Cold deployment of the510-test cohort: after save23140359/J55/55 and the ten-accepted-action audit, `CloseMainWindow` completed normally and both process/descriptor counts reached0. The development installer copied all four DLLs while stopped; installed hashes matched4/4: Plugin `109B03020675DF0481E2504514FF34C8B1CA0C97BB476B3206CC0B0E1036B7C7`, Contracts `A3416B1B0B5E356ACF417CEE21798BFE58A48FA98F960D0E3BA30E5208EEDD3A`, Core `5BA234B7F607DA7695FFE4F01C8127E8B6A56AFCA72B37510D8936FA27E40FB3`, Newtonsoft `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. Independently launched source MCP completed real stdio initialize/tools/list/resources/list/resources/read:0.4.0.0,64 tools/1 resource, priority parameter and Governor read-only annotation present, playbook15609 characters including both, exit0 after stdin closed. No gameplay request occurred in that metadata probe. Protected resume and new game-domain checks follow separately; this remains a development install, not a final or cross-computer package.

Local live priority follow-up (same510-test installation): protected resume `d21617fd-3507-41df-9f47-aaf3f06ded1f` completed and automatically saved23140390, same owned identity/planet104/Journal55. Priority action `446d7daa-4f00-4620-8b84-c273cf1fba5a` at23147953 changed[3402,1202,2701] to[2701,3402,1202], with all research progress and inventory preserved in the immediate readback. A later ordinary100-circuit transfer, automatic research-item management and native research ticks completed2701 at23177285; fresh23178354 showed12000/12000/unlocked and the remaining queue[3402,1202]. Sorting did not itself unlock the technology. This proves current-DLL local priority, not blueprint construction or an independent final-package test.

### Native per-object connection projection correction

The current DLL `BuildTool_Path.CreatePrebuilds` iterates each preview and writes its nonzero `outputObjId/outputFromSlot/outputToSlot` (or output preview) to the native connection pool. A complete blueprint graph also contains belt-to-sorter pickup edges. The original finite-step adapter projected every outgoing edge to the same preview fields, so a later pickup edge could replace the approved downstream belt with an unbuilt sorter ID0. No live blueprint commit had occurred when this was found.

Core now projects only belt-to-belt outputs for belt creation, both endpoints for sorter creation and no sorter edges for machine creation. Duplicate eligible endpoints fail closed; edge ordering cannot choose the winner. Dependency ordering, exact native debit/drone construction and complete reciprocal/free-end verification are unchanged. Eleven new regressions plus all521 Debug/Release tests (21 Contracts/465 Core/35 MCP) pass; full Release build has0 warnings/errors. This follow-up is not in the currently running510-test cohort; first blueprint construction remains held until normal save/close and a matching cold deployment (IFX-033/EXP-201).

521-test cold deployment: after save23213258 and the full five-action early audit, normal window close succeeded, DSP process and runtime descriptor counts both reached0. All four installed DLLs matched Release outputs: Plugin `86F380C5559BFF74901874CEA74CE69B44B7A1CE6D9E26AC6E8466B6C53D9597`, Core `DA8998E6D8795F014947EDF370D71BCEDB8E0A7B22D464608312A287477A6739`, Contracts `A3416B1B0B5E356ACF417CEE21798BFE58A48FA98F960D0E3BA30E5208EEDD3A`, Newtonsoft unchanged. Protected resume `d829f535-047f-4bc9-8e49-6ee09f5a7857` then completed and automatically saved23213289; fresh23215523–38 remained owned/healthy/planet104/Journal55/55. First prepare timed out without an action; only a successful fresh prepare was committed. This is a development cohort, not the final package.

Governor local-live first read (prior510-test cohort) at target1203@30/min with source724: the600tick23201207–23201806 window was ready but production/consumption0, baseline warming_up with0 independent samples, executable/balanced bothfalse. Native blueprint capacity subsequently read150 with whole inventory and technology satisfied for the old five-object source; its occupied original site and sorter `ErrorInserterData` remained rejected before native conditions ran. Neither result is a positive running-module copy or2× throughput claim.

## Material compilation

### Native inserter candidate-angle selection (2026-09-06; new adapter live pending)

Current baseline DSP0.10.34.28529 / Assembly-CSharp SHA-256 `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85` remains unchanged. `BuildTool_Inserter.DeterminePreviews` compares each device slot's outward vector with the direction to the opposite endpoint, plus the deviation from opposite slot directions. The selected pair's maximum angular error must be below14 degrees for two non-belt devices, below11 in the final belt branch (which has additional native offset searches). It sets `TooSkew` during candidate selection, before `CheckBuildConditions`. Our previous exact-slot enumerator skipped this earlier stage. The bounded adapter now implements only the conservative straight subset, after checking positive live pool identities; it does not claim to reproduce bent/offset search, nor adjust any game object.

`BlueprintUtils.Refresh` separately rebuilds sorter endpoints from connected native slot poses, tests displacement/grid geometry and rejects a negative dot between a slot outward vector and the direction to the other endpoint with `ErrorInserterData`. Site objects now expose those two nullable diagnostic dots from copied preview poses; belt/open ends are null. No condition is cleared and no source pose is changed. Fifteen pure geometry tests cover facing/backwards pairs,11/14-degree limits, pair opposition and invalid vectors. The preceding full Release run passed582 tests; final identity-guard and documentation changes require the next regression run.

Local live on the older554-test installation: explicit [715,716,717,720,719] export retained recipe60, both native30-grid/default storage settings and internal slot connections. Sites at ticks23348926 and23348937 independently returned `ErrorInserterData` on both sorters, alongside965 and298/304 occupancy respectively. No blueprint was committed. This proves the reported native rejection, not yet the exact old sorter facing dots or a successful fix. Normal save23355362 and a complete four-action audit (Journal55/55,2267built/0prebuild, healthy, all power networks supplied) precede the planned cold update. Cargo and source boundary connections are not copied; normal reselection or new seed construction must be explicit.

### Stable source binding correction (2026-09-06; live pending)

Follow-up source-only throughput observer reuses exactly the same copied native Overseer600-tick windows; no new game API or write is introduced. A returned ready proposal must be explicitly locked by its exact hash before a revision/source change and within3600 game ticks. Copied declaration scalars cannot be replaced by later DTO changes or a caller's historical rate. At most8 passive runs use constant-size summaries; overlapping windows contribute only their union, gaps and invalid conditions reset progress, and fresh source changes cannot reuse pre-change windows. Same-session memory only; restart deliberately makes old hashes unavailable. Thirty-five new Core tests and the complete617-test Release suite initially pass. This is not deployment/live2× acceptance, and its sampled power/diagnostics do not certify every intervening tick.

582-test cold deployment and final regression: locked restore, Debug/Release582 tests (21Contracts/526Core/35MCP), full Release0 warnings/errors and26 package-policy checks passed. After saved23355362/four-action audit, normal CloseMainWindow of the exact DSP process succeeded, both process/descriptor counts reached0, and all four installed files matched: Plugin `ABB100B85C6C14B134515A78913D513A946828AB8ABBDB814061DC45FA3AA4B3`, Contracts `286BC9145D859A46B308938A20923345784EE128AC0A4D7F4F07727A7ACD0413`, Core `050CA6B0882C4A151A44EB67968AAC0AC1AAEE04A20C89B8F5D9C4652E1FA11A`, Newtonsoft unchanged. This development installation includes static binding and native angle guards, but not the later617-test throughput observer. No ZIP was generated.

Review found that the Governor adapter hashed `FactoryConfiguration`, which intentionally includes cargo buffers and inserter held stacks. Thus normal output accumulation changed the binding and cleared independent windows. `GovernorSourceBinding` now hashes only static current entity/recipe/endpoint/network/settings; stock changes remain independently measured. Existing native `AssemblerComponent.forceAccMode` read paths already prove its semantics; it is now exposed in the general entity snapshot, included with sorter filters in fresh selection hashes, and checked against native blueprint export parameters. No new native writes or custom production loop is introduced. Thirteen regressions simulate four genuinely progressing production/storage samples, establish three independent windows, and reject real recipe/filter/mode/network/pose/storage/source changes; total567 Release tests and full Release build pass. Neither this offline proof nor the older zero-output live proposal certifies a nonzero live baseline or ten-minute expansion.

### Ordinary storage blueprint extension (2026-09-06; live pending)

554-test cold deployment followed normal save23304910, full four-action audit and normal process close (process/descriptor0). All installed DLLs matched current Release output: Plugin `9A4F79691E03AC2F3743B208D69BCFFB93E9370E21E00F2C1ADE53E53BF38E56`, Contracts `65972FE2F75ADA3ACF79C4627EEE9C9548EEF169FA618A532CD920E5450917C2`, Core `E9B7A490A7F694FD5AFAA0199D835663D42208CDC81189E5039F20DD1B0C4462`, Newtonsoft `F0C07AF0E84D4DD4DA4BD7823BA4535BC0481B3BF623ED40B659B68147A6BB75`. The wrapper incorrectly checked a stale native LASTEXITCODE after the PowerShell installer returned success; read-only hash verification proved the installation, without repeating it. This is not yet a final release package or a passed storage build.

The current DLL baseline remains DSP0.10.34.28529, SHA-256 `AE0BA95F75BD879A62AA4CE253B2AB78EAA4FB3C7C595F5E1FEE75EBE0E0EF85`. `BuildingParameters.FromFactoryObject` reads `storagePool[entity.storageId]`, copying only `bans`, native type and grid filters, never cargo. `ToParamsArray` emits110 integers:0 bans,1 mode0/9,2–9 reserved,10–109 filters. Native `ApplyPrebuildParametersToEntity` (called by normal construction, never directly by Spherewright) uses `SetBans` (which would clamp), `SetFilter` and Default/Filtered mode; its indexed `StorageComponent.itemStackCount[filter]` must not receive unvalidated IDs. `PrefabDesc.storageCol*storageRow` defines actual capacity, separate from connection ports. Unknown layouts, ignored fields, filters beyond capacity, invalid stack IDs, clamped bans and stacked storage links are refused. Positive source IDs are checked before native export allocation; configuration roundtrip and completed-entity native readback are mandatory. The ordinary BuildTool_Click path still performs item debit/prebuild creation and native drones perform completion; no direct proto/configuration/cargo mutation is added.

The motivating live521-test selection was storage716 → sorter720 → smelter715(recipe60) → sorter719 → storage717. Including stores failed with `blueprint_type_unsupported`; omitting them created open sorter ends and correctly failed site preflight. There was no accepted blueprint write. Source-only storage extension adds33 pure tests (including explicit bounds, exact settings, stale hashes, closed graph, stack/port rejection and filter technology), with554 Debug/Release tests and a full Release build passing. Live configuration/copy/save/resume remain separate pending evidence.

`FoundryPlanCompiler` reuses `RuntimeDependencyGraphBuilder`, then selects unlocked runtime producers with compatible available buildings. Explicit item/recipe/building choices take precedence; otherwise selection is deterministic (single-output recipe, recipe ID, building grade, building ID). Raw `ItemProto.isRaw` entries stop recursion unless explicitly recipe-selected; other externally supplied intermediates must be named explicitly. A cycle or unusable selection is rejected, not silently fed from inventory.

Demand is aggregated in reverse topological order before sizing shared upstream producers. Output batch sizes are used for both cycles and rates. Coproducts are listed as required sinks and are not silently credited as free inputs. Bounds are 64 stages, 16 recipe levels and 256 production machines, with finite catalog/choice sizes and checked decimal/integer budgets. Unrepresentably small flows fail closed.

The material hash includes selected recipe types, times, input/output counts, machine speed/power, demand and explicit supply boundaries. It excludes session and capture tick so an identical calculation is reproducible after restart; **it is not a state hash, ownership proof or write token**. In particular, doubling all recipe batch counts can preserve material demand and machine count but changes capacity and must change this hash.

## Validation status and remaining work

### Machine-site preview (2026-09-06)

Installed and compile-reference `Assembly-CSharp.dll` hashes were independently reread and still match the baseline above. Targeted current-assembly ILSpy inspection confirms `PlanetAuxData.Snap(Vector3,bool)` uses the active native grid and `realRadius+0.2f` for its on-terrain placement shell; **Snap itself does not prove dry/clear/buildable terrain**. `Maths.SphericalRotation`, already used by ordinary building preparation, supplies the runtime tangent frame. Core produces only a bounded centred candidate grid (32 machines, 64 m maximum tangent offset), not an autonomous site search.

`PrefabDesc.hasBuildCollider`, `buildCollider`, `buildColliders` and `ColliderData.shape/pos/ext/radius` provide build volumes. Inspection of `ColliderData.InitFromCollider` proves that Box uses half-extents, Capsule uses an oriented half-segment plus a separate radius, and Sphere uses only that radius. Core computes an origin-centred sphere as `|pos|+|ext|` for Box, `|pos|+|ext|+radius` for Capsule, or `|pos|+radius` for Sphere. The adapter exposes their maximum as `placementRadius` over the primary and additional build colliders (at most 64 additional colliders). This rotation-independent bound is deliberately conservative and separate from render-oriented `roughRadius`; unknown/nonfinite shapes fail closed. A pre-deployment audit caught and fixed the first draft's omission of sphere/capsule radius, with focused regressions (IFX-025). Pairwise clearance is checked **after** grid snapping, including a 0.5 m margin.

`NormalGameActionCoordinator.InspectFoundryMachine` calls the **current structured** `TryValidateClickBuild` path. Its player-owned UI and existing entity/prebuild collider guards run before the isolated native preview; the older `ValidateExactCoreBuildCandidate` helper alone is insufficient because it lacks that occupied-object guard. Site scans reject factories whose entity or prebuild cursor exceeds 131072. Optional evidence captures an occupied object ID (positive entity, negative prebuild), whether native checking ran, and its first blocking condition, without changing ordinary build callers. Native validation binds the exact local factory, copies the actual package/in-hand state, adds one temporary `BuildPreview`, calls `CheckBuildConditions`, and frees preview/snapshot/tool in `finally`. It consumes only `tmpPackage`; `CreatePrebuilds` is separate and never called by this reader. A native-adjusted position or rotation requires a new explicit assessment. No virtual inventory is added. Whole-plan package demand is checked separately because independent preview copies could otherwise reuse the same single building item. A first `NotEnoughItem` or `OutOfReach` result is not proof that later native conditions would pass.

The optional site assessment is not an executable graph or write authority. It has session/revision/player and pose/condition/inventory evidence, but does not fingerprint every world field or route logistics. Its `assessmentHash` cannot replace ordinary action state hashes. Offline tests cover deterministic spherical layout, bounds, invalid frames/coordinates, post-snap collision, aggregate inventory, native negative/unperformed conditions, occupied-object rejection even with an inconsistent native Ok flag, hash changes and read-only MCP discovery. At this implementation checkpoint, all **317** Core/Contracts/MCP tests and the complete current-DSP Release build pass (0 warnings/errors). Subsequent same-batch local live: occupied entity1491 at ticks19785959/19785969 rejected before native checking; single-smelter candidate at tick19790031 actually ran native checking and returned NotEnoughItem with whole budget missing1. This proves those negative paths only; native Ok, completed site construction and cross-computer live remain unverified.

### Earlier material-compiler evidence

- Offline: shared-input aggregation, 0.75× production, output batches, exact choices, cycles, locked inputs, unknown speeds, bounds/overflow/underflow, canonical hashes and read-only MCP mapping are covered by automated tests.
- Full current-DSP Release compilation is checked separately from the no-game-DLL test suite.
- Local live: same-batch `b9e74bd` Plugin deployment matched all four DLL hashes after normal exit. Protected resume restored the same long-running owned world from minimum tick `19369335`, automatically saved `19369366`, and retained Journal `51/51` durable. A fresh source Release MCP Host advertised 54 tools/1 resource; its real `spherewright_get_foundry_plan` call at tick `19392231` succeeded. Target motor `1203 @ 30/min` returned depth 3, 8 machines, base working power `2520000 W`, external iron ore `120/min`, copper ore `15/min`, and no byproducts. Two equivalent calls had the same material hash. Rate zero and a wrong session were rejected as `INVALID_REQUEST` / `STALE_SESSION` without any gameplay action.
- The live build catalog independently reported smelter `2302`: speed `10000`, work energy `6000/tick`; assembler `2303`: speed `7500`, work energy `4500/tick`. Both were unlocked/available grade 1. The plan therefore cost four of each, excluding logistics and power infrastructure. No machine was constructed in this check.
- This earlier material-only validation did not prove native site feasibility, sufficient ongoing automatic supply, exact belt/sorter/storage/power cost, durable action graph, restart reconciliation, or completed Foundry construction. The optional site phase is tracked separately above; both public phases explicitly return `executable=false`. Normal owned resume is proven here, **not** resumable Foundry construction.
- Cross-computer live validation of this new capability has not been performed.

Local live stage readback (rates are items/game minute, not newly measured factory output):

| Item | Recipe | Required rate | Base rate per machine | Machines | Installed capacity |
|---|---|---:|---:|---:|---:|
| Iron ingot 1101 | 1 | 90 | 60 | 2 | 120 |
| Gear 1201 | 5 | 30 | 45 | 1 | 45 |
| Magnet 1102 | 2 | 30 | 40 | 1 | 40 |
| Copper ingot 1104 | 3 | 15 | 60 | 1 | 60 |
| Magnetic coil 1202 | 6 | 30 | 90 | 1 | 90 |
| Electric motor 1203 | 97 | 30 | 22.5 | 2 | 45 |

Rejected alternatives: UI request-dependent `refProductSpeed` cache; hard-coded game recipes; assumed 1× machine speed; finite inventory as sustained supply; passing mutable runtime pools into Core; treating a material calculation as permission to build.
